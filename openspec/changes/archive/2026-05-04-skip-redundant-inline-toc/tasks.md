## 1. Утилиты и критерий

- [x] 1.1 Добавить извлечение «текста для проверки дублей»: без блока `<!-- toc:start -->`…`<!-- toc:end -->`, без строки крошек `**↑**…`
- [x] 1.2 Реализовать проверку «`](path)` есть в тексте» буквально по строке `path` как в будущем TOC
- [x] 1.3 Собрать множество валидных целей TOC: непустой `path`, файл `out_dir / path` существует; пустые и битые цели исключить из вывода и из проверки «незакрытая ссылка»

## 2. Рендер и инжекция

- [x] 2.1 Изменить `render_inline_toc` (или обёртку): принимать `out_dir` и `scan_text`, возвращать полный markdown-блок или пустую строку по правилам спеки
- [x] 2.2 Обновить `inject_inline_toc_into_content`: при пустом TOC удалять существующий блок и сохранять файл при изменении; при непустом — прежняя идемпотентность
- [x] 2.3 Вызвать из этапа инжекции (там, где сейчас строится inline TOC): читать файл, вычислять `scan_text`, передавать `out_dir`

## 3. Тесты

- [x] 3.1 Юнит-тест: все валидные пути уже в теле → TOC не генерируется / пустая строка
- [x] 3.2 Юнит-тест: есть валидный путь не в теле → полный блок с `## Оглавление`
- [x] 3.3 Юнит-тест: только пустые/битые цели → пустой TOC, инжекция снимает блок при необходимости
- [x] 3.4 Юнит-тест: `inject_inline_toc_into_content` с пустой строкой удаляет маркеры

## 4. Проверка

- [x] 4.1 `python -m pytest tests/test_toc.py` (и при затронутых местах полный набор тестов проекта)
