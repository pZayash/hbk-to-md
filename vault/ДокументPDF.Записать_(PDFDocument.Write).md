# ДокументPDF.Записать

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Общие объекты](Общие_объекты.md) › [Работа с PDF](Работа_с_PDF-2.md) › [ДокументPDF](ДокументPDF_(PDFDocument).md) › [Методы](ДокументPDF__Методы.md)

ДокументPDF (PDFDocument)

Записать (Write)

Доступен, начиная с версии 8.3.21.

Вариант синтаксиса: В файл

Синтаксис:

Записать(<ИмяФайла>, <Пароль>, <ТипФайла>)

Параметры:

<ИмяФайла> (обязательный)

Тип: [Строка](lang__def_String.md).   
Имя файла для записи PDF-документа.

<Пароль> (необязательный)

Тип: [Строка](lang__def_String.md).   
Пароль для доступа к PDF-документу.

<ТипФайла> (необязательный)

Тип: [ТипФайлаДокументаPDF](ТипФайлаДокументаPDF_(PDFDocumentFileType).md), [Неопределено](lang__def_Undefined.md).   
Тип файла документа PDF. Если содержит [Неопределено](lang__def_Undefined.md), то в качестве значения параметра используется свойство [ТипФайла](ДокументPDF.ТипФайла_(PDFDocument.FileType).md) объекта [ДокументPDF](ДокументPDF_(PDFDocument).md).

Описание варианта метода:

Записывает документ PDF.  
Если документ имеет электронную цифровую подпись и выполняется запись с паролем, то генерируется исключение.  
  
Доступность:  
Тонкий клиент, сервер, толстый клиент.

Вариант синтаксиса: В поток

Синтаксис:

Записать(<Поток>, <Пароль>, <ТипФайла>)

Параметры:

<Поток> (обязательный)

Тип: [Поток](Поток_(Stream).md), [ПотокВПамяти](ПотокВПамяти_(MemoryStream).md), [ФайловыйПоток](ФайловыйПоток_(FileStream).md).   
Поток для записи PDF-документа.

<Пароль> (необязательный)

Тип: [Строка](lang__def_String.md).   
Пароль для доступа к PDF-документу.

<ТипФайла> (необязательный)

Тип: [ТипФайлаДокументаPDF](ТипФайлаДокументаPDF_(PDFDocumentFileType).md), [Неопределено](lang__def_Undefined.md).   
Тип файла документа PDF. Если содержит [Неопределено](lang__def_Undefined.md), то в качестве значения параметра используется свойство [ТипФайла](ДокументPDF.ТипФайла_(PDFDocument.FileType).md) объекта [ДокументPDF](ДокументPDF_(PDFDocument).md).

Описание варианта метода:

Записывает документ в поток PDF-документа.  
Если документ имеет электронную цифровую подпись и производится запись с паролем, то генерируется исключение.

Описание:

Записывает изменения в PDF-документ.

Доступность:

Тонкий клиент, веб-клиент, сервер, толстый клиент.

См. также:

[ЗаписьPDF](ЗаписьPDF_(PDFWriter).md), метод [ЗаписатьВложения](ЗаписьPDF.ЗаписатьВложения_(PDFWriter.WriteAttachments).md)  
[ЗаписьPDF](ЗаписьPDF_(PDFWriter).md), метод [НачатьЗаписьВложений](ЗаписьPDF.НачатьЗаписьВложений_(PDFWriter.BeginWriteAttachments).md)  
[ЗаписьPDF](ЗаписьPDF_(PDFWriter).md), метод [ЗаписатьВложенияАсинх](ЗаписьPDF.ЗаписатьВложенияАсинх_(PDFWriter.WriteAttachmentsAsync).md)  

Использование в версии:

Доступен, начиная с версии 8.3.21.

Описание изменено в версии 8.3.24.

---
