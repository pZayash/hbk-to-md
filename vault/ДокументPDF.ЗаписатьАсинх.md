# ДокументPDF.ЗаписатьАсинх

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog2657.md">Работа с PDF</a> › [ДокументPDF](ДокументPDF.md)

<!-- signature:start -->
`ЗаписатьАсинх`(`<ИмяФайла>`: `Строка`, `<Пароль>`: `Строка`, `<ТипФайла>`: [`ТипФайлаДокументаPDF`](ТипФайлаДокументаPDF.md) | `Неопределено`)

`ЗаписатьАсинх`(`<Поток>`: [`Поток`](Поток.md) | [`ПотокВПамяти`](ПотокВПамяти.md) | [`ФайловыйПоток`](ФайловыйПоток.md), `<Пароль>`: `Строка`, `<ТипФайла>`: [`ТипФайлаДокументаPDF`](ТипФайлаДокументаPDF.md) | `Неопределено`) → [`Обещание`](Обещание.md)
<!-- signature:end -->

ДокументPDF (PDFDocument)

ЗаписатьАсинх (WriteAsync)

Доступен, начиная с версии 8.3.21.

Вариант синтаксиса: В файл

Синтаксис:

ЗаписатьАсинх(<ИмяФайла>, <Пароль>, <ТипФайла>)

Параметры:

<ИмяФайла> (обязательный)

Тип: Строка.   
Имя файла для записи PDF-документа.

<Пароль> (необязательный)

Тип: Строка.   
Пароль для доступа к PDF-документу.

<ТипФайла> (необязательный)

Тип: [ТипФайлаДокументаPDF](ТипФайлаДокументаPDF.md), Неопределено.   
Тип файла документа PDF. Если содержит Неопределено, то в качестве значения параметра используется свойство [ТипФайла](ДокументPDF.ТипФайла.md) объекта [ДокументPDF](ДокументPDF.md).

Описание варианта метода:

Асинхронная функция. Записывает документ PDF.  
Если документ имеет электронную цифровую подпись и выполняется запись с паролем, то генерируется исключение.  
  
Доступность:  
Тонкий клиент, толстый клиент, веб-клиент.

Вариант синтаксиса: В поток

Синтаксис:

ЗаписатьАсинх(<Поток>, <Пароль>, <ТипФайла>)

Параметры:

<Поток> (обязательный)

Тип: [Поток](Поток.md), [ПотокВПамяти](ПотокВПамяти.md), [ФайловыйПоток](ФайловыйПоток.md).   
Поток для записи PDF-документа.

<Пароль> (необязательный)

Тип: Строка.   
Пароль для доступа к PDF-документу.

<ТипФайла> (необязательный)

Тип: [ТипФайлаДокументаPDF](ТипФайлаДокументаPDF.md), Неопределено.   
Тип файла документа PDF. Если содержит Неопределено, то в качестве значения параметра используется свойство [ТипФайла](ДокументPDF.ТипФайла.md) объекта [ДокументPDF](ДокументPDF.md).

Описание варианта метода:

Асинхронная функция. Записывает изменения в документ PDF.  
Если документ имеет электронную цифровую подпись и выполняется запись с паролем, то генерируется исключение.  
  
Доступность:  
Тонкий клиент, толстый клиент, веб-клиент.

Возвращаемое значение:

Тип: [Обещание](Обещание.md).   

Описание:

Асинхронная функция. Записывает изменения в PDF-документ.

Доступность:

Тонкий клиент, веб-клиент, сервер, толстый клиент.

Примечание:

Для работы метода в веб-клиенте требуется наличие расширения работы с файлами.

См. также:

[ДокументPDF](ДокументPDF.md), метод [Записать](ДокументPDF.Записать.md)  

Использование в версии:

Доступен, начиная с версии 8.3.21.

Описание изменено в версии 8.3.24.

---
