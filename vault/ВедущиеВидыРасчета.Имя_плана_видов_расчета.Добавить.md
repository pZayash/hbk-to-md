# ВедущиеВидыРасчета.<Имя плана видов расчета>.Добавить

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Прикладные_объекты.md">Прикладные объекты</a> › <a href="obsidian://open?file=Планы_видов_расчета.md">Планы видов расчета</a> › [ВедущиеВидыРасчета.<Имя плана видов расчета>](ВедущиеВидыРасчета.Имя_плана_видов_расчета.md)

<!-- signature:start -->
`Добавить`() → [`ВедущиеВидыРасчетаСтрока.\<Имя плана видов расчета\>`](ВедущиеВидыРасчетаСтрока.Имя_плана_видов_расчета.md)
<!-- signature:end -->

ВедущиеВидыРасчета.<Имя плана видов расчета> (LeadingCalculationTypes.<Chart of calculation types name>)

Добавить (Add)

Доступен, начиная с версии 8.0.

Синтаксис:

Добавить()

Возвращаемое значение:

Тип: [ВедущиеВидыРасчетаСтрока.\<Имя плана видов расчета\>](ВедущиеВидыРасчетаСтрока.Имя_плана_видов_расчета.md).   

Описание:

Добавляет строку в конец табличной части.

Доступность:

Сервер, толстый клиент, внешнее соединение.

Примечание:

Использование метода допустимо только в том случае, если табличная часть получена из свойства объекта. Если табличная часть получена из свойства ссылки (или выборки), то использование этого метода будет вызывать ошибку выполнения.

Пример:

|  |
| --- |
| Процедура УстановитьВедущиеРасчеты(ВидРасчета, ВедущиеВидыРасчета)     ВР = ВидРасчета.ПолучитьОбъект();     ВР.ВедущиеВидыРасчета.Очистить();     Если ВедущиеВидыРасчета <> Неопределено Тогда         Для каждого Элемент Из ВедущиеВидыРасчета Цикл             НоваяСтрока = ВР.ВедущиеВидыРасчета.Добавить();             НоваяСтрока.ВидРасчета = Элемент.Значение;         КонецЦикла;     КонецЕсли;      ВР.Записать(); КонецПроцедуры |

Использование в версии:

Доступен, начиная с версии 8.0.

---
