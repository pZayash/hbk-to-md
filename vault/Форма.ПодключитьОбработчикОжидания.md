# Форма.ПодключитьОбработчикОжидания

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Интерфейс-2.md">Интерфейс</a> › <a href="obsidian://open?file=Форма.md">Форма</a> › [Форма](Форма-2.md)

<!-- signature:start -->
`ПодключитьОбработчикОжидания`(`<ИмяПроцедуры>`: `Строка`, `<Интервал>`: `Число`, `<Однократно>`: `Булево`)
<!-- signature:end -->

Форма (Form)

ПодключитьОбработчикОжидания (AttachIdleHandler)

Доступен, начиная с версии 8.0.

Синтаксис:

ПодключитьОбработчикОжидания(<ИмяПроцедуры>, <Интервал>, <Однократно>)

Параметры:

<ИмяПроцедуры> (обязательный)

Тип: Строка.   
Имя процедуры, подключаемой в качестве обработчика ожидания.  
Процедура не должна содержать параметров.

<Интервал> (обязательный)

Тип: Число.   
Интервал времени в секундах с точностью до 1/10 секунды, через который будет осуществляться вызов процедуры (положительное число).  
Если указано значение меньше 1, то значение третьего параметра должно быть равно Истина.  
Внимание! Не следует использовать данный обработчик в качестве таймера. Фактический интервал срабатывания может отличаться от указанного.

<Однократно> (необязательный)

Тип: Булево.   
Признак однократного выполнения обработчика ожидания.  
Истина - указанный обработчик ожидания будет выполнен один раз.  
Значение по умолчанию: Ложь.

Описание:

Подключает указанную процедуру в качестве обработчика ожидания. Процедура будет вызываться в период ожидания системы каждый раз по истечению указанного интервала времени.  
Вызов будет осуществляться только в "состоянии покоя", то есть в тот момент, когда программа не выполняет никаких действий.  
Во время временной блокировки, вызванной методом [ЗаблокироватьРаботуПользователя](Глобальный_контекст.ЗаблокироватьРаботуПользователя.md) или соответствующей командой "Временная блокировка", обработчики ожидания не выполняются.

Доступность:

Толстый клиент.

Примечание:

Вызов обработчика ожидания продолжается пока форма не будет закрыта или пока не будет вызван метод формы [ОтключитьОбработчикОжидания](Форма.ОтключитьОбработчикОжидания.md).

Пример:

|  |
| --- |
| ПодключитьОбработчикОжидания("ПриОжидании", 1); |

См. также:

[Форма](Форма.md), метод [ОтключитьОбработчикОжидания](Форма.ОтключитьОбработчикОжидания.md)  
[Глобальный контекст](Глобальный_контекст.md), метод [ПодключитьОбработчикОжидания](Глобальный_контекст.ПодключитьОбработчикОжидания.md)  

Использование в версии:

Доступен, начиная с версии 8.0.

---
