# ЭлементDOM.УстановитьСоответствиеПространстваИмен

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog1055.md">DOM</a> › [ЭлементDOM](ЭлементDOM.md)

<!-- signature:start -->
`УстановитьСоответствиеПространстваИмен`(`<Префикс>`: `Строка`, `<URIПространстваИмен>`: `Строка`, `<Согласовать>`: `Булево`)

`УстановитьСоответствиеПространстваИмен`(`<URIПространстваИмен>`: `Строка`, `<Согласовать>`: `Булево`) → [`АтрибутDOM`](АтрибутDOM.md) | `Неопределено`
<!-- signature:end -->

ЭлементDOM (DOMElement)

УстановитьСоответствиеПространстваИмен (SetNamespaceMapping)

Доступен, начиная с версии 8.1.

Вариант синтаксиса: Основной

Синтаксис:

УстановитьСоответствиеПространстваИмен(<Префикс>, <URIПространстваИмен>, <Согласовать>)

Параметры:

<Префикс> (обязательный)

Тип: Строка.   
Префикс пространства имен.

<URIПространстваИмен> (обязательный)

Тип: Строка.   
URI пространства имен.

<Согласовать> (необязательный)

Тип: Булево.   
Признак необходимости согласования префиксов дочерних элементов и атрибутов для указываемого пространства имен. Для атрибутов согласование выполняется только в случае непустого существующего префикса.  
Значение по умолчанию: Ложь.

Описание варианта метода:

Если для данного префикса уже установлено соответствие пространству имен, то никаких действий производиться не будет, и будет возвращено Неопределено.  
Причины вызова исключений:

- указанный префикс содержит символы, недопустимые стандартом XML, соответствующим версии XML, указанной в свойстве [ВерсияXML](ДокументDOM.ВерсияXML.md);
- данный узел в режиме Только для чтения;
- <Префикс> и <URIПространстваИмен> не соответствуют спецификации XML Namespaces.

Вариант синтаксиса: Автоматическое создание префикса

Синтаксис:

УстановитьСоответствиеПространстваИмен(<URIПространстваИмен>, <Согласовать>)

Параметры:

<URIПространстваИмен> (обязательный)

Тип: Строка.   
URI пространства имен.

<Согласовать> (необязательный)

Тип: Булево.   
Признак необходимости согласования префиксов дочерних элементов и атрибутов для указываемого пространства имен. Для атрибутов согласование выполняется только в случае непустого существующего префикса.  
Значение по умолчанию: Ложь.

Описание варианта метода:

Префикс соответствия пространства имен будет сгенерирован автоматически. Если для данного пространства имен уже установлено соответствие, то никаких действий производиться не будет и будет возвращен соответствующий узел-атрибут.  
Причины вызова исключений:

- указанный URI пространства имен содержит символы, недопустимые стандартом XML, соответствующим версии XML, указанной в свойстве [ВерсияXML](ДокументDOM.ВерсияXML.md).
- данный узел в режиме Только для чтения.

Возвращаемое значение:

Тип: [АтрибутDOM](АтрибутDOM.md), Неопределено.   
Возвращает - атрибут, соответствующий созданному соответствию.  
Если для данного префикса уже установлено соответствие пространству имен, то никаких действий производиться не будет, и будет возвращено Неопределено.

Описание:

Создает и устанавливает в элемент атрибут соответствия пространства имен.

Доступность:

Сервер, толстый клиент, внешнее соединение, мобильное приложение (сервер), мобильный автономный сервер.

Использование в версии:

Доступен, начиная с версии 8.1.

---
