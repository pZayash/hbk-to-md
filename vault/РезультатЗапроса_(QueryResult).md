# РезультатЗапроса

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Работа с запросами](Работа_с_запросами.md) › [Выполнение и работа с запросами во встроенном языке](Выполнение_и_работа_с_запросами_во_встроенном_языке.md)

Доступен, начиная с версии 8.0.

Свойства:

[ВремяАктуальностиДанных (DataRelevanceTime)](РезультатЗапроса.ВремяАктуальностиДанных_(QueryResult.DataRelevanceTime).md)  
[ДанныеАктуальны (DataIsRelevant)](РезультатЗапроса.ДанныеАктуальны_(QueryResult.DataIsRelevant).md)  
[Колонки (Columns)](РезультатЗапроса.Колонки_(QueryResult.Columns).md)  
[КопияБазыДанных (DatabaseCopy)](РезультатЗапроса.КопияБазыДанных_(QueryResult.DatabaseCopy).md)  

Методы:

[Выбрать (Select)](РезультатЗапроса.Выбрать_(QueryResult.Select).md)  
[Выгрузить (Unload)](РезультатЗапроса.Выгрузить_(QueryResult.Unload).md)  
[Пустой (IsEmpty)](РезультатЗапроса.Пустой_(QueryResult.IsEmpty).md)  

Описание:

Содержит результат выполнения запроса. Предназначен для хранения и обработки полученных данных.

Доступность:

Сервер, толстый клиент, внешнее соединение, мобильное приложение (сервер), мобильный автономный сервер.   
Возможен обмен с сервером. Сериализуется.

Пример:

|  |
| --- |
| // форма отчета содержит табличный элемент ТабДок  // макет отчета содержит именованную область "Строка",  // которая состоит из ячеек с параметрами:   // Номенклатура НачОстаток Приход Расход КонОстаток  // Ячейка Номенклатура имеет параметр расшифровки Расшифровка   // свойство ИспользованиеРасшифровки установлено Строка.   // вывод шапки отчета   ... // текст запроса  ... // текст запроса в примере не приводится  ... РезультатЗапроса = Запрос.Выполнить(); ТабДок.НачатьАвтогруппировкуСтрок(); ВыборкаИтоги =      РезультатЗапроса.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам); ВыборкаИтоги.Следующий(); Выборка = ВыборкаИтоги.Выбрать(ОбходРезультатаЗапроса.Прямой); Пока Выборка.Следующий() Цикл     Область = Макет.ПолучитьОбласть("Строка");     // заполнение параметров строки отчета      Область.Параметры.Номенклатура =          Выборка.НоменклатураПредставление;     Область.Параметры.Расшифровка = Выборка.Номенклатура;     Область.Параметры.НачОстаток = Выборка.НачОстаток;     Область.Параметры.Приход = Выборка.Приход;     Область.Параметры.Расход = Выборка.Расход;     Область.Параметры.КонОстаток = Выборка.КонОстаток;     ТабДок.Вывести(Область, Выборка.Уровень()); КонецЦикла; ТабДок.ЗакончитьАвтогруппировкуСтрок();  // далее производится вывод подвала |

См. также:

[СводнаяТаблица](СводнаяТаблица_(PivotTable).md), свойство [ИсточникДанных](СводнаяТаблица.ИсточникДанных_(PivotTable.DataSource).md)  
[Запрос](Запрос_(Query).md), метод [Выполнить](Запрос.Выполнить_(Query.Execute).md)  
[ПостроительОтчета](ПостроительОтчета_(ReportBuilder).md), свойство [Результат](ПостроительОтчета.Результат_(ReportBuilder.Result).md)  
[ПостроительЗапроса](ПостроительЗапроса_(QueryBuilder).md), свойство [Результат](ПостроительЗапроса.Результат_(QueryBuilder.Result).md)  
[ВыборкаИзРезультатаЗапроса](ВыборкаИзРезультатаЗапроса_(QueryResultSelection).md), метод [Владелец](ВыборкаИзРезультатаЗапроса.Владелец_(QueryResultSelection.Owner).md)  

Использование в версии:

Доступен, начиная с версии 8.0.

---

<!-- toc:start -->
## Оглавление

### Подразделы (2)

- [Методы (3 страниц)](РезультатЗапроса__Методы.md)
- [Свойства (4 страниц)](РезультатЗапроса__Свойства.md)
<!-- toc:end -->
