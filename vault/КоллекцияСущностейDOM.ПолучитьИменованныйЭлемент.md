# КоллекцияСущностейDOM.ПолучитьИменованныйЭлемент

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog1055.md">DOM</a> › [КоллекцияСущностейDOM](КоллекцияСущностейDOM.md)

<!-- signature:start -->
`ПолучитьИменованныйЭлемент`(`<Имя>`: `Строка`) → [`СущностьDOM`](СущностьDOM.md)
<!-- signature:end -->

КоллекцияСущностейDOM (DOMEntityMap)

ПолучитьИменованныйЭлемент (GetNamedItem)

Доступен, начиная с версии 8.1.

Синтаксис:

ПолучитьИменованныйЭлемент(<Имя>)

Параметры:

<Имя> (обязательный)

Тип: Строка.   
Имя узла DOM.

Возвращаемое значение:

Тип: [СущностьDOM](СущностьDOM.md).   

Описание:

Получает узел DOM по имени.

Доступность:

Сервер, толстый клиент, внешнее соединение, мобильное приложение (сервер), мобильный автономный сервер.

Использование в версии:

Доступен, начиная с версии 8.1.

---
