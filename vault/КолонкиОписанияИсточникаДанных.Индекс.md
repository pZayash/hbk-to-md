# КолонкиОписанияИсточникаДанных.Индекс

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Прикладные_объекты.md">Прикладные объекты</a> › <a href="obsidian://open?file=Универсальные_объекты.md">Универсальные объекты</a> › [КолонкиОписанияИсточникаДанных](КолонкиОписанияИсточникаДанных.md)

<!-- signature:start -->
`Индекс`(`<Колонка>`: [`КолонкаОписанияИсточникаДанных`](КолонкаОписанияИсточникаДанных.md))
<!-- signature:end -->

КолонкиОписанияИсточникаДанных (DataSourceDescriptionColumns)

Индекс (IndexOf)

Доступен, начиная с версии 8.0.

Синтаксис:

Индекс(<Колонка>)

Параметры:

<Колонка> (обязательный)

Тип: [КолонкаОписанияИсточникаДанных](КолонкаОписанияИсточникаДанных.md).   
Искомая колонка.

Возвращаемое значение:

Индекс колонки, или -1, в случае, если колонка отсутствует в коллекции.

Описание:

Получить индекс колонки в коллекции.

Доступность:

Сервер, толстый клиент, внешнее соединение.

Использование в версии:

Доступен, начиная с версии 8.0.

---
