# БиблиотекаСтилей.Индекс

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Интерфейс](Интерфейс_(обычный).md) › [Общие интерфейсные объекты](Общие_интерфейсные_объекты.md) › [БиблиотекаСтилей](БиблиотекаСтилей_(StyleLib).md) › [Методы](БиблиотекаСтилей__Методы.md)

БиблиотекаСтилей (StyleLib)

Индекс (IndexOf)

Доступен, начиная с версии 8.0.

Синтаксис:

Индекс(<СсылкаНаСтиль>)

Параметры:

<СсылкаНаСтиль> (обязательный)

Тип: [Стиль](Стиль_(Style).md).   
Ссылка на стиль коллекции, для которой необходимо получить индекс.

Возвращаемое значение:

Тип: [Число](lang__def_Number.md).   
Индекс указанного стиля в коллекции. Если не найдено, то возвращается -1.

Описание:

Получает индекс указанного стиля в коллекции стилей.

Доступность:

Сервер, толстый клиент.

Пример:

|  |
| --- |
| ИндексСтиля = БиблиотекаСтилей.Индекс(ТекущийСтиль); |

Использование в версии:

Доступен, начиная с версии 8.0.

---
