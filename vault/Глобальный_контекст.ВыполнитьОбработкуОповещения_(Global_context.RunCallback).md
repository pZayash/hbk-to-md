# Глобальный контекст.ВыполнитьОбработкуОповещения

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Глобальный контекст](Глобальный_контекст.md) › [Методы](Методы.md) › [Процедуры и функции интерактивной работы](Процедуры_и_функции_интерактивной_работы.md)

Глобальный контекст (Global context)

ВыполнитьОбработкуОповещения (RunCallback)

Доступен, начиная с версии 8.3.3.

Синтаксис:

ВыполнитьОбработкуОповещения(<ВыполняемоеОповещение>, <Результат>)

Параметры:

<ВыполняемоеОповещение> (обязательный)

Тип: [ОписаниеОповещения](ОписаниеОповещения_(CallbackDescription).md).   
Содержит описание процедуры, вызов которой будет произведен со следующими параметрами:

- <Результат> - значение результата, переданное вторым параметром при вызове метода,
- <ДополнительныеПараметры> - значение, которое было указано при создании объекта оповещения.

<Результат> (необязательный)

Значение, которое будет передано в процедуру, описанную параметром <ВыполняемоеОповещение>. Содержит результат, который мог быть получен при помощи алгоритма с использованием блокирующих методов.  
Значение по умолчанию: [Неопределено](lang__def_Undefined.md).

Возвращаемое значение:

[Неопределено](lang__def_Undefined.md) - если в параметре <ВыполняемоеОповещение> передан объект оповещения инициализированный именем процедуры.  
Произвольное значение (результат выполнения функции), если в описание оповещения передана функция.

Описание:

Запускает выполнение процедуры (или функции), описанной в параметре <ВыполняемоеОповещение>.   
Вызывающая процедура может передать процедуру обработки результата в виде объекта [ОписаниеОповещения](ОписаниеОповещения_(CallbackDescription).md), которая будет вызвана после получения результата блокирующего метода.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, толстый клиент, мобильное приложение (клиент).

Примечание:

Используется в реализации процедур, которые внутри себя могут открывать блокирующие окна (например, вызывать метод [ПоказатьВопрос](Глобальный_контекст.ПоказатьВопрос_(Global_context.ShowQueryBox).md)) и при этом должны каким-то образом вернуть свой результат в вызывающие процедуры.

См. также:

[ОписаниеОповещения](ОписаниеОповещения_(CallbackDescription).md)  

Использование в версии:

Доступен, начиная с версии 8.3.3.

Описание изменено в версии 8.3.22.

---
