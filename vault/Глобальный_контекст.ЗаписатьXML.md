# Глобальный контекст.ЗаписатьXML

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Глобальный_контекст.md">Глобальный контекст</a> › [Процедуры и функции работы с XML](Процедуры_и_функции_работы_с_XML.md)

<!-- signature:start -->
`ЗаписатьXML`(`<ЗаписьXML>`: [`ЗаписьXML`](ЗаписьXML.md), `<Значение>`, `<НазначениеТипа>`: [`НазначениеТипаXML`](НазначениеТипаXML.md))

`ЗаписатьXML`(`<ЗаписьXML>`: [`ЗаписьXML`](ЗаписьXML.md), `<Значение>`, `<ПолноеИмя>`: `Строка`, `<НазначениеТипа>`: [`НазначениеТипаXML`](НазначениеТипаXML.md))

`ЗаписатьXML`(`<ЗаписьXML>`: [`ЗаписьXML`](ЗаписьXML.md), `<Значение>`, `<ЛокальноеИмя>`: `Строка`, `<URIПространстваИмен>`: `Строка`, `<НазначениеТипа>`: [`НазначениеТипаXML`](НазначениеТипаXML.md))
<!-- signature:end -->

Глобальный контекст (Global context)

ЗаписатьXML (WriteXML)

Доступен, начиная с версии 8.0.

Вариант синтаксиса: Записать без имени

Синтаксис:

ЗаписатьXML(<ЗаписьXML>, <Значение>, <НазначениеТипа>)

Параметры:

<ЗаписьXML> (обязательный)

Тип: [ЗаписьXML](ЗаписьXML.md).   
Объект, через который осуществляется запись XML.

<Значение> (обязательный)

Записываемое в поток XML значение. Тип параметра определяется совокупностью типов, для которых определена XML-сериализация.  
При записи объекта метаданных выгружаются все записи объекта метаданных, доступные для пользователя.

<НазначениеТипа> (необязательный)

Тип: [НазначениеТипаXML](НазначениеТипаXML.md).   
Определяет необходимость назначения типа элементу XML.  
Значение по умолчанию: [Неявное](НазначениеТипаXML.Неявное.md).

Описание варианта метода:

В качестве имени элемента будет использован тип значения.

Вариант синтаксиса: Записать с полным именем

Синтаксис:

ЗаписатьXML(<ЗаписьXML>, <Значение>, <ПолноеИмя>, <НазначениеТипа>)

Параметры:

<ЗаписьXML> (обязательный)

Тип: [ЗаписьXML](ЗаписьXML.md).   
Объект, через который осуществляется запись XML.

<Значение> (обязательный)

Записываемое в поток XML значение. Тип параметра определяется совокупностью типов, для которых определена XML-сериализация.  
При записи объекта метаданных выгружаются все записи объекта метаданных, доступные для пользователя.

<ПолноеИмя> (обязательный)

Тип: Строка.   
Полное имя элемента XML, в который будет записано значение.

<НазначениеТипа> (необязательный)

Тип: [НазначениеТипаXML](НазначениеТипаXML.md).   
Определяет необходимость назначения типа элементу XML.  
Значение по умолчанию: [Неявное](НазначениеТипаXML.Неявное.md).

Описание варианта метода:

Выполняет запись значения с указанием полного имени элемента.

Вариант синтаксиса: Записать с локальным именем и пространством имен

Синтаксис:

ЗаписатьXML(<ЗаписьXML>, <Значение>, <ЛокальноеИмя>, <URIПространстваИмен>, <НазначениеТипа>)

Параметры:

<ЗаписьXML> (обязательный)

Тип: [ЗаписьXML](ЗаписьXML.md).   
Объект, через который осуществляется запись XML.

<Значение> (обязательный)

Записываемое в поток XML значение. Тип параметра определяется совокупностью типов, для которых определена XML-сериализация.  
При записи объекта метаданных выгружаются все записи объекта метаданных, доступные для пользователя.

<ЛокальноеИмя> (обязательный)

Тип: Строка.   
Локальное имя элемента XML, в который будет записано значение.

<URIПространстваИмен> (обязательный)

Тип: Строка.   
URI пространства имен, к которому принадлежит указанное ЛокальноеИмя.

<НазначениеТипа> (необязательный)

Тип: [НазначениеТипаXML](НазначениеТипаXML.md).   
Определяет необходимость назначения типа элементу XML.  
Значение по умолчанию: [Неявное](НазначениеТипаXML.Неявное.md).

Описание варианта метода:

Выполняет запись значения с указанием локального имени элемента и пространства имен, к которому принадлежит локальное имя.

Описание:

Выполняет сериализацию значения в формат XML.

Доступность:

Тонкий клиент, мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер.

Примечание:

Значения не всех типов могут быть записаны в формате XML данным методом.  
В XML могут быть записаны значения следующих типов: Неопределено, Null, Булево, Число, Строка, Дата, Тип, [УникальныйИдентификатор](УникальныйИдентификатор.md), [ДвоичныеДанные](ДвоичныеДанные.md), [ХранилищеЗначения](ХранилищеЗначения.md), [ОписаниеТипов](ОписаниеТипов.md), а также ссылки объектов данных и сами объекты данных, наборы записей регистров, менеджер значения константы.   
Также поддерживаются следующие объекты метаданных:

- [ОбъектМетаданных: Справочник](ОбъектМетаданных_Справочник.md),
- [ОбъектМетаданных: Константа](ОбъектМетаданных_Константа.md),
- [ОбъектМетаданных: Документ](ОбъектМетаданных_Документ.md),
- [ОбъектМетаданных: ПланВидовХарактеристик](ОбъектМетаданных_ПланВидовХарактеристик.md),
- [ОбъектМетаданных: РегистрСведений](ОбъектМетаданных_РегистрСведений.md) (в том числе, независимый),
- [ОбъектМетаданных: РегистрНакопления](ОбъектМетаданных_РегистрНакопления.md),
- [ОбъектМетаданных: Последовательность](ОбъектМетаданных_Последовательность.md),
- [ОбъектМетаданных: ПланСчетов](ОбъектМетаданных_ПланСчетов.md),
- [ОбъектМетаданных: РегистрБухгалтерии](ОбъектМетаданных_РегистрБухгалтерии.md),
- [ОбъектМетаданных: ПланВидовРасчета](ОбъектМетаданных_ПланВидовРасчета.md),
- [ОбъектМетаданных: РегистрРасчета](ОбъектМетаданных_РегистрРасчета.md),
- [ОбъектМетаданных: БизнесПроцесс](ОбъектМетаданных_БизнесПроцесс.md),
- [ОбъектМетаданных: Задача](ОбъектМетаданных_Задача.md).

В дальнейшем список может пополняться.  
При попытке записи значения неподходящего типа будет вызвано исключение.

Использование в версии:

Доступен, начиная с версии 8.0.

Описание изменено в версии 8.3.21.

---
