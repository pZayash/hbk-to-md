# СписокЗначений.НайтиПоЗначению

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Универсальные коллекции значений](Универсальные_коллекции_значений.md) › [Список значений](Список_значений.md) › [СписокЗначений](СписокЗначений_(ValueList).md) › [Методы](СписокЗначений__Методы.md)

СписокЗначений (ValueList)

НайтиПоЗначению (FindByValue)

Доступен, начиная с версии 8.0.

Синтаксис:

НайтиПоЗначению(<ИскомоеЗначение>)

Параметры:

<ИскомоеЗначение> (обязательный)

Тип: Произвольный.   
Выражение со значением, которое требуется найти.

Возвращаемое значение:

Тип: [ЭлементСпискаЗначений](ЭлементСпискаЗначений_(ValueListItem).md), [Неопределено](lang__def_Undefined.md).   
Если ни один элемент не хранит значения, равного искомому, то возвращается значение [Неопределено](lang__def_Undefined.md).

Описание:

Осуществляет поиск значения элемента списка значений.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер.

Пример:

|  |
| --- |
| Если ПолеСписка1.НайтиПоЗначению(ВыбЗначение) = Неопределено Тогда     Сообщить("Значение не найдено!"); КонецЕсли; |

Использование в версии:

Доступен, начиная с версии 8.0.

---
