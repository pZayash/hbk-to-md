# ОбработчикиWebSocketКлиентСоединения.По умолчанию

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=WebSocket.md">WebSocket</a> › [ОбработчикиWebSocketКлиентСоединения](ОбработчикиWebSocketКлиентСоединения.md)

<!-- signature:start -->
`ОбработчикиWebSocketКлиентСоединения`(`<ОбработчикОткрытияСоединения>`: `Строка`, `<ОбработчикПолученияСообщения>`: `Строка`, `<ОбработчикОшибки>`: `Строка`, `<ОбработчикЗакрытияСоединения>`: `Строка`, `<Модуль>`: [`ФормаКлиентскогоПриложения`](ФормаКлиентскогоПриложения.md) | [`КомандаКомандногоИнтерфейса`](КомандаКомандногоИнтерфейса.md) | [`ОбщийМодуль`](ОбщийМодуль.md) | `Строка`)
<!-- signature:end -->

ОбработчикиWebSocketКлиентСоединения (WebSocketClientConnectionHandlers)

По умолчанию

Доступен, начиная с версии 8.3.27.

Синтаксис:

Новый ОбработчикиWebSocketКлиентСоединения(<ОбработчикОткрытияСоединения>, <ОбработчикПолученияСообщения>, <ОбработчикОшибки>, <ОбработчикЗакрытияСоединения>, <Модуль>)

Параметры:

<ОбработчикОткрытияСоединения> (необязательный)

Тип: Строка.   
Имя экспортируемой процедуры обработчика события открытия соединения.

<ОбработчикПолученияСообщения> (необязательный)

Тип: Строка.   
Имя экспортируемой процедуры-обработчика события получения сообщения.

<ОбработчикОшибки> (необязательный)

Тип: Строка.   
Имя экспортируемой процедуры обработчика события возникновения ошибки.

<ОбработчикЗакрытияСоединения> (необязательный)

Тип: Строка.   
Имя экспортируемой процедуры обработчика события закрытия соединения.

<Модуль> (необязательный)

Тип: [ФормаКлиентскогоПриложения](ФормаКлиентскогоПриложения.md), [КомандаКомандногоИнтерфейса](КомандаКомандногоИнтерфейса.md), [ОбщийМодуль](ОбщийМодуль.md), Строка.   
Модуль процедур-обработчиков событий.

Описание:

Создает объект [ОбработчикиWebSocketКлиентСоединения](ОбработчикиWebSocketКлиентСоединения.md) и связывает его с вызовом указанных процедур модуля.

Использование в версии:

Доступен, начиная с версии 8.3.27.

---
