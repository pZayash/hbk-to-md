# ЗапускПриложенияМобильногоУстройства.Запустить

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=Запуск_приложения_мобильного_устройства.md">Запуск приложения мобильного устройства</a> › [ЗапускПриложенияМобильногоУстройства](ЗапускПриложенияМобильногоУстройства.md)

<!-- signature:start -->
`Запустить`(`<Ожидать>`: `Булево`) → `Булево, Число`
<!-- signature:end -->

ЗапускПриложенияМобильногоУстройства (MobileDeviceApplicationRun)

Запустить (Run)

Доступен, начиная с версии 8.3.5.

Синтаксис:

Запустить(<Ожидать>)

Параметры:

<Ожидать> (обязательный)

Тип: Булево.   
Определяет необходимость ожидания результата запуска приложения.

Возвращаемое значение:

Тип: Булево, Число.   
Зависит от параметра <Ожидать>.  
Если параметр имеет значение Ложь, то в зависимости от успешности запуска возвращается значение Истина или Ложь.  
Если параметр имеет значение Истина, то возвращается числовой код результата работы приложения.

Описание:

Запускает приложение.  
Данный метод поддерживается не на всех устройствах. Для проверки совместимости устройства перед вызовом данного метода следует использовать метод [ПоддерживаетсяЗапуск](ЗапускПриложенияМобильногоУстройства.ПоддерживаетсяЗапуск.md).

Доступность:

Мобильный клиент, мобильное приложение (клиент).

См. также:

[ЗапускПриложенияМобильногоУстройства](ЗапускПриложенияМобильногоУстройства.md), метод [ПоддерживаетсяЗапуск](ЗапускПриложенияМобильногоУстройства.ПоддерживаетсяЗапуск.md)  

Использование в версии:

Доступен, начиная с версии 8.3.5.

Описание изменено в версии 8.3.23.

---
