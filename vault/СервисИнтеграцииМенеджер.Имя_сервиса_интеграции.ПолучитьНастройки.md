# СервисИнтеграцииМенеджер.<Имя сервиса интеграции>.ПолучитьНастройки

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog2700.md">Сервисы интеграции</a> › [СервисИнтеграцииМенеджер.<Имя сервиса интеграции>](СервисИнтеграцииМенеджер.Имя_сервиса_интеграции.md)

<!-- signature:start -->
`ПолучитьНастройки`() → [`НастройкиСервисаИнтеграции`](НастройкиСервисаИнтеграции.md)
<!-- signature:end -->

СервисИнтеграцииМенеджер.<Имя сервиса интеграции> (IntegrationServiceManager.<Integration service name>)

ПолучитьНастройки (GetSettings)

Доступен, начиная с версии 8.3.17.

Синтаксис:

ПолучитьНастройки()

Возвращаемое значение:

Тип: [НастройкиСервисаИнтеграции](НастройкиСервисаИнтеграции.md).   
Если настройки в режиме "1С:Предприятие" не выполнялись, то возвращает новый объект [НастройкиСервисаИнтеграции](НастройкиСервисаИнтеграции.md).

Описание:

При получении настроек выполняются следующие действия:

- Если в сервисе интеграции определены разделители:
  - Генерируется исключение, если выполняется одно из перечисленных условий:
    - В сеансе используется разделитель, который не разделяет сервис интеграции.
    - В сеансе не используется разделитель, который разделяет сервис интеграции.
  - Возвращаются настройки сервиса интеграции для области данных.
- Если сервисе интеграции не определены разделители:
  - Генерируется исключение, если в сеансе используется любой разделитель.
  - Возвращаются настройки сервиса интеграции для информационной базы.

Доступность:

Сервер, толстый клиент, внешнее соединение.

Использование в версии:

Доступен, начиная с версии 8.3.17.

Описание изменено в версии 8.3.18.

---
