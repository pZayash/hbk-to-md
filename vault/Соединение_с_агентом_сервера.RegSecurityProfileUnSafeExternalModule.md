# Соединение с агентом сервера.RegSecurityProfileUnSafeExternalModule

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Средства_интеграции_и_администрирования.md">Средства интеграции и администрирования</a> › <a href="obsidian://open?file=Менеджер_COM-соединений.md">Менеджер COM-соединений</a> › <a href="obsidian://open?file=Администрирование_кластера_серверов.md">Администрирование кластера серверов</a> › [Соединение с агентом сервера](Соединение_с_агентом_сервера.md)

<!-- signature:start -->
`RegSecurityProfileUnSafeExternalModule`(`<Кластер>`: [`Кластер серверов`](Кластер_серверов.md), `<ИмяПрофиля>`: `Строка`, `<ВнешнийМодуль>`: [`Внешний модуль`](Внешний_модуль.md))
<!-- signature:end -->

Соединение с агентом сервера (IServerAgentConnection)

RegSecurityProfileUnSafeExternalModule (RegSecurityProfileUnSafeExternalModule)

Доступен, начиная с версии 8.3.3.

Синтаксис:

RegSecurityProfileUnSafeExternalModule(<Кластер>, <ИмяПрофиля>, <ВнешнийМодуль>)

Параметры:

<Кластер> (обязательный)

Тип: [Кластер серверов](Кластер_серверов.md).   
Кластер, в котором должен быть зарегистрирован внешний модуль.

<ИмяПрофиля> (обязательный)

Тип: Строка.   
Имя профиля безопасности.

<ВнешнийМодуль> (обязательный)

Тип: [Внешний модуль](Внешний_модуль.md).   
Регистрируемый внешний модуль.

Описание:

Выполняет регистрацию нового внешнего модуля или обновление существующего. Внешний модуль идентифицируется своей контрольной суммой.

Доступность:

Интеграция.

Примечание:

Если внешний модуль существует, то выполняется обновление, в противном случае - добавление.  
Если профиль не найден, генерируется исключение.

Использование в версии:

Доступен, начиная с версии 8.3.3.

---
