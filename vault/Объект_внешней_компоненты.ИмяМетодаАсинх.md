# Объект внешней компоненты.<ИмяМетода>Асинх

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=Внешние_компоненты.md">Внешние компоненты</a> › [Объект внешней компоненты](Объект_внешней_компоненты.md)

<!-- signature:start -->
`<ИмяМетода>Асинх`(`<Параметры>`: `Произвольный`) → [`РезультатАсинхВызоваВнешнейКомпоненты`](РезультатАсинхВызоваВнешнейКомпоненты.md)
<!-- signature:end -->

Объект внешней компоненты (Add-in object)

<ИмяМетода>Асинх (<MethodName>Async)

Доступен, начиная с версии 8.3.18.

Синтаксис:

<ИмяМетода>Асинх(<Параметры>)

Параметры:

<Параметры> (необязательный)

Тип: Произвольный.   
Список входных параметров внешней компоненты в том же порядке, в каком они указаны в компоненте.

Возвращаемое значение:

Тип: [РезультатАсинхВызоваВнешнейКомпоненты](РезультатАсинхВызоваВнешнейКомпоненты.md).   

Описание:

Асинхронно вызывает метод объекта внешней компоненты.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, толстый клиент, мобильное приложение (клиент).

См. также:

[Объект внешней компоненты](Объект_внешней_компоненты.md), метод [НачатьВызов\<ИмяМетода\>](Объект_внешней_компоненты.НачатьВызовИмяМетода.md)  

Использование в версии:

Доступен, начиная с версии 8.3.18.

---
