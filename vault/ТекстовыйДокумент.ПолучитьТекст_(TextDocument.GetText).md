# ТекстовыйДокумент.ПолучитьТекст

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Общие объекты](Общие_объекты.md) › [Текстовый документ](Текстовый_документ.md) › [ТекстовыйДокумент](ТекстовыйДокумент_(TextDocument).md) › [Методы](ТекстовыйДокумент__Методы.md)

ТекстовыйДокумент (TextDocument)

ПолучитьТекст (GetText)

Доступен, начиная с версии 8.0.

Синтаксис:

ПолучитьТекст()

Возвращаемое значение:

Тип: [Строка](lang__def_String.md).   
Строка, содержащая текст из текстового документа.

Описание:

Получает текст, находящийся в текстовом документе.

Доступность:

Тонкий клиент, веб-клиент, сервер, толстый клиент, внешнее соединение.

Пример:

|  |
| --- |
| Процедура КоманднаяПанельКонструкторЗапросов(Кнопка)     КонструкторЗапроса = Новый КонструкторЗапроса;     КонструкторЗапроса.АвтоДобавлениеПредставлений = Истина;     Попытка         Текст = ТекстЗапроса.ПолучитьТекст();         Если Текст <> "" Тогда             КонструкторЗапроса.Текст = Текст;         КонецЕсли;          Если КонструкторЗапроса.ОткрытьМодально() Тогда              ТекстЗапроса.УстановитьТекст(КонструкторЗапроса.Текст);         КонецЕсли;     Исключение         Предупреждение(ОписаниеОшибки());     КонецПопытки; КонецПроцедуры |

Использование в версии:

Доступен, начиная с версии 8.0.

---
