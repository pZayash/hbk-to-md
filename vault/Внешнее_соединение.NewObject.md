# Внешнее соединение.NewObject

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Средства_интеграции_и_администрирования.md">Средства интеграции и администрирования</a> › <a href="obsidian://open?file=Менеджер_COM-соединений.md">Менеджер COM-соединений</a> › [Внешнее соединение](Внешнее_соединение.md)

<!-- signature:start -->
`NewObject`(`<Имя>`: `Строка`, `<ПараметрыКонструктора>`: `Произвольный`) → [`COMОбъект`](COMОбъект.md)
<!-- signature:end -->

Внешнее соединение (External connection)

NewObject (NewObject)

Доступен, начиная с версии 8.1.

Синтаксис:

NewObject(<Имя>, <ПараметрыКонструктора>)

Параметры:

<Имя> (обязательный)

Тип: Строка.   
Имя типа создаваемого объекта аналогичное тому, которое может быть задано в операторе встроенного языка Новый.

<ПараметрыКонструктора> (необязательный)

Тип: Произвольный.   
Список параметров для конструктора объекта создаваемого типа, перечисленных через запятую в том порядке, в котором они определены в конструкторе.

Возвращаемое значение:

Тип: [COMОбъект](COMОбъект.md).   

Описание:

Создает объект, для которого предусмотрен конструктор, и возвращает ссылку на него.

Доступность:

Интеграция.

Примечание:

Последующие параметры метода те же, что у конструктора объекта, имя которого указано в качестве значения первого параметра.

Использование в версии:

Доступен, начиная с версии 8.1.

Описание изменено в версии 8.3.27.

---
