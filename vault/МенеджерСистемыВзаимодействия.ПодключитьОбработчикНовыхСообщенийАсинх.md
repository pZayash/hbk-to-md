# МенеджерСистемыВзаимодействия.ПодключитьОбработчикНовыхСообщенийАсинх

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog2454.md">Система взаимодействия</a> › [МенеджерСистемыВзаимодействия](МенеджерСистемыВзаимодействия.md)

<!-- signature:start -->
`ПодключитьОбработчикНовыхСообщенийАсинх`(`<Обсуждение>`: [`ИдентификаторОбсужденияСистемыВзаимодействия`](ИдентификаторОбсужденияСистемыВзаимодействия.md), `<Обработчик>`: [`ОписаниеОповещения`](ОписаниеОповещения.md), `<СтатусОповещенияПользователя>`: [`СтатусОповещенияПользователя`](СтатусОповещенияПользователя.md))

`ПодключитьОбработчикНовыхСообщенийАсинх`(`<Ключ>`: `Строка`, `<Обработчик>`: [`ОписаниеОповещения`](ОписаниеОповещения.md), `<СтатусОповещенияПользователя>`: [`СтатусОповещенияПользователя`](СтатусОповещенияПользователя.md)) → [`Обещание`](Обещание.md)
<!-- signature:end -->

МенеджерСистемыВзаимодействия (CollaborationSystemManager)

ПодключитьОбработчикНовыхСообщенийАсинх (AttachNewMessagesHandlerAsync)

Доступен, начиная с версии 8.3.18.

Вариант синтаксиса: По идентификатору

Синтаксис:

ПодключитьОбработчикНовыхСообщенийАсинх(<Обсуждение>, <Обработчик>, <СтатусОповещенияПользователя>)

Параметры:

<Обсуждение> (обязательный)

Тип: [ИдентификаторОбсужденияСистемыВзаимодействия](ИдентификаторОбсужденияСистемыВзаимодействия.md).   
Идентификатор обсуждения, к которому подключается обработчик.

<Обработчик> (необязательный)

Тип: [ОписаниеОповещения](ОписаниеОповещения.md).   
Содержит описание процедуры, которая будет вызвана при появлении новых сообщений в обсуждении со следующими параметрами:

- <Сообщение> - тип [СообщениеСистемыВзаимодействия](СообщениеСистемыВзаимодействия.md). В обработчик передается копия объекта;
- <ДополнительныеПараметры> - тип Произвольный. Значение, которое было указано при создании объекта [ОписаниеОповещения](ОписаниеОповещения.md).

<СтатусОповещенияПользователя> (необязательный)

Тип: [СтатусОповещенияПользователя](СтатусОповещенияПользователя.md).   
Если значение параметра задано, но при появлении нового сообщения будет показано оповещение пользователя с указанным статусом.

Описание варианта метода:

Выполняет подключение обработчика по идентификатору обсуждения.

Вариант синтаксиса: По ключу

Синтаксис:

ПодключитьОбработчикНовыхСообщенийАсинх(<Ключ>, <Обработчик>, <СтатусОповещенияПользователя>)

Параметры:

<Ключ> (обязательный)

Тип: Строка.   
Ключ обсуждения, к которому подключается обработчик.

<Обработчик> (необязательный)

Тип: [ОписаниеОповещения](ОписаниеОповещения.md).   
Содержит описание процедуры, которая будет вызвана при появлении новых сообщений в обсуждении со следующими параметрами:

- <Сообщение> - тип [СообщениеСистемыВзаимодействия](СообщениеСистемыВзаимодействия.md). В обработчик передается копия объекта.
- <ДополнительныеПараметры> - значение, которое было указано при создании объекта [ОписаниеОповещения](ОписаниеОповещения.md).

<СтатусОповещенияПользователя> (необязательный)

Тип: [СтатусОповещенияПользователя](СтатусОповещенияПользователя.md).   
Если значение параметра установлено, при появлении нового сообщения будет показано оповещение пользователя с указанным статусом.

Описание варианта метода:

Выполняет подключение обработчика по ключу обсуждения.

Возвращаемое значение:

Тип: [Обещание](Обещание.md).   
Результат выполнения обещания - Неопределено.

Описание:

Асинхронная функция. Выполняет подключение обработчика, который будет вызван при появлении новых сообщений в обсуждении.  
Если <СтатусОповещенияПользователя> установлен в значение, отличное от Неопределено, то при получении нового сообщения отображается оповещение с соответствующим статусом. При нажатии на оповещение будет вызвана процедура, заданная в параметре <Обработчик>.  
Если <СтатусОповещенияПользователя> установлен в значение Неопределено, то при получении нового сообщения будет вызвана процедура, заданная в параметре <Обработчик>.  
После подключения обработчик будет вызван для каждого непрочитанного сообщения в обсуждении. Перед вызовом обработчика (или отображения оповещения пользователя) для неотображаемых обсуждений сообщение помечается как прочитанное.  
Допускается наличие нескольких обработчиков для одного обсуждения. В этом случае будут вызваны все обработчики. Порядок вызова обработчиков не определен.  
Вызов метода для одного обсуждения с тем же обработчиком замещает обработчик.  
Сравнение обработчиков происходит по равенству значений свойств:

- ИмяПроцедуры,
- ИмяПроцедурыОбработкиОшибки,
- Модуль,
- МодульОбработкиОшибки.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, толстый клиент.

См. также:

[МенеджерСистемыВзаимодействия](МенеджерСистемыВзаимодействия.md), метод [НачатьПодключениеОбработчикаНовыхСообщений](МенеджерСистемыВзаимодействия.НачатьПодключениеОбработчикаНовыхСообщений.md)  

Использование в версии:

Доступен, начиная с версии 8.3.18.

---
