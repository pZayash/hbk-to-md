# Соединение с агентом сервера.SetClusterRecyclingExpirationTimeout

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Средства интеграции и администрирования](Средства_интеграции_и_администрирования.md) › [Менеджер COM-соединений](Менеджер_COM-соединений.md) › [Администрирование кластера серверов](Администрирование_кластера_серверов.md) › [Соединение с агентом сервера](Соединение_с_агентом_сервера_(IServerAgentConnection).md) › [Методы](Соединение_с_агентом_сервера__Методы.md)

Соединение с агентом сервера (IServerAgentConnection)

SetClusterRecyclingExpirationTimeout (SetClusterRecyclingExpirationTimeout)

Доступен, начиная с версии 8.2.

Синтаксис:

SetClusterRecyclingExpirationTimeout(<Кластер>, <ТаймаутЗавершения>)

Параметры:

<Кластер> (обязательный)

Тип: [Кластер серверов](Кластер_серверов_(IClusterInfo).md).   
Кластер, для которого требуется изменить настройку автоматического перезапуска рабочих процессов. Должен быть получен методом [GetClusters](Соединение_с_агентом_сервера.GetClusters_(IServerAgentConnection.GetClusters).md) объекта [Соединение с агентом сервера](Соединение_с_агентом_сервера_(IServerAgentConnection).md).

<ТаймаутЗавершения> (обязательный)

Тип: [Число](lang__def_Number.md).   
Время принудительного завершения рабочих процессов.   
0 - принудительное завершение рабочих процессов не выполняется.

Описание:

Устанавливает время принудительного завершения. Если запущенный рабочий процесс выключен, то он будет завершен после того, как через него не будет установлено ни одного соединения с информационными базами.   
Однако после того, как со времени выключения процесса пройдет <ТаймаутЗавершения> секунд, рабочий процесс будет завершен принудительно, даже если соединения остались.

Доступность:

Интеграция.

Использование в версии:

Доступен, начиная с версии 8.2.

---
