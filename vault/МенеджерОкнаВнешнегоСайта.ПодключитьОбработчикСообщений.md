# МенеджерОкнаВнешнегоСайта.ПодключитьОбработчикСообщений

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=Внешний_сайт.md">Внешний сайт</a> › [МенеджерОкнаВнешнегоСайта](МенеджерОкнаВнешнегоСайта.md)

<!-- signature:start -->
`ПодключитьОбработчикСообщений`(`<Обработчик>`: [`ОписаниеОповещения`](ОписаниеОповещения.md))
<!-- signature:end -->

МенеджерОкнаВнешнегоСайта (ExternalSiteWindowManager)

ПодключитьОбработчикСообщений (AttachMessageHandler)

Доступен, начиная с версии 8.3.16.

Синтаксис:

ПодключитьОбработчикСообщений(<Обработчик>)

Параметры:

<Обработчик> (обязательный)

Тип: [ОписаниеОповещения](ОписаниеОповещения.md).   
Содержит описание процедуры, которая будет вызвана при получении сообщения из внешнего сайта со следующими параметрами:

- <Сообщение> - тип [СообщениеВнешнегоСайта](СообщениеВнешнегоСайта.md),
- <ДополнительныеПараметры> - значение, которое было указано при создании объекта [ОписаниеОповещения](ОписаниеОповещения.md).

Описание:

Подключает процедуру-обработчик, вызываемую при получении сообщения из внешнего сайта.  
Допускается наличие нескольких обработчиков. В этом случае при получении сообщения из внешнего сайта будут вызваны все обработчики. Порядок вызова обработчиков не определен.  
Вызов метода с тем же обработчиком замещает обработчик.  
Сравнение обработчиков выполняется по равенству значений свойств:

- ИмяПроцедуры,
- ИмяПроцедурыОбработкиОшибки,
- Модуль,
- МодульОбработкиОшибки.

Если окно внешнего сайта не доступно (свойство [Доступно](МенеджерОкнаВнешнегоСайта.Доступно.md) имеет значение Ложь), тогда при вызове данного метода будет сгенерировано исключение "Окно внешнего сайта не доступно".

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, толстый клиент.

См. также:

[СообщениеВнешнегоСайта](СообщениеВнешнегоСайта.md)  

Использование в версии:

Доступен, начиная с версии 8.3.16.

---
