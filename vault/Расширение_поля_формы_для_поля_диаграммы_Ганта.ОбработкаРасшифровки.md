# Расширение поля формы для поля диаграммы Ганта.ОбработкаРасшифровки

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Интерфейс.md">Интерфейс</a> › <a href="obsidian://open?file=Поле_формы.md">Поле формы</a> › [Расширение поля формы для поля диаграммы Ганта](Расширение_поля_формы_для_поля_диаграммы_Ганта.md)

<!-- signature:start -->
`ОбработкаРасшифровки`(`<Расшифровка>`, `<СтандартнаяОбработка>`, `<Дата>`)
<!-- signature:end -->

Расширение поля формы для поля диаграммы Ганта (Form field extension for a Gantt chart field)

ОбработкаРасшифровки (DetailProcessing)

Доступен, начиная с версии 8.2.

Синтаксис:

ОбработкаРасшифровки(<Расшифровка>, <СтандартнаяОбработка>, <Дата>)

Параметры:

<Расшифровка>

Тип: Произвольный, [Массив](Массив.md).   
Значение расшифровки точки, серии, значения диаграммы Ганта или метки элемента шкалы времени. Если параметр - [Массив](Массив.md), то первый элемент - расшифровка значения диаграммы Ганта, второй и последующие - расшифровки интервалов диаграммы Ганта (в случае перекрывающихся интервалов).

<СтандартнаяОбработка>

Тип: Булево.   
В данный параметр передается признак выполнения стандартной (системной) обработки события. Если в теле процедуры-обработчика установить данному параметру значение Ложь, стандартная обработка события производиться не будет. Отказ от стандартной обработки не отменяет действие.  
Значение по умолчанию: Истина.

<Дата>

Тип: Дата.   
Выбранная дата.

Описание:

Вызывается при выборе значения.

Доступность:

Тонкий клиент, веб-клиент, сервер, толстый клиент.

Использование в версии:

Доступен, начиная с версии 8.2.

---
