# КонфигурацияЗаписиDOM.УстановитьПараметр

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Общие объекты](Общие_объекты.md) › [DOM](DOM-2.md) › [КонфигурацияЗаписиDOM](КонфигурацияЗаписиDOM_(DOMWriterConfiguration).md) › [Методы](КонфигурацияЗаписиDOM__Методы.md)

КонфигурацияЗаписиDOM (DOMWriterConfiguration)

УстановитьПараметр (SetParameter)

Доступен, начиная с версии 8.1.

Синтаксис:

УстановитьПараметр(<Имя>, <Значение>)

Параметры:

<Имя> (обязательный)

Тип: [Строка](lang__def_String.md).   
Имя параметра.

<Значение> (обязательный)

Тип: Произвольный.   
Значение параметра.

Описание:

Устанавливает значение параметра.

Доступность:

Сервер, толстый клиент, внешнее соединение, мобильное приложение (сервер), мобильный автономный сервер.

Примечание:

Причины вызова исключений:

- указанный квалифицированный параметр не определен в данной конфигурации;
- параметр с указанным именем определен в данной конфигурации, но не может быть изменен;
- тип устанавливаемого значения параметра отличается от ожидаемого.

Использование в версии:

Доступен, начиная с версии 8.1.

---
