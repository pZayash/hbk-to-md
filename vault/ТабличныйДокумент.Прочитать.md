# ТабличныйДокумент.Прочитать

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog64.md">Табличный документ</a> › [ТабличныйДокумент](ТабличныйДокумент.md)

<!-- signature:start -->
`Прочитать`(`<ИмяФайла>`: `Строка`, `<СпособЧтенияЗначений>`: [`СпособЧтенияЗначенийТабличногоДокумента`](СпособЧтенияЗначенийТабличногоДокумента.md))

`Прочитать`(`<Поток>`: [`Поток`](Поток.md) | [`ПотокВПамяти`](ПотокВПамяти.md) | [`ФайловыйПоток`](ФайловыйПоток.md), `<СпособЧтенияЗначений>`: [`СпособЧтенияЗначенийТабличногоДокумента`](СпособЧтенияЗначенийТабличногоДокумента.md), `<ТипФайлаТаблицы>`: [`ТипФайлаТабличногоДокумента`](ТипФайлаТабличногоДокумента.md))
<!-- signature:end -->

ТабличныйДокумент (SpreadsheetDocument)

Прочитать (Read)

Доступен, начиная с версии 8.0.

Вариант синтаксиса: Из файла

Синтаксис:

Прочитать(<ИмяФайла>, <СпособЧтенияЗначений>)

Параметры:

<ИмяФайла> (обязательный)

Тип: Строка.   
Имя файла табличного документа.

<СпособЧтенияЗначений> (необязательный)

Тип: [СпособЧтенияЗначенийТабличногоДокумента](СпособЧтенияЗначенийТабличногоДокумента.md).   
Определяет, каким образом нужно интерпретировать значения, считываемые из исходного документа XLS, XLSX или ODS.  
При загрузке табличного документа из формата Excel 97 - 2010 и OpenOffice Calc, в случае если в ячейке исходного документа содержалось значение типа Дата или Число, то в ячейку результирующего табличного документа это значение попадает в зависимости от значения этого параметра.  
Значение по умолчанию: [Текст](СпособЧтенияЗначенийТабличногоДокумента.Текст.md).

Описание варианта метода:

Чтение табличного документа из файла.  
При работе на мобильной платформе возможно чтение только в формате MXL.

Вариант синтаксиса: Из потока

Синтаксис:

Прочитать(<Поток>, <СпособЧтенияЗначений>, <ТипФайлаТаблицы>)

Параметры:

<Поток> (обязательный)

Тип: [Поток](Поток.md), [ПотокВПамяти](ПотокВПамяти.md), [ФайловыйПоток](ФайловыйПоток.md).   
Поток для чтения табличного документа.

<СпособЧтенияЗначений> (необязательный)

Тип: [СпособЧтенияЗначенийТабличногоДокумента](СпособЧтенияЗначенийТабличногоДокумента.md).   
Определяет, каким образом нужно интерпретировать значения, считываемые из исходного документа.  
Значение по умолчанию: [Текст](СпособЧтенияЗначенийТабличногоДокумента.Текст.md).

<ТипФайлаТаблицы> (необязательный)

Тип: [ТипФайлаТабличногоДокумента](ТипФайлаТабличногоДокумента.md).   
Формат, в котором табличный документ хранится в потоке.  
При чтении табличного документа из потока допустимы следующие форматы: MXL, ODS.  
Значение по умолчанию: [MXL](ТипФайлаТабличногоДокумента.MXL.md).

Описание варианта метода:

Чтение табличного документа из потока.  
При чтении из потока допускается использование только следующих форматов: MXL, MXL7, ODS.  
При работе на мобильной платформе возможно чтение только в формате MXL.

Описание:

Считывает табличный документ из файла.   
Позволяет считывать табличный документ из файла табличного документа Microsoft Excel 97 - 2010 ( \*.xls и \*.xlsx) или электронной таблицы OpenOffice Calc ( \*.ods).

Доступность:

Сервер, толстый клиент, внешнее соединение, мобильное приложение (сервер), мобильный автономный сервер.

Пример:

|  |
| --- |
| ТабДок = Новый ТабличныйДокумент; ТабДок.Прочитать("C:\My Documents\Таблица1.mxl"); |

Использование в версии:

Доступен, начиная с версии 8.0.

---
