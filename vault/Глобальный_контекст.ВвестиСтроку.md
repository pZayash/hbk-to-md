# Глобальный контекст.ВвестиСтроку

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Глобальный_контекст.md">Глобальный контекст</a> › [Функции для вызова диалога ввода данных](Функции_для_вызова_диалога_ввода_данных.md)

<!-- signature:start -->
`ВвестиСтроку`(`<Строка>`: `Строка`, `<Подсказка>`: `Строка`, `<Длина>`: `Число`, `<Многострочность>`: `Булево`) → `Булево`
<!-- signature:end -->

Глобальный контекст (Global context)

ВвестиСтроку (InputString)

Доступен, начиная с версии 8.0.

Синтаксис:

ВвестиСтроку(<Строка>, <Подсказка>, <Длина>, <Многострочность>)

Параметры:

<Строка> (обязательный)

Тип: Строка.   
Доступная в модуле переменная. В эту переменную будет помещена введенная в диалоге строка. Начальное значение переменной будет использовано в качестве начального значения в диалоге.

<Подсказка> (необязательный)

Тип: Строка.   
Текст заголовка окна диалога ввода строки. Может использоваться в качестве подсказки пользователю.  
Значение по умолчанию: Пустая строка.

<Длина> (необязательный)

Тип: Число.   
Длина вводимой строки. Если параметр не указан, то строка неограниченной длины.  
Значение по умолчанию: 0.

<Многострочность> (необязательный)

Тип: Булево.   
Определяет режим ввода многострочного текста: Истина - ввод многострочного текста с разделителями строк; Ложь - ввод простой строки.  
Значение по умолчанию: Ложь.

Возвращаемое значение:

Тип: Булево.   
Истина - строка введена; Ложь - пользователь отказался от ввода строки.

Описание:

Вызывает диалог для ввода строки.  
При вызове метода в веб-клиенте генерируется исключение.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, толстый клиент, мобильное приложение (клиент).

Примечание:

Если для конфигурации свойство [РежимИспользованияМодальности](ОбъектМетаданныхКонфигурация.РежимИспользованияМодальности.md) установлено в [НеИспользовать](РежимИспользованияМодальности.НеИспользовать.md), следует использовать метод [ПоказатьВводСтроки](Глобальный_контекст.ПоказатьВводСтроки.md).

Пример:

|  |
| --- |
| Текст = ""; Подсказка = "Введите текст напоминания"; Если ВвестиСтроку(Текст, Подсказка, 0, Истина) Тогда     // запомнить текст напоминания  КонецЕсли; |

См. также:

[Глобальный контекст](Глобальный_контекст.md), метод [ВвестиСтрокуАсинх](Глобальный_контекст.ВвестиСтрокуАсинх.md)  

Использование в версии:

Доступен, начиная с версии 8.0.

Описание изменено в версии 8.3.18.

---
