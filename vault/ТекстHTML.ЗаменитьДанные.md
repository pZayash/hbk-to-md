# ТекстHTML.ЗаменитьДанные

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=HTML.md">HTML</a> › [ТекстHTML](ТекстHTML.md)

<!-- signature:start -->
`ЗаменитьДанные`(`<Смещение>`: `Число`, `<Длина>`: `Число`, `<Строка>`: `Строка`)
<!-- signature:end -->

ТекстHTML (HTMLText)

ЗаменитьДанные (ReplaceData)

Доступен, начиная с версии 8.2.

Синтаксис:

ЗаменитьДанные(<Смещение>, <Длина>, <Строка>)

Параметры:

<Смещение> (обязательный)

Тип: Число.   
Смещение от начала символьных данных.

<Длина> (обязательный)

Тип: Число.   
Длина замещаемых символьных данных.

<Строка> (обязательный)

Тип: Строка.   
Строка, замещающая символьные данные.

Описание:

Заменяет диапазон символьных данных.

Доступность:

Мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер.

Примечание:

Причины вызова исключения:

- смещение превышает размер символьных данных;
- данный узел в режиме Только для чтения.

Использование в версии:

Доступен, начиная с версии 8.2.

---
