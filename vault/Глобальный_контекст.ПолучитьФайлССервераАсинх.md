# Глобальный контекст.ПолучитьФайлССервераАсинх

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Глобальный_контекст.md">Глобальный контекст</a> › [Процедуры и функции работы с файлами](Процедуры_и_функции_работы_с_файлами.md)

<!-- signature:start -->
`ПолучитьФайлССервераАсинх`(`<Адрес>`: `Строка`, `<ИмяФайла>`: `Строка`, `<ПараметрыДиалогаПолученияФайлов>`: [`ПараметрыДиалогаПолученияФайлов`](ПараметрыДиалогаПолученияФайлов.md))

`ПолучитьФайлССервераАсинх`(`<Адрес>`: `Строка`, `<ПутьКФайлу>`: `Строка`) → [`Обещание`](Обещание.md)
<!-- signature:end -->

Глобальный контекст (Global context)

ПолучитьФайлССервераАсинх (GetFileFromServerAsync)

Доступен, начиная с версии 8.3.18.

Вариант синтаксиса: С диалогом

Синтаксис:

ПолучитьФайлССервераАсинх(<Адрес>, <ИмяФайла>, <ПараметрыДиалогаПолученияФайлов>)

Параметры:

<Адрес> (обязательный)

Тип: Строка.   
Расположение данных во временном хранилище или в информационной базе.  
Параметр не должен быть пустой строкой. В противном случае будет сгенерировано сообщение об ошибке.

<ИмяФайла> (необязательный)

Тип: Строка.   
Имя файла, которое будет предложено пользователю для сохранения файла. Пользователь может изменить имя файла.  
Значение по умолчанию: Пустая строка.

<ПараметрыДиалогаПолученияФайлов> (необязательный)

Тип: [ПараметрыДиалогаПолученияФайлов](ПараметрыДиалогаПолученияФайлов.md).   
Структура, содержащая параметры диалога, который будет показан пользователю.

Описание варианта метода:

Получает файл и сохраняет его в локальную файловую систему пользователя, предлагая пользователю выбрать месторасположение получаемого файла.  
  
Результат выполнения обещания заполняется сразу (в веб-клиенте - до показа диалога, в остальных клиентах - после показа диалога) значением Неопределено.  
  
В веб-клиенте расширение работы с файлами не требуется, но используется, если оно уже установлено. В веб-клиенте, если расширение работы с файлами не установлено, настройки из параметра <ПараметрыДиалогаПолученияФайлов> игнорируются.  
В тонком и толстом клиентах отображается диалог, позволяющий указать действие над полученным файлом – открыть его или сохранить в выбранном пользователем каталоге. Если параметр <ИмяФайла> не указан, то операция открытия файла недоступна. При открытии полученного файла выполняется проверка наличия приложения, ассоциированного с типом файла. Проверка выполняется по расширению файла. Если подходящее приложение не найдено, то отображается системный диалог выбора приложения для открытия файла. После выбора приложения файл открывается в нем. Отказ от выбора приложения приводит к отмене открытия файла.  
На мобильной платформе диалоговое окно выбора файла не отображается.

Вариант синтаксиса: Без диалога

Синтаксис:

ПолучитьФайлССервераАсинх(<Адрес>, <ПутьКФайлу>)

Параметры:

<Адрес> (обязательный)

Тип: Строка.   
Расположение данных во временном хранилище или в информационной базе.  
Параметр не должен быть пустой строкой. В противном случае будет сгенерировано сообщение об ошибке.

<ПутьКФайлу> (обязательный)

Тип: Строка.   
Путь к файлу, в который будет сохранен файл.

Описание варианта метода:

Получает файл и сохраняет его в локальную файловую систему пользователя по указанному пути. При этом пользователь не может изменить месторасположение получаемого файла.  
  
Результат выполнения обещания – объект типа [ОписаниеПереданногоФайла](ОписаниеПереданногоФайла.md).  
  
В веб-клиенте требуется расширение работы с файлами.

Возвращаемое значение:

Тип: [Обещание](Обещание.md).   

Описание:

Асинхронная функция. Получает файл.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, толстый клиент, мобильное приложение (клиент).

См. также:

[Глобальный контекст](Глобальный_контекст.md), метод [НачатьПолучениеФайлаССервера](Глобальный_контекст.НачатьПолучениеФайлаССервера.md)  
[Глобальный контекст](Глобальный_контекст.md), метод [ПолучитьФайл](Глобальный_контекст.ПолучитьФайл.md)  

Использование в версии:

Доступен, начиная с версии 8.3.18.

---
