# СтандартноеХранилищеНастроекМенеджер.Сохранить

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Прикладные_объекты.md">Прикладные объекты</a> › <a href="obsidian://open?file=Хранилища_настроек.md">Хранилища настроек</a> › [СтандартноеХранилищеНастроекМенеджер](СтандартноеХранилищеНастроекМенеджер.md)

<!-- signature:start -->
`Сохранить`(`<КлючОбъекта>`: `Строка`, `<КлючНастроек>`: `Строка`, `<Настройки>`: `Произвольный`, `<ОписаниеНастроек>`: [`ОписаниеНастроек`](ОписаниеНастроек.md), `<ИмяПользователя>`: `Строка`)
<!-- signature:end -->

СтандартноеХранилищеНастроекМенеджер (StandardSettingsStorageManager)

Сохранить (Save)

Доступен, начиная с версии 8.2.

Синтаксис:

Сохранить(<КлючОбъекта>, <КлючНастроек>, <Настройки>, <ОписаниеНастроек>, <ИмяПользователя>)

Параметры:

<КлючОбъекта> (обязательный)

Тип: Строка.   
Ключ объекта настройки.  
Подробнее см. в [Настройки, автоматически сохраняемые в системном хранилище](lang__SavingSettings.md).

<КлючНастроек> (необязательный)

Тип: Строка.   
Ключ сохраняемых настроек.  
Значение по умолчанию: Пустая строка.

<Настройки> (обязательный)

Тип: Произвольный.   
Содержит настройки, которые нужно сохранить в хранилище.

<ОписаниеНастроек> (необязательный)

Тип: [ОписаниеНастроек](ОписаниеНастроек.md).   
Описание настроек.   
Для стандартного хранилища настроек из данного параметра используется только свойство [Представление](ОписаниеНастроек.Представление.md).  
Если не указан, то описание не устанавливается.

<ИмяПользователя> (необязательный)

Тип: Строка.   
Имя пользователя, настройки которого сохраняются.  
Если не указан, то сохраняются настройки текущего пользователя.  
В случае переименования пользователя прежние настройки не заменяются. Если будет создан пользователь, имя которого совпадает с именем прежнего пользователя, для него будут использованы существующие настройки.  
Если указан пользователь, не являющийся текущим пользователем, т.е. делается попытка обратиться к настройкам, не принадлежащим текущему пользователю, то требуется наличие у текущего пользователя права АдминистрированиеДанных.

Описание:

Сохраняет настройки в хранилище.  
Вызов метода требует наличия права "СохранениеДанныхПользователя".

Доступность:

Сервер, толстый клиент, внешнее соединение.

Примечание:

Если сохраняется объект настроек внешнего вида формы для пользователя, не являющегося текущим пользователем информационной базы, и такой объект уже существует, то вновь сохраненные настройки будут применяться при загрузке формы, если старый объект настроек не был закэширован или после удаления его из кэша.  
Настройки формы кэшируются при загрузке формы и удаляются из кэша через 20 минут после применения либо при разрыве всех соединений с информационной базой, а также в случае сохранения настроек для текущего пользователя.  
Таким образом, если требуется, чтобы сохраняемые настройки форм вступили в силу немедленно, рекомендуется сохранять их в сеансе того же пользователя, для которого они предназначены.

Использование в версии:

Доступен, начиная с версии 8.2.

---
