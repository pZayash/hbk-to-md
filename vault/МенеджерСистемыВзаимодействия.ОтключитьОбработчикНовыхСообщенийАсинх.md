# МенеджерСистемыВзаимодействия.ОтключитьОбработчикНовыхСообщенийАсинх

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog2454.md">Система взаимодействия</a> › [МенеджерСистемыВзаимодействия](МенеджерСистемыВзаимодействия.md)

<!-- signature:start -->
`ОтключитьОбработчикНовыхСообщенийАсинх`(`<Обсуждение>`: [`ИдентификаторОбсужденияСистемыВзаимодействия`](ИдентификаторОбсужденияСистемыВзаимодействия.md), `<Обработчик>`: [`ОписаниеОповещения`](ОписаниеОповещения.md))

`ОтключитьОбработчикНовыхСообщенийАсинх`(`<Ключ>`: `Строка`, `<Обработчик>`: [`ОписаниеОповещения`](ОписаниеОповещения.md)) → [`Обещание`](Обещание.md)
<!-- signature:end -->

МенеджерСистемыВзаимодействия (CollaborationSystemManager)

ОтключитьОбработчикНовыхСообщенийАсинх (DetachNewMessagesHandlerAsync)

Доступен, начиная с версии 8.3.18.

Вариант синтаксиса: По идентификатору

Синтаксис:

ОтключитьОбработчикНовыхСообщенийАсинх(<Обсуждение>, <Обработчик>)

Параметры:

<Обсуждение> (обязательный)

Тип: [ИдентификаторОбсужденияСистемыВзаимодействия](ИдентификаторОбсужденияСистемыВзаимодействия.md).   
Идентификатор обсуждения, для которого отключается обработчик.

<Обработчик> (необязательный)

Тип: [ОписаниеОповещения](ОписаниеОповещения.md).   
Содержит описание процедуры, заданной при вызове метода [НачатьПодключениеОбработчикаНовыхСообщений](МенеджерСистемыВзаимодействия.НачатьПодключениеОбработчикаНовыхСообщений.md).

Описание варианта метода:

Выполняет отключение обработчика по идентификатору обсуждения.

Вариант синтаксиса: По ключу

Синтаксис:

ОтключитьОбработчикНовыхСообщенийАсинх(<Ключ>, <Обработчик>)

Параметры:

<Ключ> (обязательный)

Тип: Строка.   
Ключ обсуждения, для которого отключается обработчик.

<Обработчик> (необязательный)

Тип: [ОписаниеОповещения](ОписаниеОповещения.md).   
Содержит описание процедуры, заданной при вызове метода [НачатьПодключениеОбработчикаНовыхСообщений](МенеджерСистемыВзаимодействия.НачатьПодключениеОбработчикаНовыхСообщений.md).

Описание варианта метода:

Выполняет отключение обработчика по ключу обсуждения.

Возвращаемое значение:

Тип: [Обещание](Обещание.md).   
Результат выполнения обещания - Неопределено.

Описание:

Асинхронная функция. Отключает ранее подключенный обработчик.  
Сравнение обработчиков происходит по равенству значений свойств:

- ИмяПроцедуры,
- ИмяПроцедурыОбработкиОшибки,
- Модуль,
- МодульОбработкиОшибки.

Не рекомендуется выполнять отключение обработчика в коде самого обработчика.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, толстый клиент.

См. также:

[МенеджерСистемыВзаимодействия](МенеджерСистемыВзаимодействия.md), метод [НачатьОтключениеОбработчикаНовыхСообщений](МенеджерСистемыВзаимодействия.НачатьОтключениеОбработчикаНовыхСообщений.md)  

Использование в версии:

Доступен, начиная с версии 8.3.18.

---
