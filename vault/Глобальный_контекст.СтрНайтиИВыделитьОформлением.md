# Глобальный контекст.СтрНайтиИВыделитьОформлением

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Глобальный_контекст.md">Глобальный контекст</a> › [Функции работы со значениями типа Строка](Функции_работы_со_значениями_типа_Строка.md)

<!-- signature:start -->
`СтрНайтиИВыделитьОформлением`(`<Строка>`: `Строка` | [`ФорматированнаяСтрока`](ФорматированнаяСтрока.md), `<Подстрока>`: `Строка` | [`Массив`](Массив.md)) → [`ФорматированнаяСтрока`](ФорматированнаяСтрока.md) | `Неопределено`
<!-- signature:end -->

Глобальный контекст (Global context)

СтрНайтиИВыделитьОформлением (StrFindAndHighlightByAppearance)

Доступен, начиная с версии 8.3.15.

Синтаксис:

СтрНайтиИВыделитьОформлением(<Строка>, <Подстрока>)

Параметры:

<Строка> (обязательный)

Тип: Строка, [ФорматированнаяСтрока](ФорматированнаяСтрока.md).   
Исходная строка.

<Подстрока> (обязательный)

Тип: Строка, [Массив](Массив.md).   
Искомая строка или массив искомых строк.

Возвращаемое значение:

Тип: [ФорматированнаяСтрока](ФорматированнаяСтрока.md), Неопределено.   
Если хотя бы одна искомая строка не найдена, возвращается Неопределено.

Описание:

Выполняет поиск в строке.  
Поиск выполняется по началу слова.  
Если найдены все искомые строки, то возвращается форматированная строка, в которой искомые строки подсвечены стандартным образом.  
Если искомое слово содержит буквы и цифры:

- последовательность, состоящая только из букв, считается отдельным словом,
- последовательность, состоящая только из цифр, считается отдельным словом.

В слове, состоящем из цифр, лидирующие нули считаются и значащими, и незначащими.  
При наличии иерархии элемент считается найденным:

- если в нем есть все искомые слова,
- или в нем есть хотя бы одно искомое слово, а остальные слова содержатся в его родителях.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер, интеграция.

Использование в версии:

Доступен, начиная с версии 8.3.15.

---
