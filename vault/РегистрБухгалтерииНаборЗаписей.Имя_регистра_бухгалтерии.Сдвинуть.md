# РегистрБухгалтерииНаборЗаписей.<Имя регистра бухгалтерии>.Сдвинуть

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Прикладные_объекты.md">Прикладные объекты</a> › <a href="obsidian://open?file=Регистры_бухгалтерского_учета.md">Регистры бухгалтерского учета</a> › [РегистрБухгалтерииНаборЗаписей.<Имя регистра бухгалтерии>](РегистрБухгалтерииНаборЗаписей.Имя_регистра_бухгалтерии.md)

<!-- signature:start -->
`Сдвинуть`(`<Запись>`: [`РегистрБухгалтерииЗапись.\<Имя регистра бухгалтерии\>`](РегистрБухгалтерииЗапись.Имя_регистра_бухгалтерии.md), `<Смещение>`: `Число`)

`Сдвинуть`(`<Индекс>`: `Число`, `<Смещение>`: `Число`)
<!-- signature:end -->

РегистрБухгалтерииНаборЗаписей.<Имя регистра бухгалтерии> (AccountingRegisterRecordSet.<Accounting register name>)

Сдвинуть (Move)

Доступен, начиная с версии 8.0.

Вариант синтаксиса: По записи

Синтаксис:

Сдвинуть(<Запись>, <Смещение>)

Параметры:

<Запись> (обязательный)

Тип: [РегистрБухгалтерииЗапись.\<Имя регистра бухгалтерии\>](РегистрБухгалтерииЗапись.Имя_регистра_бухгалтерии.md).   
Перемещаемая запись набора.

<Смещение> (обязательный)

Тип: Число.   
Количество позиций, на которое следует сдвинуть запись. Положительное значение соответствует смещению записи в сторону конца списка записей набора (увеличение индекса), отрицательное - к началу.

Вариант синтаксиса: По индексу

Синтаксис:

Сдвинуть(<Индекс>, <Смещение>)

Параметры:

<Индекс> (обязательный)

Тип: Число.   
Позиция сдвигаемой записи в наборе.

<Смещение> (обязательный)

Тип: Число.   
Количество позиций, на которое следует сдвинуть запись. Положительное значение соответствует смещению записи в сторону конца списка записей набора (увеличение индекса), отрицательное - к началу.

Описание:

Сдвигает запись в наборе на указанное число позиций.

Доступность:

Сервер, толстый клиент, внешнее соединение.

Использование в версии:

Доступен, начиная с версии 8.0.

---
