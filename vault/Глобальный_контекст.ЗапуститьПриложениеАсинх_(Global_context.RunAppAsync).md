# Глобальный контекст.ЗапуститьПриложениеАсинх

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Глобальный контекст](Глобальный_контекст.md) › [Методы](Методы.md) › [Процедуры и функции работы с операционной системой](Процедуры_и_функции_работы_с_операционной_системой.md)

Глобальный контекст (Global context)

ЗапуститьПриложениеАсинх (RunAppAsync)

Доступен, начиная с версии 8.3.18.

Синтаксис:

ЗапуститьПриложениеАсинх(<СтрокаКоманды>, <ТекущийКаталог>, <ДождатьсяЗавершения>)

Параметры:

<СтрокаКоманды> (обязательный)

Тип: [Строка](lang__def_String.md).   
Командная строка для запуска приложения либо имя файла, ассоциированного с некоторым приложением.

<ТекущийКаталог> (необязательный)

Тип: [Строка](lang__def_String.md).   
Задает текущий каталог запускаемого приложения.  
В режиме веб-клиента игнорируется.

<ДождатьсяЗавершения> (необязательный)

Тип: [Булево](lang__Булево_(Boolean).md).   
[Истина](lang__def_BooleanTrue.md) - дожидаться завершения запущенного приложения перед продолжением работы.  
Значение по умолчанию: [Ложь](lang__def_BooleanFalse.md).

Возвращаемое значение:

Тип: [Обещание](Обещание_(Promise).md).   
Результатом выполнения обещания является код возврата типа [Число](lang__def_Number.md) или [Неопределено](lang__def_Undefined.md), если соответствующий входной параметр <ДождатьсяЗавершения> не указан.

Описание:

Асинхронная функция.  
Выполняет запуск внешнего приложения либо открытие файла с использованием ассоциированного с ним приложения.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, толстый клиент, мобильное приложение (клиент).

Примечание:

В отличие от функции [КомандаСистемы](Глобальный_контекст.КомандаСистемы_(Global_context.System).md) запуск приложения выполняется непосредственно, минуя командный интерпретатор.  
Поведение метода зависит от используемой операционной системы и режима запуска. Используемые в реализации метода механизмы операционной системы допускают различные варианты использования. Например, в некоторых режимах метод может открывать html ссылки с помощью браузера "по умолчанию". Такие возможности не являются штатными т.к. не гарантируется их наличие во всех режимах запуска.  
При запуске "1С:Предприятия" в пакетном режиме, а также при вызове метода на сервере будет сгенерировано исключение.  
Для работы метода на веб-клиенте необходимо предварительно подключить расширение работы с файлами.  
При использовании метода на мобильной платформе возможно только открытие файла по ассоциации.  
На мобильной платформе под управлением ОС Windows параметр <ДождатьсяЗавершения> игнорируется, а результат выполнения возвращается сразу после запуска приложения.

См. также:

[Глобальный контекст](Глобальный_контекст.md), метод [НачатьЗапускПриложения](Глобальный_контекст.НачатьЗапускПриложения_(Global_context.BeginRunningApplication).md)  
[Глобальный контекст](Глобальный_контекст.md), метод [ЗапуститьПриложение](Глобальный_контекст.ЗапуститьПриложение_(Global_context.RunApp).md)  

Использование в версии:

Доступен, начиная с версии 8.3.18.

---
