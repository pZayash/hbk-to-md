# КвалификаторыСтроки.По умолчанию

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=Описание_типов_значений.md">Описание типов значений</a> › [КвалификаторыСтроки](КвалификаторыСтроки.md)

<!-- signature:start -->
`КвалификаторыСтроки`(`<ДлинаСтроки>`: `Число`, `<ДопустимаяДлина>`: [`ДопустимаяДлина`](ДопустимаяДлина.md))
<!-- signature:end -->

КвалификаторыСтроки (StringQualifiers)

На основании описания строки

Доступен, начиная с версии 8.0.

Синтаксис:

Новый КвалификаторыСтроки(<ДлинаСтроки>, <ДопустимаяДлина>)

Параметры:

<ДлинаСтроки> (необязательный)

Тип: Число.   
Допустимая длина строки. Если параметр не указан, то строка по длине не ограничивается.  
Значение по умолчанию: 0.

<ДопустимаяДлина> (необязательный)

Тип: [ДопустимаяДлина](ДопустимаяДлина.md).   
Вариант ограничения длины строки, если задана длина строки, отличная от 0.  
Значение по умолчанию: [Переменная](ДопустимаяДлина.Переменная.md).

Описание:

Создает квалификатор строки заданием длины строки.

Пример:

|  |
| --- |
| ПараметрыСтроки = Новый КвалификаторыСтроки(50); |

Использование в версии:

Доступен, начиная с версии 8.0.

---
