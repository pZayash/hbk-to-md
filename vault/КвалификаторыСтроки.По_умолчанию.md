# КвалификаторыСтроки.По умолчанию

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Общие объекты](Общие_объекты.md) › [Описание типов значений](Описание_типов_значений.md) › [КвалификаторыСтроки](КвалификаторыСтроки_(StringQualifiers).md) › [Конструкторы](КвалификаторыСтроки__Конструкторы.md)

КвалификаторыСтроки (StringQualifiers)

На основании описания строки

Доступен, начиная с версии 8.0.

Синтаксис:

Новый КвалификаторыСтроки(<ДлинаСтроки>, <ДопустимаяДлина>)

Параметры:

<ДлинаСтроки> (необязательный)

Тип: [Число](lang__def_Number.md).   
Допустимая длина строки. Если параметр не указан, то строка по длине не ограничивается.  
Значение по умолчанию: 0.

<ДопустимаяДлина> (необязательный)

Тип: [ДопустимаяДлина](ДопустимаяДлина_(AllowedLength).md).   
Вариант ограничения длины строки, если задана длина строки, отличная от 0.  
Значение по умолчанию: [Переменная](ДопустимаяДлина.Переменная_(AllowedLength.Variable).md).

Описание:

Создает квалификатор строки заданием длины строки.

Пример:

|  |
| --- |
| ПараметрыСтроки = Новый КвалификаторыСтроки(50); |

Использование в версии:

Доступен, начиная с версии 8.0.

---
