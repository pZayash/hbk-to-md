# ТекстовыйДокумент.ЗаменитьСтроку

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=Текстовый_документ.md">Текстовый документ</a> › [ТекстовыйДокумент](ТекстовыйДокумент.md)

<!-- signature:start -->
`ЗаменитьСтроку`(`<НомерСтроки>`: `Число`, `<Строка>`: `Строка`)
<!-- signature:end -->

ТекстовыйДокумент (TextDocument)

ЗаменитьСтроку (ReplaceLine)

Доступен, начиная с версии 8.0.

Синтаксис:

ЗаменитьСтроку(<НомерСтроки>, <Строка>)

Параметры:

<НомерСтроки> (обязательный)

Тип: Число.   
Номер замещаемой строки.

<Строка> (обязательный)

Тип: Строка.   
Замещающая строка.

Описание:

Заменяет строку с указанным номером на переданную.

Доступность:

Тонкий клиент, веб-клиент, сервер, толстый клиент, внешнее соединение.

Пример:

|  |
| --- |
| ТекстДок.ЗаменитьСтроку(23, "Строка с исправлениями"); |

Использование в версии:

Доступен, начиная с версии 8.0.

---
