# ЭлементDOM.УстановитьАтрибутИдентификатор

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog1055.md">DOM</a> › [ЭлементDOM](ЭлементDOM.md)

<!-- signature:start -->
`УстановитьАтрибутИдентификатор`(`<Имя>`: `Строка`, `<ЭтоИД>`: `Булево`)

`УстановитьАтрибутИдентификатор`(`<URIПространстваИмен>`: `Строка`, `<ЛокальноеИмя>`: `Строка`, `<ЭтоИД>`: `Булево`)
<!-- signature:end -->

ЭлементDOM (DOMElement)

УстановитьАтрибутИдентификатор (SetIdAttribute)

Доступен, начиная с версии 8.1.

Вариант синтаксиса: По имени

Синтаксис:

УстановитьАтрибутИдентификатор(<Имя>, <ЭтоИД>)

Параметры:

<Имя> (обязательный)

Тип: Строка.   
Имя атрибута.

<ЭтоИД> (обязательный)

Тип: Булево.   
Признак идентификатора.

Описание варианта метода:

Если параметр <ЭтоИД> = Истина, то указанный атрибут становится "идентификатором, определенным пользователем".

Вариант синтаксиса: По имени и URI пространства имен

Синтаксис:

УстановитьАтрибутИдентификатор(<URIПространстваИмен>, <ЛокальноеИмя>, <ЭтоИД>)

Параметры:

<URIПространстваИмен> (обязательный)

Тип: Строка.   
URI пространства имен.

<ЛокальноеИмя> (обязательный)

Тип: Строка.   
Локальное имя атрибута.

<ЭтоИД> (обязательный)

Тип: Булево.   
Признак идентификатора.

Описание варианта метода:

Если значение параметра <ЭтоИД> = Истина, указанный атрибут становится "идентификатором, определенным пользователем".

Описание:

Устанавливает атрибуту признак идентификатора.

Доступность:

Сервер, толстый клиент, внешнее соединение, мобильное приложение (сервер), мобильный автономный сервер.

Примечание:

Метод не меняет установленной в документе схемы.  
Причины вызова исключений:

- данный узел в режиме Только для чтения;
- указанный атрибут не является атрибутом данного элемента.

Использование в версии:

Доступен, начиная с версии 8.1.

---
