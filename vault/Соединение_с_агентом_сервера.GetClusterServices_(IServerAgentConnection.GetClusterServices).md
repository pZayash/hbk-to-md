# Соединение с агентом сервера.GetClusterServices

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Средства интеграции и администрирования](Средства_интеграции_и_администрирования.md) › [Менеджер COM-соединений](Менеджер_COM-соединений.md) › [Администрирование кластера серверов](Администрирование_кластера_серверов.md) › [Соединение с агентом сервера](Соединение_с_агентом_сервера_(IServerAgentConnection).md) › [Методы](Соединение_с_агентом_сервера__Методы.md)

Соединение с агентом сервера (IServerAgentConnection)

GetClusterServices (GetClusterServices)

Доступен, начиная с версии 8.2.

Синтаксис:

GetClusterServices(<Кластер>)

Параметры:

<Кластер> (обязательный)

Тип: [Кластер серверов](Кластер_серверов_(IClusterInfo).md).   
Кластер серверов.

Возвращаемое значение:

Тип: [COMSafeArray](COMSafeArray_(COMSafeArray).md).   
Массив объектов [Сервис кластера](Сервис_кластера_(IClusterServiceInfo).md).

Описание:

Получает список сервисов кластера. Список сервисов фиксирован для кластера серверов 1С:Предприятия определенной версии. Каждый сервис, кроме сервиса сеансовых данных, работает в главном или каком-нибудь из дополнительных менеджеров кластера. Сервис сеансовых данных может работать на нескольких менеджерах одновременно. Сервис конфигурации кластера, Сервис блокировок кластера и Сервис управления предметами отладки могут работать только в главном менеджере кластера.

Доступность:

Интеграция.

Использование в версии:

Доступен, начиная с версии 8.2.

---
