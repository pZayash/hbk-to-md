# КоллекцияАтрибутовDOM.Элемент

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog1055.md">DOM</a> › [КоллекцияАтрибутовDOM](КоллекцияАтрибутовDOM.md)

<!-- signature:start -->
`Элемент`(`<Индекс>`: `Число`) → [`АтрибутDOM`](АтрибутDOM.md)
<!-- signature:end -->

КоллекцияАтрибутовDOM (DOMAttributeMap)

Элемент (Item)

Доступен, начиная с версии 8.1.

Синтаксис:

Элемент(<Индекс>)

Параметры:

<Индекс> (обязательный)

Тип: Число.   
Индекс атрибута в коллекции.

Возвращаемое значение:

Тип: [АтрибутDOM](АтрибутDOM.md).   

Описание:

Получает узел атрибута DOM по индексу.

Доступность:

Сервер, толстый клиент, внешнее соединение, мобильное приложение (сервер), мобильный автономный сервер.

Использование в версии:

Доступен, начиная с версии 8.1.

---
