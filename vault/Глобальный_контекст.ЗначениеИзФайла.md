# Глобальный контекст.ЗначениеИзФайла

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Глобальный_контекст.md">Глобальный контекст</a> › [Процедуры и функции сохранения значений](Процедуры_и_функции_сохранения_значений.md)

<!-- signature:start -->
`ЗначениеИзФайла`(`<ИмяФайла>`: `Строка`) → `Произвольный`
<!-- signature:end -->

Глобальный контекст (Global context)

ЗначениеИзФайла (ValueFromFile)

Доступен, начиная с версии 8.0.

Синтаксис:

ЗначениеИзФайла(<ИмяФайла>)

Параметры:

<ИмяФайла> (обязательный)

Тип: Строка.   
Имя файла. Не может быть пустым. Если включает путь к существующему каталогу, то значение будет извлекаться из файла, найденного в этом каталоге, в противном случае будет использован каталог исполняемых файлов программы.

Возвращаемое значение:

Тип: Произвольный.   

Описание:

Получает из файла значение, ранее сохраненное в нем при помощи функции [ЗначениеВФайл](Глобальный_контекст.ЗначениеВФайл.md). Тип возвращаемого значения определяется типом значения, сохраненного в файле.

Доступность:

Сервер, толстый клиент, внешнее соединение, мобильное приложение (сервер), мобильный автономный сервер.

Пример:

|  |
| --- |
| Попытка     ПолученноеЗначение = ЗначениеИзФайла(ИмяФайла);     Если ТипЗнч(ПолученноеЗначение) = Тип("ТаблицаЗначений") Тогда         // обработка значения      ИначеЕсли ТипЗнч(ПолученноеЗначение) = Тип("ДеревоЗначений") Тогда         // обработка значения      Иначе // Формат файла не опознан          Предупреждение(НСтр("ru='Невозможно загрузить список отчетов из указанного файла!                        |Выберите другой файл.';"            + " en='Impossible to load list of reports from selected file!                        |Select other file.'"));     КонецЕсли; Исключение     Предупреждение(НСтр("ru='Невозможно загрузить список отчетов из указанного файла!          |Выберите другой файл.';"        + " en='Impossible to load list of reports from selected file!          |Select other file.'")); КонецПопытки; |

См. также:

[Глобальный контекст](Глобальный_контекст.md), метод [ЗначениеВФайл](Глобальный_контекст.ЗначениеВФайл.md)  

Использование в версии:

Доступен, начиная с версии 8.0.

---
