# ОписаниеОповещения.По умолчанию

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Интерфейс.md">Интерфейс</a> › <a href="obsidian://open?file=Форма_клиентского_приложения.md">Форма клиентского приложения</a> › [ОписаниеОповещения](ОписаниеОповещения.md)

<!-- signature:start -->
`ОписаниеОповещения`(`<ИмяПроцедуры>`: `Строка, Неопределено`, `<Модуль>`: [`ФормаКлиентскогоПриложения`](ФормаКлиентскогоПриложения.md) | [`КомандаКомандногоИнтерфейса`](КомандаКомандногоИнтерфейса.md) | [`ОбщийМодуль`](ОбщийМодуль.md) | `Строка, Неопределено`, `<ДополнительныеПараметры>`: `Произвольный`, `<ИмяПроцедурыОбработкиОшибки>`, `<МодульОбработкиОшибки>`: [`ФормаКлиентскогоПриложения`](ФормаКлиентскогоПриложения.md) | [`КомандаКомандногоИнтерфейса`](КомандаКомандногоИнтерфейса.md) | [`ОбщийМодуль`](ОбщийМодуль.md))
<!-- signature:end -->

ОписаниеОповещения (CallbackDescription)

По умолчанию

Доступен, начиная с версии 8.3.3.

Синтаксис:

Новый ОписаниеОповещения(<ИмяПроцедуры>, <Модуль>, <ДополнительныеПараметры>, <ИмяПроцедурыОбработкиОшибки>, <МодульОбработкиОшибки>)

Параметры:

<ИмяПроцедуры> (необязательный)

Тип: Строка, Неопределено.   
Имя экспортируемой процедуры модуля, которая будет вызвана.

<Модуль> (необязательный)

Тип: [ФормаКлиентскогоПриложения](ФормаКлиентскогоПриложения.md), [КомандаКомандногоИнтерфейса](КомандаКомандногоИнтерфейса.md), [ОбщийМодуль](ОбщийМодуль.md), Строка, Неопределено.   
Программный модуль, процедура которого будет вызвана.  
При использовании типа [ФормаКлиентскогоПриложения](ФормаКлиентскогоПриложения.md) будет вызван метод модуля указанной формы клиентского приложения.  
При использовании типа [ОбщийМодуль](ОбщийМодуль.md) будет вызван метод общего неглобального модуля.  
При использовании типа [КомандаКомандногоИнтерфейса](КомандаКомандногоИнтерфейса.md) будет вызван метод модуля команды командного интерфейса.  
Объект данного типа может быть получен только из модуля команды.  
Для получения ссылки на него необходимо использовать свойство "ЭтотОбъект".  
Оно доступно в контексте модуля команды.

<ДополнительныеПараметры> (необязательный)

Тип: Произвольный.   
Значение любого типа, которое при вызове будет передано в указанную процедуру последним параметром.  
Значение по умолчанию: Неопределено.

<ИмяПроцедурыОбработкиОшибки> (необязательный)

Имя экспортируемой процедуры модуля, которая будет вызвана в случае возникновения ошибки.

<МодульОбработкиОшибки> (необязательный)

Тип: [ФормаКлиентскогоПриложения](ФормаКлиентскогоПриложения.md), [КомандаКомандногоИнтерфейса](КомандаКомандногоИнтерфейса.md), [ОбщийМодуль](ОбщийМодуль.md).   
Программный модуль, процедура которого будет вызвана в случае возникновения ошибки.  
При использовании типа [ФормаКлиентскогоПриложения](ФормаКлиентскогоПриложения.md) будет вызван метод модуля указанной формы клиентского приложения.  
При использовании типа [КомандаКомандногоИнтерфейса](КомандаКомандногоИнтерфейса.md) будет вызван метод модуля команды командного интерфейса.  
При использовании типа [ОбщийМодуль](ОбщийМодуль.md) будет вызван метод общего неглобального модуля.

Описание:

Создает объект и связывает его с вызовом указанной процедуры модуля.

Использование в версии:

Доступен, начиная с версии 8.3.3.

---
