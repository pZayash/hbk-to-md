# СертификатКриптографии.Выгрузить

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Общие объекты](Общие_объекты.md) › [Криптография](Криптография-2.md) › [СертификатКриптографии](СертификатКриптографии_(CryptoCertificate).md) › [Методы](СертификатКриптографии__Методы.md)

СертификатКриптографии (CryptoCertificate)

Выгрузить (Unload)

Доступен, начиная с версии 8.2.

Вариант синтаксиса: В двоичные данные

Синтаксис:

Выгрузить()

Описание варианта метода:

Возвращает двоичные данные выгруженного сертификата.

Вариант синтаксиса: В файл

Синтаксис:

Выгрузить(<ИмяФайла>)

Параметры:

<ИмяФайла> (обязательный)

Тип: [Строка](lang__def_String.md).   
Имя файла, в который будет выгружен сертификат.

Описание варианта метода:

Выгружает сертификат в файл. Возвращает [Неопределено](lang__def_Undefined.md).

Вариант синтаксиса: В поток

Синтаксис:

Выгрузить(<Поток>)

Параметры:

<Поток> (обязательный)

Тип: [Поток](Поток_(Stream).md), [ПотокВПамяти](ПотокВПамяти_(MemoryStream).md), [ФайловыйПоток](ФайловыйПоток_(FileStream).md).   
Поток, в который будет записан сертификат.

Описание варианта метода:

Выгружает сертификат в поток.

Возвращаемое значение:

Тип: [ДвоичныеДанные](ДвоичныеДанные_(BinaryData).md), [Неопределено](lang__def_Undefined.md).   
Если выгрузка производится в файл или поток, то возвращает [Неопределено](lang__def_Undefined.md). Если в двоичные данные, то возвращает [ДвоичныеДанные](ДвоичныеДанные_(BinaryData).md).

Описание:

Выгружает сертификат.  
Для работы метода на веб-клиенте необходимо предварительно подключить расширение работы с криптографией. В веб-клиенте метод поддерживается только в браузере Internet Explorer, если в конфигурации используется режим синхронных вызовов расширений платформы и внешних компонент.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер.

Примечание:

В случае, если сертификат связан с закрытым ключом, выгрузка закрытого ключа не производится.

См. также:

[СертификатКриптографии](СертификатКриптографии_(CryptoCertificate).md), метод [ВыгрузитьАсинх](СертификатКриптографии.ВыгрузитьАсинх_(CryptoCertificate.UnloadAsync).md)  
[СертификатКриптографии](СертификатКриптографии_(CryptoCertificate).md), метод [НачатьВыгрузку](СертификатКриптографии.НачатьВыгрузку_(CryptoCertificate.BeginUnloading).md)  

Использование в версии:

Доступен, начиная с версии 8.2.

Описание изменено в версии 8.3.24.

---
