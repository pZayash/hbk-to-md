# МенеджерСистемыВзаимодействия.НачатьОтключениеОбработчикаНовыхСообщений

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog2454.md">Система взаимодействия</a> › [МенеджерСистемыВзаимодействия](МенеджерСистемыВзаимодействия.md)

<!-- signature:start -->
`НачатьОтключениеОбработчикаНовыхСообщений`(`<ОписаниеОповещения>`: [`ОписаниеОповещения`](ОписаниеОповещения.md), `<Обсуждение>`: [`ИдентификаторОбсужденияСистемыВзаимодействия`](ИдентификаторОбсужденияСистемыВзаимодействия.md), `<Обработчик>`: [`ОписаниеОповещения`](ОписаниеОповещения.md))

`НачатьОтключениеОбработчикаНовыхСообщений`(`<ОписаниеОповещения>`: [`ОписаниеОповещения`](ОписаниеОповещения.md), `<Ключ>`: `Строка`, `<Обработчик>`: [`ОписаниеОповещения`](ОписаниеОповещения.md))
<!-- signature:end -->

МенеджерСистемыВзаимодействия (CollaborationSystemManager)

НачатьОтключениеОбработчикаНовыхСообщений (BeginDetachNewMessagesHandler)

Доступен, начиная с версии 8.3.11.

Вариант синтаксиса: По идентификатору

Синтаксис:

НачатьОтключениеОбработчикаНовыхСообщений(<ОписаниеОповещения>, <Обсуждение>, <Обработчик>)

Параметры:

<ОписаниеОповещения> (необязательный)

Тип: [ОписаниеОповещения](ОписаниеОповещения.md).   
Содержит описание процедуры, которая будет вызвана после отключения обработчика со следующими параметрами:

- <Дополнительные параметры> - значение, которое было указано при создании объекта [ОписаниеОповещения](ОписаниеОповещения.md).

<Обсуждение> (обязательный)

Тип: [ИдентификаторОбсужденияСистемыВзаимодействия](ИдентификаторОбсужденияСистемыВзаимодействия.md).   
Идентификатор обсуждения, для которого отключается обработчик.

<Обработчик> (необязательный)

Тип: [ОписаниеОповещения](ОписаниеОповещения.md).   
Содержит описание процедуры, заданной при вызове метода [НачатьПодключениеОбработчикаНовыхСообщений](МенеджерСистемыВзаимодействия.НачатьПодключениеОбработчикаНовыхСообщений.md).

Описание варианта метода:

Выполняет отключение обработчика по идентификатору обсуждения.

Вариант синтаксиса: По ключу

Синтаксис:

НачатьОтключениеОбработчикаНовыхСообщений(<ОписаниеОповещения>, <Ключ>, <Обработчик>)

Параметры:

<ОписаниеОповещения> (необязательный)

Тип: [ОписаниеОповещения](ОписаниеОповещения.md).   
Содержит описание процедуры, которая будет вызвана после отключения обработчика со следующими параметрами:

- <Дополнительные параметры> - значение, которое было указано при создании объекта [ОписаниеОповещения](ОписаниеОповещения.md).

<Ключ> (обязательный)

Тип: Строка.   
Ключ обсуждения, для которого отключается обработчик.

<Обработчик> (необязательный)

Тип: [ОписаниеОповещения](ОписаниеОповещения.md).   
Содержит описание процедуры, заданной при вызове метода [НачатьПодключениеОбработчикаНовыхСообщений](МенеджерСистемыВзаимодействия.НачатьПодключениеОбработчикаНовыхСообщений.md).

Описание варианта метода:

Выполняет отключение обработчика по ключу обсуждения.

Описание:

Отключает ранее подключенный обработчик.  
Сравнение обработчиков выполняется по равенству значений свойств:

- ИмяПроцедуры,
- ИмяПроцедурыОбработкиОшибки,
- Модуль,
- МодульОбработкиОшибки.

Не рекомендуется выполнять отключение обработчика в коде самого обработчика.  
Параметр <ОписаниеОповещения> всегда вызывается как результат успешного выполнения, вне зависимости от того, был ли ранее подключен обработчик и выполнил ли сервер системы взаимодействия отключение обработчика.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, толстый клиент.

См. также:

[МенеджерСистемыВзаимодействия](МенеджерСистемыВзаимодействия.md), метод [ОтключитьОбработчикНовыхСообщенийАсинх](МенеджерСистемыВзаимодействия.ОтключитьОбработчикНовыхСообщенийАсинх.md)  

Использование в версии:

Доступен, начиная с версии 8.3.11.

Описание изменено в версии 8.3.18.

---
