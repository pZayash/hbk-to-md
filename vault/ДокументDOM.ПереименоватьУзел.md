# ДокументDOM.ПереименоватьУзел

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog1055.md">DOM</a> › [ДокументDOM](ДокументDOM.md)

<!-- signature:start -->
`ПереименоватьУзел`(`<Узел>`: [`АтрибутDOM`](АтрибутDOM.md) | [`ЭлементDOM`](ЭлементDOM.md), `<URIПространстваИмен>`: `Строка`, `<КвалифицированноеИмя>`: `Строка`) → [`АтрибутDOM`](АтрибутDOM.md) | [`ДокументDOM`](ДокументDOM.md) | [`ЭлементDOM`](ЭлементDOM.md) | [`ОпределениеТипаДокументаDOM`](ОпределениеТипаДокументаDOM.md) | [`НотацияDOM`](НотацияDOM.md) | [`СущностьDOM`](СущностьDOM.md) | [`ФрагментДокументаDOM`](ФрагментДокументаDOM.md) | [`ТекстDOM`](ТекстDOM.md) | [`КомментарийDOM`](КомментарийDOM.md) | [`СекцияCDATADOM`](СекцияCDATADOM.md) | [`ИнструкцияОбработкиDOM`](ИнструкцияОбработкиDOM.md) | [`СсылкаНаСущностьDOM`](СсылкаНаСущностьDOM.md) | [`ПространствоИменXPath`](ПространствоИменXPath.md)
<!-- signature:end -->

ДокументDOM (DOMDocument)

ПереименоватьУзел (RenameNode)

Доступен, начиная с версии 8.1.

Синтаксис:

ПереименоватьУзел(<Узел>, <URIПространстваИмен>, <КвалифицированноеИмя>)

Параметры:

<Узел> (обязательный)

Тип: [АтрибутDOM](АтрибутDOM.md), [ЭлементDOM](ЭлементDOM.md).   
Переименовываемый узел DOM.

<URIПространстваИмен> (обязательный)

Тип: Строка.   
URI пространства имен переименовываемого элемента/атрибута.

<КвалифицированноеИмя> (обязательный)

Тип: Строка.   
Новое квалифицируемое имя переименовываемого узла.

Возвращаемое значение:

Тип: [АтрибутDOM](АтрибутDOM.md), [ДокументDOM](ДокументDOM.md), [ЭлементDOM](ЭлементDOM.md), [ОпределениеТипаДокументаDOM](ОпределениеТипаДокументаDOM.md), [НотацияDOM](НотацияDOM.md), [СущностьDOM](СущностьDOM.md), [ФрагментДокументаDOM](ФрагментДокументаDOM.md), [ТекстDOM](ТекстDOM.md), [КомментарийDOM](КомментарийDOM.md), [СекцияCDATADOM](СекцияCDATADOM.md), [ИнструкцияОбработкиDOM](ИнструкцияОбработкиDOM.md), [СсылкаНаСущностьDOM](СсылкаНаСущностьDOM.md), [ПространствоИменXPath](ПространствоИменXPath.md).   

Описание:

Переименовывает узлы [Элемент](ТипУзлаDOM.Элемент.md) и [Атрибут](ТипУзлаDOM.Атрибут.md).  
Выполняется попытка переименования существующего узла, в случае невозможности, создает новый узел с новым именем, префиксом и URI пространства имен и заменяет им исходный узел.

Доступность:

Сервер, толстый клиент, внешнее соединение, мобильное приложение (сервер), мобильный автономный сервер.

Примечание:

Причины вызова исключений:

- попытка переименования узлов, отличных от [Элемент](ТипУзлаDOM.Элемент.md) или [Атрибут](ТипУзлаDOM.Атрибут.md), или в случае, если реализация не поддерживает переименование узлов;
- квалифицированное имя содержит символы, недопустимые стандартом XML данного документа;
- попытка переименования узла, созданного другим документом DOM;
- квалифицированное имя содержит префикс, значение которого недопустимо для данного значения URI пространства имен.

Использование в версии:

Доступен, начиная с версии 8.1.

---
