# ФабрикаXDTO.ПрочитатьJSON

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog1015.md">XDTO</a> › [ФабрикаXDTO](ФабрикаXDTO.md)

<!-- signature:start -->
`ПрочитатьJSON`(`<ЧтениеJSON>`: [`ЧтениеJSON`](ЧтениеJSON.md), `<Тип>`: [`ТипЗначенияXDTO`](ТипЗначенияXDTO.md) | [`ТипОбъектаXDTO`](ТипОбъектаXDTO.md), `<ИмяФункцииВосстановления>`: `Строка`, `<МодульФункцииВосстановления>`: [`ФормаКлиентскогоПриложения`](ФормаКлиентскогоПриложения.md) | [`КомандаКомандногоИнтерфейса`](КомандаКомандногоИнтерфейса.md) | [`ОбщийМодуль`](ОбщийМодуль.md), `<ДополнительныеПараметрыФункцииВосстановления>`: `Произвольный`, `<ТипыДляОбработкиВосстановления>`: [`Массив`](Массив.md), `<ИменаСвойствДляВосстановления>`: [`Массив`](Массив.md), `<НазначениеТипа>`: [`НазначениеТипаXML`](НазначениеТипаXML.md)) → [`ОбъектXDTO`](ОбъектXDTO.md)
<!-- signature:end -->

ФабрикаXDTO (XDTOFactory)

ПрочитатьJSON (ReadJSON)

Доступен, начиная с версии 8.3.7.

Синтаксис:

ПрочитатьJSON(<ЧтениеJSON>, <Тип>, <ИмяФункцииВосстановления>, <МодульФункцииВосстановления>, <ДополнительныеПараметрыФункцииВосстановления>, <ТипыДляОбработкиВосстановления>, <ИменаСвойствДляВосстановления>, <НазначениеТипа>)

Параметры:

<ЧтениеJSON> (обязательный)

Тип: [ЧтениеJSON](ЧтениеJSON.md).   
Объект чтения JSON.

<Тип> (необязательный)

Тип: [ТипЗначенияXDTO](ТипЗначенияXDTO.md), [ТипОбъектаXDTO](ТипОбъектаXDTO.md).   
Тип элемента данных XDTO. Если тип не указан, то будет сделана попытка определить тип данных по текущему состоянию объекта [ЧтениеJSON](ЧтениеJSON.md).

<ИмяФункцииВосстановления> (необязательный)

Тип: Строка.   
Имя функции восстановления значения.  
Функция должна иметь следующие параметры:

- <Свойство> - значение типа Строка,
- <Тип> - значение типа [ТипОбъектаXDTO](ТипОбъектаXDTO.md),
- <Значение> - значение допустимого для сериализации типа,
- <ДополнительныеПараметры>.

Возвращаемое значение - допустимого для XDTO сериализации типа.  
Значение по умолчанию: Неопределено.

<МодульФункцииВосстановления> (необязательный)

Тип: [ФормаКлиентскогоПриложения](ФормаКлиентскогоПриложения.md), [КомандаКомандногоИнтерфейса](КомандаКомандногоИнтерфейса.md), [ОбщийМодуль](ОбщийМодуль.md).   
Указывает модуль, процедура которого будет использована для восстановления значения. В зависимости от типа параметра будет вызван соответствующий метод:

- [ФормаКлиентскогоПриложения](ФормаКлиентскогоПриложения.md) - будет вызван метод модуля указанной формы клиентского приложения.
- [КомандаКомандногоИнтерфейса](КомандаКомандногоИнтерфейса.md) - будет вызван метод модуля команды командного интерфейса.
- [ОбщийМодуль](ОбщийМодуль.md) - будет вызван метод неглобального общего модуля.

  
Значение по умолчанию: Неопределено.

<ДополнительныеПараметрыФункцииВосстановления> (необязательный)

Тип: Произвольный.   
Дополнительные параметры для функции восстановления.  
Значение по умолчанию: Неопределено.

<ТипыДляОбработкиВосстановления> (необязательный)

Тип: [Массив](Массив.md).   
Массив объектов [ТипОбъектаXDTO](ТипОбъектаXDTO.md), соответствующих типам XDTO, для которых требуется вызвать функцию восстановления.  
Если функция восстановления не задана, то параметры <ТипыДляОбработкиВосстановления> и <ИменаСвойствДляВосстановления> игнорируются.

<ИменаСвойствДляВосстановления> (необязательный)

Тип: [Массив](Массив.md).   
Имена свойств, для которых нужно вызвать функцию восстановления.  
Если функция восстановления не задана, то параметры <ТипыДляОбработкиВосстановления> и <ИменаСвойствДляВосстановления> игнорируются.

<НазначениеТипа> (необязательный)

Тип: [НазначениеТипаXML](НазначениеТипаXML.md).   
Назначение типа данных произвольных свойства при чтении.  
Значение по умолчанию: [Неявное](НазначениеТипаXML.Неявное.md).

Возвращаемое значение:

Тип: [ОбъектXDTO](ОбъектXDTO.md).   

Описание:

Читает элемент данных XDTO указанного типа из объекта чтения JSON.

Доступность:

Тонкий клиент, мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер.

Использование в версии:

Доступен, начиная с версии 8.3.7.

Описание изменено в версии 8.3.25.

---
