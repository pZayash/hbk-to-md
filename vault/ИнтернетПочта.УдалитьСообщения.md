# ИнтернетПочта.УдалитьСообщения

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog2442.md">Почта</a> › <a href="obsidian://open?file=ИнтернетПочта.md">ИнтернетПочта</a> › [ИнтернетПочта](ИнтернетПочта-2.md)

<!-- signature:start -->
`УдалитьСообщения`(`<МассивУдаляемыхДанных>`: [`Массив`](Массив.md))
<!-- signature:end -->

ИнтернетПочта (InternetMail)

УдалитьСообщения (DeleteMessages)

Доступен, начиная с версии 8.0.

Синтаксис:

УдалитьСообщения(<МассивУдаляемыхДанных>)

Параметры:

<МассивУдаляемыхДанных> (обязательный)

Тип: [Массив](Массив.md).   
Массив, содержащий либо заголовки сообщений, либо серверные идентификаторы сообщений, которые необходимо удалить с сервера.  
Для варианта работы с [IMAP](ПротоколИнтернетПочты.IMAP.md) протоколом также допускается передать массив порядковых номеров сообщений (целые числа) в текущем почтовом ящике ([ТекущийПочтовыйЯщик](ИнтернетПочта.ТекущийПочтовыйЯщик.md)).

Описание:

При работе с POP3 удаляет с сервера все сообщения, указанные либо объектами [ИнтернетПочтовоеСообщение](ИнтернетПочтовоеСообщение.md), либо идентификаторами, находящимися в массиве, принимаемом в качестве параметра.  
При работе с IMAP помечает как удаленные на сервере все сообщения, указанные одним из следующих способов:

- объектами [ИнтернетПочтовоеСообщение](ИнтернетПочтовоеСообщение.md),
- идентификаторами, находящимися в массиве, принимаемом в качестве параметра,
- порядковыми номерами сообщений в текущем почтовом ящике, заданном свойством [ТекущийПочтовыйЯщик](ИнтернетПочта.ТекущийПочтовыйЯщик.md).

Помеченные сообщения можно окончательно удалить методом [ОчиститьУдаленныеСообщения](ИнтернетПочта.ОчиститьУдаленныеСообщения.md) или снять отметку удаления с помощью метода [ОтменитьУдалениеСообщений](ИнтернетПочта.ОтменитьУдалениеСообщений.md).

Доступность:

Тонкий клиент, мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер.

Пример:

|  |
| --- |
| Профиль = Новый ИнтернетПочтовыйПрофиль; ЗаполнитьПрофиль(Профиль);  Почта = Новый ИнтернетПочта; Почта.Подключиться(Профиль);  Масс = Почта.Выбрать(Ложь);  МассСооб   = Новый Массив; МассСервИД = Новый Массив;  Для Ин = 0 По Масс.Количество() - 1 Цикл          Если Цел(Ин / 2) = Ин / 2 Тогда                  МассСооб.Добавить(Масс[Ин]);              Иначе                  МассСервИД.Добавить(Масс[Ин].Идентификатор[0]);              КонецЕсли;  КонецЦикла;      Почта.УдалитьСообщения(МассСооб); Почта.УдалитьСообщения(МассСервИД);  Почта.Отключиться(); |

Использование в версии:

Доступен, начиная с версии 8.0.

---
