# ТабличныйДокумент.ПроверитьВывод

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog64.md">Табличный документ</a> › [ТабличныйДокумент](ТабличныйДокумент.md)

<!-- signature:start -->
`ПроверитьВывод`(`<Таблицы>`: [`Массив`](Массив.md) | [`ТабличныйДокумент`](ТабличныйДокумент.md)) → `Булево`
<!-- signature:end -->

ТабличныйДокумент (SpreadsheetDocument)

ПроверитьВывод (CheckPut)

Доступен, начиная с версии 8.0.

Синтаксис:

ПроверитьВывод(<Таблицы>)

Параметры:

<Таблицы> (обязательный)

Тип: [Массив](Массив.md), [ТабличныйДокумент](ТабличныйДокумент.md).   
Массив из проверяемых таблиц или табличный документ.

Возвращаемое значение:

Тип: Булево.   
Истина - умещаются; Ложь - в противном случае.

Описание:

Проверяет, умещаются ли переданные табличные документы на страницу при печати.

Доступность:

Сервер, толстый клиент, внешнее соединение.

Примечание:

При возникновении проблем с получением информации о текущем принтере (например, в системе не установлено ни одного принтера), будет вызвано исключение.  
Следует учитывать, если для табличного документа установлены поля, размер которых меньше размера полей, установленных для принтера, на котором документ будет напечатан, то при печати содержимое некоторых строк может не уместиться на странице, даже если метод возвращает значение Истина.

Пример:

|  |
| --- |
| Т.Добавить(ШапкаДокумента); Т.Добавить(СтрокаДокумента); Т.Добавить(ПодвалДокумента); Если Не ТабДок.ПроверитьВывод(Т) Тогда     Сообщить("Документ не умещается на страницу!"); КонецЕсли; |

См. также:

[ТабличныйДокумент](ТабличныйДокумент.md), метод [ПроверитьПрисоединение](ТабличныйДокумент.ПроверитьПрисоединение.md)  

Использование в версии:

Доступен, начиная с версии 8.0.

---
