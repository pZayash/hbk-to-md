# МенеджерПолнотекстовогоПоиска.ОбновитьИндекс

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Общие объекты](Общие_объекты.md) › [Полнотекстовый поиск](Полнотекстовый_поиск.md) › [МенеджерПолнотекстовогоПоиска](МенеджерПолнотекстовогоПоиска_(FullTextSearchManager).md) › [Методы](МенеджерПолнотекстовогоПоиска__Методы.md)

МенеджерПолнотекстовогоПоиска (FullTextSearchManager)

ОбновитьИндекс (UpdateIndex)

Доступен, начиная с версии 8.1.

Синтаксис:

ОбновитьИндекс(<РазрешитьСлияние>, <Порционное>)

Параметры:

<РазрешитьСлияние> (необязательный)

Тип: [Булево](lang__Булево_(Boolean).md).   
Разрешает слияние индексов.   
Если [Истина](lang__def_BooleanTrue.md), то выполняется слияние частичного и полного индексов.  
Значение по умолчанию: [Ложь](lang__def_BooleanFalse.md).

<Порционное> (необязательный)

Тип: [Булево](lang__Булево_(Boolean).md).   
[Истина](lang__def_BooleanTrue.md) - обновление индексов будет осуществляться порциями. При каждом вызове метода выполняется порционное обновление индекса. Размер порции равен 10 тысяч объектов индексирования. При этом сначала в порцию выбираются объекты, не привязанные ко времени (например, справочники), затем, если порция еще не заполнена, выбираются объекты, привязанные ко времени (например, документы). Сначала выбираются новые объекты, а затем старые. При выборе анализируются все временные объекты, в том числе и регистры сведений с периодами (берется старшая дата периода), так, чтобы порция включала поровну объекты всех типов.   
После индексирования данных одной порции процесс завершается.   
Если [Ложь](lang__def_BooleanFalse.md), то индексирует все.  
Значение по умолчанию: [Ложь](lang__def_BooleanFalse.md).

Описание:

Обновляет индекс полнотекстового поиска.

Доступность:

Сервер, толстый клиент, внешнее соединение, мобильное приложение (сервер), мобильный автономный сервер.

Примечание:

Обновление индекса может выполняться продолжительное время.  
Если режим полнотекстового поиска запрещен, то использование метода вызывает исключение.  
  
Если используется [Версия2](ВерсияПолнотекстовогоПоиска.Версия2_(FullTextSearchVersion.Version2).md) полнотекстового поиска, то метод запускается асинхронно и сразу возвращает управление. Чтобы дождаться полной индексации поиска из встроенного языка, требуется периодически вызывать [ИндексАктуален](МенеджерПолнотекстовогоПоиска.ИндексАктуален_(FullTextSearchManager.IndexTrue).md), который вернет [Ложь](lang__def_BooleanFalse.md) при наличии непроиндексированных данных.

Использование в версии:

Доступен, начиная с версии 8.1.

Описание изменено в версии 8.3.22.

---
