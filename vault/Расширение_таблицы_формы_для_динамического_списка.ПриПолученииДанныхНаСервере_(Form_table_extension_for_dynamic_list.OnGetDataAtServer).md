# Расширение таблицы формы для динамического списка.ПриПолученииДанныхНаСервере

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Интерфейс](Интерфейс_(управляемый).md) › [Таблица формы](Таблица_формы.md) › [Расширение для динамического списка](Расширение_для_динамического_списка.md) › [Расширение таблицы формы для динамического списка](Расширение_таблицы_формы_для_динамического_списка_(Form_table_extension_for_dynamic_list).md) › [События](Расширение_таблицы_формы_для_динамического_списка__События.md)

Расширение таблицы формы для динамического списка (Form table extension for dynamic list)

ПриПолученииДанныхНаСервере (OnGetDataAtServer)

Доступен, начиная с версии 8.3.10.

Синтаксис:

ПриПолученииДанныхНаСервере(<ИмяЭлемента>, <Настройки>, <Строки>)

Параметры:

<ИмяЭлемента>

Тип: [Строка](lang__def_String.md).   
Содержит имя таблицы формы, из которой вызван обработчик события.

<Настройки>

Тип: [НастройкиКомпоновкиДанных](НастройкиКомпоновкиДанных_(DataCompositionSettings).md).   
Содержит копию полных настроек динамического списка.

<Строки>

Тип: [СтрокиДинамическогоСписка](СтрокиДинамическогоСписка_(DynamicListRows).md).   
Коллекция содержит данные и оформление всех строк, получаемых в списке, кроме строк группировок.

Описание:

Обработчик события вызывается на сервере после получения данных динамическим списком, только если была получена как минимум одна строка. При этом элемент формы, с которым связано событие, не передается в обработчик.   
Обработчик вызывается в режиме совместимости конфигурации [Версия8\_3\_8](РежимСовместимости.Версия8_3_8_(CompatibilityMode.Version8_3_8).md) и выше.  
Если в данном обработчике события изменяются данные строк, то рекомендуется исключать поля, в которых изменяются данные, из списков полей, используемых для группировки, упорядочивания и отбора. Если этого не сделать, группировка, упорядочивание и отбор (включая все виды поиска) будет работать неожиданно для пользователя. Для установки ограничения на использование поля в той или иной операции предназначены методы динамического списка [УстановитьОграниченияИспользованияВГруппировке](ДинамическийСписок.УстановитьОграниченияИспользованияВГруппировке_(DynamicList.SetRestrictionsForUseInGroup).md), [УстановитьОграниченияИспользованияВПорядке](ДинамическийСписок.УстановитьОграниченияИспользованияВПорядке_(DynamicList.SetRestrictionsForUseInOrder).md) и [УстановитьОграниченияИспользованияВОтборе](ДинамическийСписок.УстановитьОграниченияИспользованияВОтборе_(DynamicList.SetRestrictionsForUseInFilter).md).

Доступность:

Сервер.

Примечание:

Обработчик данного события должен иметь директиву &НаСервереБезКонтекста.  
Отбор (а также поиск), сортировка, группировка и условное оформление будут применяться к значениям до изменения их обработчиком.

Использование в версии:

Доступен, начиная с версии 8.3.10.

---
