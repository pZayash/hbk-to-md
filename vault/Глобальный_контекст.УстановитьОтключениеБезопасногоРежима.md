# Глобальный контекст.УстановитьОтключениеБезопасногоРежима

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Глобальный_контекст.md">Глобальный контекст</a> › [Процедуры и функции работы с информационной базой](Процедуры_и_функции_работы_с_информационной_базой.md)

<!-- signature:start -->
`УстановитьОтключениеБезопасногоРежима`(`<Отключить>`: `Булево`)
<!-- signature:end -->

Глобальный контекст (Global context)

УстановитьОтключениеБезопасногоРежима (SetSafeModeDisabled)

Доступен, начиная с версии 8.3.9.

Синтаксис:

УстановитьОтключениеБезопасногоРежима(<Отключить>)

Параметры:

<Отключить> (необязательный)

Тип: Булево.   
Устанавливает состояние безопасного режима:

- Истина - режим будет отключен;
- Ложь - режим будет включен.

Описание:

Выключает текущий режим безопасности.  
Чтобы включить безопасный режим в той же процедуре или функции, в которой он был отключен, следует вызвать метод с параметром, установленным в значение Ложь, столько раз, сколько раз он был вызван с параметром, установленным в значение Истина.  
При завершении процедуры/возврате из функции, если безопасный режим был выключен, он будет включен автоматически.   
Если в тексте процедуры/функции количество вызовов включения безопасного режима превышает количество вызовов его отключения, будет вызвано исключение.  
Внимание! Метод не выполняет никаких действий из кода обработок и расширений, открытых в безопасном режиме.

Доступность:

Сервер, толстый клиент, внешнее соединение, мобильное приложение (сервер), мобильный автономный сервер, интеграция.

Примечание:

Для обработчиков событий объектов метаданных безопасный режим выключается автоматически.

Использование в версии:

Доступен, начиная с версии 8.3.9.

Описание изменено в версии 8.3.24.

---
