# ПоляВыбораКомпоновкиДанныхСхемыЗапроса.Добавить

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Работа_с_запросами.md">Работа с запросами</a> › <a href="obsidian://open?file=objects__catalog213__catalog2171.md">Схема запроса</a> › [ПоляВыбораКомпоновкиДанныхСхемыЗапроса](ПоляВыбораКомпоновкиДанныхСхемыЗапроса.md)

<!-- signature:start -->
`Добавить`(`<Имя колонки>`: `Строка`)

`Добавить`(`<Колонка>`: [`КолонкаСхемыЗапроса`](КолонкаСхемыЗапроса.md)) → [`ПолеВыбораКомпоновкиДанныхСхемыЗапроса`](ПолеВыбораКомпоновкиДанныхСхемыЗапроса.md)
<!-- signature:end -->

ПоляВыбораКомпоновкиДанныхСхемыЗапроса (QuerySchemaDataCompositionSelectionFields)

Добавить (Add)

Доступен, начиная с версии 8.3.14.

Вариант синтаксиса: Из строки

Синтаксис:

Добавить(<Имя колонки>)

Параметры:

<Имя колонки> (обязательный)

Тип: Строка.   
Имя колонки схемы запроса, добавляемой в поля выбора компоновки данных.

Описание варианта метода:

Добавляет поле выбора компоновки данных из строки.

Вариант синтаксиса: Из колонки

Синтаксис:

Добавить(<Колонка>)

Параметры:

<Колонка> (обязательный)

Тип: [КолонкаСхемыЗапроса](КолонкаСхемыЗапроса.md).   
Колонка схемы запроса, добавляемая в поля выбора компоновки данных.

Описание варианта метода:

Добавляет поле выбора компоновки данных из колонки схемы запроса.

Возвращаемое значение:

Тип: [ПолеВыбораКомпоновкиДанныхСхемыЗапроса](ПолеВыбораКомпоновкиДанныхСхемыЗапроса.md).   

Описание:

Добавляет новый элемент коллекции. Если тип нового элемента не подходит для данной коллекции, добавление не выполняется.

Доступность:

Сервер, толстый клиент, внешнее соединение.

Использование в версии:

Доступен, начиная с версии 8.3.14.

---
