# Соединение с агентом сервера.GetWorkingProcesses

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Средства интеграции и администрирования](Средства_интеграции_и_администрирования.md) › [Менеджер COM-соединений](Менеджер_COM-соединений.md) › [Администрирование кластера серверов](Администрирование_кластера_серверов.md) › [Соединение с агентом сервера](Соединение_с_агентом_сервера_(IServerAgentConnection).md) › [Методы](Соединение_с_агентом_сервера__Методы.md)

Соединение с агентом сервера (IServerAgentConnection)

GetWorkingProcesses (GetWorkingProcesses)

Доступен, начиная с версии 8.1.

Синтаксис:

GetWorkingProcesses(<Кластер>)

Параметры:

<Кластер> (обязательный)

Тип: [Кластер серверов](Кластер_серверов_(IClusterInfo).md).   
Кластер серверов, для которого должен быть получен массив рабочих процессов.

Возвращаемое значение:

Тип: [COMSafeArray](COMSafeArray_(COMSafeArray).md).   
Массив рабочих процессов. Каждый рабочий процесс является объектом с интерфейсом [Рабочий процесс](Рабочий_процесс_(IWorkingProcessInfo).md).

Описание:

Позволяет получить массив рабочих процессов, зарегистрированных в заданном кластере. Для успешного выполнения метода необходима аутентификация одного из администраторов кластера.

Доступность:

Интеграция.

Использование в версии:

Доступен, начиная с версии 8.1.

---
