# ВыборкаДанных.Получить

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Прикладные_объекты.md">Прикладные объекты</a> › <a href="obsidian://open?file=objects__catalog125__catalog676.md">Планы обмена</a> › [ВыборкаДанных](ВыборкаДанных.md)

<!-- signature:start -->
`Получить`() → `Данные` | [`УдалениеОбъекта`](УдалениеОбъекта.md) | `Неопределено`
<!-- signature:end -->

ВыборкаДанных (DataSelection)

Получить (Get)

Доступен, начиная с версии 8.0.

Синтаксис:

Получить()

Возвращаемое значение:

Тип: Данные, [УдалениеОбъекта](УдалениеОбъекта.md), Неопределено.   
Если текущей позиции выборки соответствует удаленный объект, то возвращается значение типа [УдалениеОбъекта](УдалениеОбъекта.md). Если выборка находится в позиции "перед первым элементом" или "после последнего", то возвращается значение Неопределено.

Описание:

Производит считывание из базы данных элемента данных, соответствующего текущей позиции выборки и возвращает соответствующий объект.

Доступность:

Сервер, толстый клиент, внешнее соединение, мобильное приложение (сервер), мобильный автономный сервер.

Пример:

|  |
| --- |
| Выб = ПланыОбмена.ВыбратьИзменения(Получатель, НомерСообщения); Пока Выб.Следующий() Цикл     Объект = Выб.Получить();     ЗаписатьXML(Запись, Объект); КонецЦикла; |

Использование в версии:

Доступен, начиная с версии 8.0.

---
