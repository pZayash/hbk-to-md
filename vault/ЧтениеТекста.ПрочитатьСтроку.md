# ЧтениеТекста.ПрочитатьСтроку

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=Последовательный_доступ_к_тексту.md">Последовательный доступ к тексту</a> › [ЧтениеТекста](ЧтениеТекста.md)

<!-- signature:start -->
`ПрочитатьСтроку`(`<Разделитель>`: `Строка`) → `Строка, Неопределено`
<!-- signature:end -->

ЧтениеТекста (TextReader)

ПрочитатьСтроку (ReadLine)

Доступен, начиная с версии 8.0.

Синтаксис:

ПрочитатьСтроку(<Разделитель>)

Параметры:

<Разделитель> (необязательный)

Тип: Строка.   
Разделитель строк. Если не указан, то совпадает с указанным при открытии файла.  
Значение по умолчанию: [ПС](Символы.ПС.md).

Возвращаемое значение:

Тип: Строка, Неопределено.   
В случае, если читаемый файл завершен, то возвращается Неопределено.

Описание:

Считывает строку текста, ограниченную разделителем.

Доступность:

Тонкий клиент, мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер.

Примечание:

Строка нулевой длины не является признаком конца файла.

Пример:

|  |
| --- |
| Текст = Новый ЧтениеТекста("d:\win.txt", КодировкаТекста.ANSI); Стр = Текст.ПрочитатьСтроку(); Пока Стр <> Неопределено Цикл // строки читаются до символа перевода строки      Сообщить(Стр);     Стр = Текст.ПрочитатьСтроку(); КонецЦикла; |

Использование в версии:

Доступен, начиная с версии 8.0.

---
