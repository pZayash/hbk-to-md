# МенеджерБезопасногоХранилища.ПоместитьДанныеАсинх

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Общие объекты](Общие_объекты.md) › [Биометрия](Биометрия-2.md) › [МенеджерБезопасногоХранилища](МенеджерБезопасногоХранилища_(SecureStorageManager).md) › [Методы](МенеджерБезопасногоХранилища__Методы.md)

МенеджерБезопасногоХранилища (SecureStorageManager)

ПоместитьДанныеАсинх (PutDataAsync)

Доступен, начиная с версии 8.3.18.

Синтаксис:

ПоместитьДанныеАсинх(<Ключ>, <Данные>, <СпособЗащитыДоступа>, <СпособПроверки>)

Параметры:

<Ключ> (обязательный)

Тип: [Строка](lang__def_String.md).   
Ключ, по которому сохраняются данные.

<Данные> (обязательный)

Тип: Произвольный.   
Данные, которые требуется защитить в рамках приложения (логины или пароли). Используется внутренняя сериализация.   
Внимание! Значение данного параметра не может быть типа [Неопределено](lang__def_Undefined.md).

<СпособЗащитыДоступа> (обязательный)

Тип: [СпособЗащитыДоступаБезопасногоХранилища](СпособЗащитыДоступаБезопасногоХранилища_(SecureStorageAccessProtectionMethod).md).   
Используемый способ защиты доступа.

<СпособПроверки> (обязательный)

Тип: [СпособДополнительнойПроверкиПользователя](СпособДополнительнойПроверкиПользователя_(AdditionalUserVerificationMethod).md).   
Используемый способ проверки пользователя.  
Актуален только если используется способ защиты доступа [ТребуетсяДополнительнаяПроверкаПользователя](СпособЗащитыДоступаБезопасногоХранилища.ТребуетсяДополнительнаяПроверкаПользователя_(SecureStorageAccessProtectionMethod.AdditionalUserVerificationRequired).md), в противном случае игнорируется.  
Значение по умолчанию: [БиометрическаяИлиВводПароля](СпособДополнительнойПроверкиПользователя.БиометрическаяИлиВводПароля_(AdditionalUserVerificationMethod.BiometricsOrPassword).md).

Возвращаемое значение:

Тип: [Обещание](Обещание_(Promise).md).   
Результат выполнения обещания - [Булево](lang__Булево_(Boolean).md). Признак того, что проверка пользователя была отменена пользователем.

Описание:

Асинхронная функция. Сохраняет сериализуемые данные по ключу в защищенное хранилище, доступ к которому можно получить в соответствии с переданным параметром доступа. Если элемент с переданным значением ключа существует, то данные по этому ключу заменяются.   
Если размер помещаемых ключа и данных после сериализации превышает максимальную длину одной записи, то генерируется исключение с текстом "Размер переданных для сохранения данных превышает максимально возможную длину". Ограничения на размер записи:

- Android – нет;
- iOS – 16 МБ;
- Windows Phone – 16 КБ.

Если при помещении данных в хранилище не хватает памяти, то генерируется исключение с текстом "Память безопасного хранилища заполнена. Невозможно сохранить данные". Ограничения на размер хранилища:

- Android – нет;
- iOS – 80 МБ;
- Windows Phone – 320 КБ.

Если при помещении данных в хранилище достигнут предел количества, то генерируется исключение с текстом "Достигнут предел количества записей в безопасном хранилище. Невозможно сохранить данные". Ограничения на количество записей:

- Android – нет;
- iOS – нет;
- Windows Phone – 20.

  
Вызов метода не блокирует исполнение кода языка, все результаты возвращаются в переданный обработчик оповещения. Помещение данных производится в соответствии с переданным способом защиты доступа (может потребоваться проверка пользователя или разблокировка экрана).  
Если <СпособЗащитыДоступа> имеет значение [ТребуетсяДополнительнаяПроверкаПользователя](СпособЗащитыДоступаБезопасногоХранилища.ТребуетсяДополнительнаяПроверкаПользователя_(SecureStorageAccessProtectionMethod.AdditionalUserVerificationRequired).md), то в среде Windows Runtime потребуется проверка пользователя вводом пароля или отпечатком пальца. Операция проверки пользователя может быть отменена пользователем.

Доступность:

Мобильный клиент, мобильное приложение (клиент).

См. также:

[МенеджерБезопасногоХранилища](МенеджерБезопасногоХранилища_(SecureStorageManager).md), метод [НачатьПомещениеДанных](МенеджерБезопасногоХранилища.НачатьПомещениеДанных_(SecureStorageManager.BeginPutData).md)  

Использование в версии:

Доступен, начиная с версии 8.3.18.

---
