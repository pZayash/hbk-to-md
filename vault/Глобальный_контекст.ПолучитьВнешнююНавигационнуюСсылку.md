# Глобальный контекст.ПолучитьВнешнююНавигационнуюСсылку

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Глобальный_контекст.md">Глобальный контекст</a> › [Прочие процедуры и функции](Прочие_процедуры_и_функции.md)

<!-- signature:start -->
`ПолучитьВнешнююНавигационнуюСсылку`(`<БазоваяНавигационнаяСсылка>`: `Строка, Неопределено`, `<ОтносительнаяНавигационнаяСсылка>`: `Строка`, `<ДополнительныеПараметрыНавигационнойСсылки>`: [`Соответствие`](Соответствие.md)) → `Строка`
<!-- signature:end -->

Глобальный контекст (Global context)

ПолучитьВнешнююНавигационнуюСсылку (GetExternalURL)

Доступен, начиная с версии 8.3.18.

Синтаксис:

ПолучитьВнешнююНавигационнуюСсылку(<БазоваяНавигационнаяСсылка>, <ОтносительнаяНавигационнаяСсылка>, <ДополнительныеПараметрыНавигационнойСсылки>)

Параметры:

<БазоваяНавигационнаяСсылка> (необязательный)

Тип: Строка, Неопределено.   
Часть ссылки, по которой должен быть выполнен переход в приложение. Если Неопределено, базовая часть будет выбрана автоматически:

- В "1С:Предприятии" для персонального компьютера поведение остается как при вызове метода [ПолучитьНавигационнуюСсылкуИнформационнойБазы](Глобальный_контекст.ПолучитьНавигационнуюСсылкуИнформационнойБазы.md).
- В мобильной платформе будет выбран из списка перехватываемых ссылок, объявленных в конфигурации. Приоритет отдается в пользу глубинных ссылок. Если список пуст, возвращаемое значение метода – пустая строка.

<ОтносительнаяНавигационнаяСсылка> (необязательный)

Тип: Строка.   
Относительная часть ссылки, указывающая на объект внутри информационной базы.

<ДополнительныеПараметрыНавигационнойСсылки> (необязательный)

Тип: [Соответствие](Соответствие.md).   
Дополнительные параметры ссылки, которые будут указаны после знака вопроса, где:

- Ключ – тип Строка, имя параметра.
- Значение – тип Строка или [Массив](Массив.md) строк, значение параметра.

Возвращаемое значение:

Тип: Строка.   

Описание:

Возвращает внешнюю навигационную ссылку на объект информационной базы, по которой можно перейти из другого (внешнего) приложения.

Доступность:

Тонкий клиент, мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер.

Использование в версии:

Доступен, начиная с версии 8.3.18.

---
