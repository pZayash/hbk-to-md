# HTTPСоединение.ЗаписатьАсинх

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog578.md">Интернет</a> › <a href="obsidian://open?file=Работа_с_HTTP.md">Работа с HTTP</a> › [HTTPСоединение](HTTPСоединение.md)

<!-- signature:start -->
`ЗаписатьАсинх`(`<HTTPЗапрос>`: [`HTTPЗапрос`](HTTPЗапрос.md)) → [`Обещание`](Обещание.md)
<!-- signature:end -->

HTTPСоединение (HTTPConnection)

ЗаписатьАсинх (PutAsync)

Доступен, начиная с версии 8.3.21.

Синтаксис:

ЗаписатьАсинх(<HTTPЗапрос>)

Параметры:

<HTTPЗапрос> (обязательный)

Тип: [HTTPЗапрос](HTTPЗапрос.md).   
Запрос, передаваемый на сервер.

Возвращаемое значение:

Тип: [Обещание](Обещание.md).   
Результат выполнения обещания – объект типа [HTTPОтвет](HTTPОтвет.md).

Описание:

Асинхронно отправляет данные на HTTP-сервер, используя PUT-запрос.

Доступность:

Тонкий клиент, мобильный клиент, толстый клиент, мобильное приложение (клиент).

Примечание:

В веб-клиенте нужно учитывать, что запрос можно отправить только на тот же самый домен, на котором опубликован веб-клиент, или на домен, в котором нет CORS-ограничений на принятие запроса от веб-клиента. В противном случае будет вызвано исключение.  
  
Если веб-клиент опубликован по HTTPS, то запрос можно отправить, только используя защищенное соединение. В противном случае будет вызвано исключение.

Использование в версии:

Доступен, начиная с версии 8.3.21.

---
