# Глобальный контекст.ЗапроситьРазрешениеПользователя

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Глобальный_контекст.md">Глобальный контекст</a> › [Процедуры и функции работы с файлами](Процедуры_и_функции_работы_с_файлами.md)

<!-- signature:start -->
`ЗапроситьРазрешениеПользователя`(`<Вызовы>`: [`Массив`](Массив.md)) → `Булево`
<!-- signature:end -->

Глобальный контекст (Global context)

ЗапроситьРазрешениеПользователя (RequestUserPermission)

Не рекомендуется использовать, начиная с версии 8.3.15.

Рекомендуется использовать:

- [НачатьЗапросРазрешенияПользователя](Глобальный_контекст.НачатьЗапросРазрешенияПользователя.md)

Доступен, начиная с версии 8.2.

Синтаксис:

ЗапроситьРазрешениеПользователя(<Вызовы>)

Параметры:

<Вызовы> (обязательный)

Тип: [Массив](Массив.md).   
Содержит массив с описаниями вызовов, для которых получается разрешение.  
Каждое описание вызова это массив. Первым элементом этого массива является строка с именем метода, для которого получается разрешение. Это методы:

- [ЗапуститьПриложение](Глобальный_контекст.ЗапуститьПриложение.md);
- [УдалитьФайлы](Глобальный_контекст.УдалитьФайлы.md);
- [НайтиФайлы](Глобальный_контекст.НайтиФайлы.md);
- [КопироватьФайл](Глобальный_контекст.КопироватьФайл.md);
- [ПереместитьФайл](Глобальный_контекст.ПереместитьФайл.md);
- [СоздатьКаталог](Глобальный_контекст.СоздатьКаталог.md).

Следующие элементы массива - это параметры вызова в правильном порядке.

Возвращаемое значение:

Тип: Булево.   
В режиме запуска тонкий и толстый клиент, а также для клиента мобильного приложения всегда возвращает Истина.  
Если метод получает пустой запрос (за вычетом проигнорированных записей), то возвращает Истина.

Описание:

Получает у пользователя разрешение на несколько операций с файлами, которые потом будут выполнены без дополнительных вопросов.  
Полученное разрешение хранится до выполнения соответствующего вызова, либо до окончания работы текущего вызова встроенного языка.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, толстый клиент, мобильное приложение (клиент).

Примечание:

Если сочетание параметров предполагает вызов выбора файлов или поиск файлов по маске, то описание вызова будет проигнорировано при запросе разрешения.  
Для работы метода на веб-клиенте необходимо предварительно подключить расширение работы с файлами.

См. также:

[Глобальный контекст](Глобальный_контекст.md), метод [ЗапроситьРазрешениеПользователяАсинх](Глобальный_контекст.ЗапроситьРазрешениеПользователяАсинх.md)  
[Глобальный контекст](Глобальный_контекст.md), метод [НачатьЗапросРазрешенияПользователя](Глобальный_контекст.НачатьЗапросРазрешенияПользователя.md)  

Использование в версии:

Доступен, начиная с версии 8.2.

Не рекомендуется использовать, начиная с версии 8.3.15.

Описание изменено в версии 8.3.18.

---
