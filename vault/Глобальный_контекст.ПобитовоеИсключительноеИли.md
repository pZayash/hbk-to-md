# Глобальный контекст.ПобитовоеИсключительноеИли

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Глобальный_контекст.md">Глобальный контекст</a> › [Процедуры и функции работы с двоичными данными](Процедуры_и_функции_работы_с_двоичными_данными.md)

<!-- signature:start -->
`ПобитовоеИсключительноеИли`(`<Число1>`: `Число`, `<Число2>`: `Число`) → `Число`
<!-- signature:end -->

Глобальный контекст (Global context)

ПобитовоеИсключительноеИли (BitwiseXor)

Доступен, начиная с версии 8.3.11.

Синтаксис:

ПобитовоеИсключительноеИли(<Число1>, <Число2>)

Параметры:

<Число1> (обязательный)

Тип: Число.   
Первый операнд побитовой операции.   
Значение должно быть целым числом в диапазоне от 0 до 2^32-1. Если число не целое или не укладывается в данный диапазон - генерируется исключение.

<Число2> (обязательный)

Тип: Число.   
Второй операнд побитовой операции.   
Значение должно быть целым числом в диапазоне от 0 до 2^32-1. Если число не целое или не укладывается в данный диапазон - генерируется исключение.

Возвращаемое значение:

Тип: Число.   

Описание:

Возвращает результат логической побитовой операции ИСКЛЮЧИТЕЛЬНОЕ ИЛИ (XOR) для заданной пары чисел.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер, интеграция.

Использование в версии:

Доступен, начиная с версии 8.3.11.

---
