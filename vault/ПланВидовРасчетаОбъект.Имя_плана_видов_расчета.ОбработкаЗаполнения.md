# ПланВидовРасчетаОбъект.<Имя плана видов расчета>.ОбработкаЗаполнения

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Прикладные_объекты.md">Прикладные объекты</a> › <a href="obsidian://open?file=Планы_видов_расчета.md">Планы видов расчета</a> › [ПланВидовРасчетаОбъект.<Имя плана видов расчета>](ПланВидовРасчетаОбъект.Имя_плана_видов_расчета.md)

<!-- signature:start -->
`ОбработкаЗаполнения`(`<ДанныеЗаполнения>`, `<ТекстЗаполнения>`, `<СтандартнаяОбработка>`)
<!-- signature:end -->

ПланВидовРасчетаОбъект.<Имя плана видов расчета> (ChartOfCalculationTypesObject.<Chart of calculation types name>)

ОбработкаЗаполнения (Filling)

Доступен, начиная с версии 8.0.

Синтаксис:

ОбработкаЗаполнения(<ДанныеЗаполнения>, <ТекстЗаполнения>, <СтандартнаяОбработка>)

Параметры:

<ДанныеЗаполнения>

Тип: Произвольный.   
Значение, которое используется как основание для заполнения.  
Если обработчик вызван при выполнении метода Заполнить, параметр равен параметру <ДанныеЗаполнения> метода Заполнить.  
При обработке заполнения во время ввода на основании параметр имеет значение основания.  
При интерактивном вводе нового из формы списка параметр является структурой, элементы которой соответствуют тем параметрам отбора формы списка, у которых способ сравнения [Равно](ВидСравнения.Равно.md) или [ВСписке](ВидСравнения.ВСписке.md) с единственным элементом списка. Если таких элементов отбор списка не содержит, данный параметр равен Неопределено. Если параметр <СтандартнаяОбработка> по окончании работы обработчика равен Истина (установлен по умолчанию), система автоматически произведет заполнение одноименными элементами структуры.

<ТекстЗаполнения>

Тип: Строка, Неопределено.   
Если у объекта основное представление [ВВидеНаименования](ОсновноеПредставлениеВидаРасчета.ВВидеНаименования.md) и реквизит [Наименование](ПланВидовРасчетаОбъект.Имя_плана_видов_расчета.Наименование.md) находится в списке [ВводПоСтроке](ОбъектМетаданных_ПланВидовРасчета.ВводПоСтроке.md), то значение присваивается реквизиту [Наименование](ПланВидовРасчетаОбъект.Имя_плана_видов_расчета.Наименование.md).  
Если основное представление [ВВидеКода](ОсновноеПредставлениеВидаРасчета.ВВидеКода.md) и реквизит [Код](ПланВидовРасчетаОбъект.Имя_плана_видов_расчета.Код.md) находится в списке [ВводПоСтроке](ОбъектМетаданных_ПланВидовРасчета.ВводПоСтроке.md), то значение присваивается реквизиту [Код](ПланВидовРасчетаОбъект.Имя_плана_видов_расчета.Код.md).  
В противном случае присваивание не выполняется.

<СтандартнаяОбработка>

Тип: Булево.   
В данный параметр передается признак выполнения стандартной (системной) обработки события. Если в теле процедуры-обработчика установить данному параметру значение Ложь, стандартная обработка события производиться не будет. Отказ от стандартной обработки не отменяет действие.  
Значение по умолчанию: Истина.

Описание:

Вызывается при выполнении заполнения объекта из какого-либо значения. Вызывается при выполнении метода [Заполнить](ПланВидовРасчетаОбъект.Имя_плана_видов_расчета.Заполнить.md), при вводе на основании, а также при интерактивном вводе нового.  
В этой процедуре должно быть выполнено заполнение реквизитов объекта на основании переданного значения.

Доступность:

Сервер, толстый клиент, внешнее соединение.

Примечание:

При копировании (как интерактивном, так и средствами встроенного языка), данный обработчик не вызывается.

Замечание:

Безопасный режим на время исполнения данного обработчика будет отключен. Данное замечание не относится к обработчикам событий, расположенным в расширении конфигурации.

Использование в версии:

Доступен, начиная с версии 8.0.

---
