# Глобальный контекст.Вопрос

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Глобальный_контекст.md">Глобальный контекст</a> › [Процедуры и функции интерактивной работы](Процедуры_и_функции_интерактивной_работы.md)

<!-- signature:start -->
`Вопрос`(`<ТекстВопроса>`: `Строка` | [`ФорматированнаяСтрока`](ФорматированнаяСтрока.md), `<Кнопки>`: [`РежимДиалогаВопрос`](РежимДиалогаВопрос.md) | [`СписокЗначений`](СписокЗначений.md), `<Таймаут>`: `Число`, `<КнопкаПоУмолчанию>`: `Произвольный`, `<Заголовок>`: `Строка`, `<КнопкаТаймаута>`: `Произвольный`) → `Произвольный`
<!-- signature:end -->

Глобальный контекст (Global context)

Вопрос (DoQueryBox)

Доступен, начиная с версии 8.0.

Синтаксис:

Вопрос(<ТекстВопроса>, <Кнопки>, <Таймаут>, <КнопкаПоУмолчанию>, <Заголовок>, <КнопкаТаймаута>)

Параметры:

<ТекстВопроса> (обязательный)

Тип: Строка, [ФорматированнаяСтрока](ФорматированнаяСтрока.md).   
Текст задаваемого вопроса.

<Кнопки> (обязательный)

Тип: [РежимДиалогаВопрос](РежимДиалогаВопрос.md), [СписокЗначений](СписокЗначений.md).   
Задает состав и текст кнопок диалога, а также, связанные с кнопками значения.  
При использовании типа [СписокЗначений](СписокЗначений.md):

- [Значение](ЭлементСпискаЗначений.Значение.md) – содержит значение, связанное с кнопкой. Это значение является возвращаемым значением при выборе кнопки. В качестве значения может использоваться значение перечисления [КодВозвратаДиалога](КодВозвратаДиалога.md), а также другие значения;
- [Представление](ЭлементСпискаЗначений.Представление.md) – задает текст кнопки. Если представление не задано и в качестве значения используется значение перечисления [КодВозвратаДиалога](КодВозвратаДиалога.md), то используется стандартное представление;
- [Картинка](ЭлементСпискаЗначений.Картинка.md) – не используется (должна быть пустой);
- [Пометка](ЭлементСпискаЗначений.Пометка.md) – не используется (должна быть пустой).

При использовании типа [СписокЗначений](СписокЗначений.md) список не должен быть пустым.

<Таймаут> (необязательный)

Тип: Число.   
Интервал времени в секундах, в течение которого система будет ожидать ответа пользователя. По истечении интервала окно вопроса будет закрыто. Если параметр не указан, то время ожидания не ограничено.  
Если параметр имеет отрицательное значение, будет сгенерировано исключение.  
Значение по умолчанию: 0.

<КнопкаПоУмолчанию> (необязательный)

Тип: Произвольный.   
Определяет кнопку по умолчанию по типу кнопки или по связанному с ней значению.

<Заголовок> (необязательный)

Тип: Строка.   
Содержит заголовок окна вопроса.  
Если не установлен, то у диалога заголовок не отображается.

<КнопкаТаймаута> (необязательный)

Тип: Произвольный.   
Определяет кнопку (по типу кнопки или по связанному с ней значению), на которой отображается количество секунд, оставшихся до истечения таймаута.

Возвращаемое значение:

Тип: Произвольный.   
В зависимости от реакции пользователя возвращается значение системного перечисления или значение, связанное с нажатой кнопкой.  
В случае закрытия диалога по истечении времени ожидания возвращается значение [Таймаут](КодВозвратаДиалога.Таймаут.md).

Описание:

Выводит на экран окно вопроса.

Доступность:

Тонкий клиент, мобильный клиент, толстый клиент, мобильное приложение (клиент).

Примечание:

В веб-клиенте в случае, если клавиша Esc не используется (нет кнопки, связанной со значением [Отмена](КодВозвратаДиалога.Отмена.md)) и пользователь закрывает окно "крестиком", повторно открывается диалог с первоначальными параметрами.  
Если для конфигурации свойство [РежимИспользованияМодальности](ОбъектМетаданныхКонфигурация.РежимИспользованияМодальности.md) установлено в [НеИспользовать](РежимИспользованияМодальности.НеИспользовать.md), следует использовать метод [ПоказатьВопрос](Глобальный_контекст.ПоказатьВопрос.md).  
  
Ширина всех кнопок диалога одинакова и равна ширине самой широкой кнопки.  
Ширина текста в диалоге ограничена 40 символами.

Пример:

|  |
| --- |
| Режим = РежимДиалогаВопрос.ДаНет; Ответ = Вопрос(НСтр("ru = 'Продолжить выполнение операции?';"      + " en = 'Do you want to continue?'"), Режим, 0); Если Ответ = КодВозвратаДиалога.Нет Тогда     Возврат; КонецЕсли; |

См. также:

[Глобальный контекст](Глобальный_контекст.md), метод [ВопросАсинх](Глобальный_контекст.ВопросАсинх.md)  
[Глобальный контекст](Глобальный_контекст.md), метод [ПоказатьВопрос](Глобальный_контекст.ПоказатьВопрос.md)  

Использование в версии:

Доступен, начиная с версии 8.0.

Описание изменено в версии 8.3.18.

---
