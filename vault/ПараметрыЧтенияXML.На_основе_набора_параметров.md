# ПараметрыЧтенияXML.На основе набора параметров

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog565.md">XML</a> › [ПараметрыЧтенияXML](ПараметрыЧтенияXML.md)

<!-- signature:start -->
`ПараметрыЧтенияXML`(`<Версия>`: `Строка`, `<Язык>`: `Строка`, `<ПробельныеСимволы>`: [`ПробельныеСимволыXML`](ПробельныеСимволыXML.md), `<ТипПроверкиПравильности>`: [`ТипПроверкиXML`](ТипПроверкиXML.md), `<ИгнорироватьОбъявлениеXML>`: `Булево`, `<ИгнорироватьТипДокумента>`: `Булево`, `<ИгнорироватьИнструкцииОбработки>`: `Булево`, `<ИгнорироватьКомментарии>`: `Булево`, `<ИгнорироватьПробельныеСимволы>`: `Булево`, `<СекцииCDATAКакТекст>`: `Булево`, `<ИспользоватьИгнорируемыеПробельныеСимволы>`: `Булево`)
<!-- signature:end -->

ПараметрыЧтенияXML (XMLReaderSettings)

На основе набора параметров

Доступен, начиная с версии 8.1.

Синтаксис:

Новый ПараметрыЧтенияXML(<Версия>, <Язык>, <ПробельныеСимволы>, <ТипПроверкиПравильности>, <ИгнорироватьОбъявлениеXML>, <ИгнорироватьТипДокумента>, <ИгнорироватьИнструкцииОбработки>, <ИгнорироватьКомментарии>, <ИгнорироватьПробельныеСимволы>, <СекцииCDATAКакТекст>, <ИспользоватьИгнорируемыеПробельныеСимволы>)

Параметры:

<Версия> (необязательный)

Тип: Строка.   
Версия спецификации XML, используемой при чтении.  
Может принимать значения: "1.0" или "1.1".  
Значение по умолчанию: 1.0.

<Язык> (необязательный)

Тип: Строка.   
Язык документа XML - начальное значение атрибута xml:lang для документа XML.  
Значение данного свойства является начальным значением свойства [Язык](ПараметрыЧтенияXML.Язык.md) объекта [ПараметрыЧтенияXML](ПараметрыЧтенияXML.md).

<ПробельныеСимволы> (необязательный)

Тип: [ПробельныеСимволыXML](ПробельныеСимволыXML.md).   
Значение по умолчанию атрибута xml:space, используемое при чтении XML.  
Влияет на свойство [ПробельныеСимволы](ПараметрыЧтенияXML.ПробельныеСимволы.md) объекта [ПараметрыЧтенияXML](ПараметрыЧтенияXML.md).  
Значение по умолчанию: [ПоУмолчанию](ПробельныеСимволыXML.ПоУмолчанию.md).

<ТипПроверкиПравильности> (необязательный)

Тип: [ТипПроверкиXML](ТипПроверкиXML.md).   
Тип проверки данных XML на правильность при чтении.  
Значение по умолчанию: [НетПроверки](ТипПроверкиXML.НетПроверки.md).

<ИгнорироватьОбъявлениеXML> (необязательный)

Тип: Булево.   
Признак игнорирования объявления XML при чтении.  
Значение по умолчанию: Истина.

<ИгнорироватьТипДокумента> (необязательный)

Тип: Булево.   
Признак игнорирования определения типа документа (DTD).  
Значение по умолчанию: Истина.

<ИгнорироватьИнструкцииОбработки> (необязательный)

Тип: Булево.   
Признак игнорирования инструкций обработки.  
Значение по умолчанию: Ложь.

<ИгнорироватьКомментарии> (необязательный)

Тип: Булево.   
Признак игнорирования комментариев.  
Значение по умолчанию: Ложь.

<ИгнорироватьПробельныеСимволы> (необязательный)

Тип: Булево.   
Признак игнорирования пробельных символов при чтении.  
Значение по умолчанию: Истина.

<СекцииCDATAКакТекст> (необязательный)

Тип: Булево.   
Признак интерпретации секций CDATA как текста при чтении.  
Значение по умолчанию: Ложь.

<ИспользоватьИгнорируемыеПробельныеСимволы> (необязательный)

Тип: Булево.   
Определяет использование узлов [ПробельныеСимволы](ТипУзлаXML.ПробельныеСимволы.md) при чтении XML.  
Значение по умолчанию: Ложь.

Описание:

Создает экземпляр объекта [ПараметрыЧтенияXML](ПараметрыЧтенияXML.md).

Использование в версии:

Доступен, начиная с версии 8.1.

---
