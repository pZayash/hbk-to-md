# ПараметрыЗаписиXML.По описании параметров записи

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog565.md">XML</a> › [ПараметрыЗаписиXML](ПараметрыЗаписиXML.md)

<!-- signature:start -->
`ПараметрыЗаписиXML`(`<Кодировка>`: `Строка`, `<Версия>`: `Строка`, `<Отступ>`: `Булево`, `<ОтступАтрибутов>`: `Булево`, `<СимволыОтступа>`: `Строка`)
<!-- signature:end -->

ПараметрыЗаписиXML (XMLWriterSettings)

По описании параметров записи

Доступен, начиная с версии 8.1.

Синтаксис:

Новый ПараметрыЗаписиXML(<Кодировка>, <Версия>, <Отступ>, <ОтступАтрибутов>, <СимволыОтступа>)

Параметры:

<Кодировка> (необязательный)

Тип: Строка.   
Кодировка, которая будет использоваться при записи документа XML (актуальна для записи в файл).  
По умолчанию будет использована кодировка UTF-8.

<Версия> (необязательный)

Тип: Строка.   
Версия стандарта XML, соответствие которому будет проверяться при записи данных XML. Может принимать значения: "1.0" или "1.1".  
Значение по умолчанию: 1.0.

<Отступ> (необязательный)

Тип: Булево.   
Признак наличия отступов при записи структуры элементов документа XML.  
Значение по умолчанию: Истина.

<ОтступАтрибутов> (необязательный)

Тип: Булево.   
Признак наличия отступов при записи атрибутов элемента XML.  
Значение по умолчанию: Ложь.

<СимволыОтступа> (необязательный)

Тип: Строка.   
Последовательность символов, которая будет использована при записи отступов при записи данных XML. В последовательность могут входит только пробельные символы (Пробел, [ВК](Символы.ВК.md), [ПС](Символы.ПС.md), [Таб](Символы.Таб.md)).  
Значение по умолчанию: [Таб](Символы.Таб.md).

Описание:

Создает объект параметров записи XML.

Использование в версии:

Доступен, начиная с версии 8.1.

---
