# Соединение с агентом сервера.GetServiceSettingByParams

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Средства_интеграции_и_администрирования.md">Средства интеграции и администрирования</a> › <a href="obsidian://open?file=Менеджер_COM-соединений.md">Менеджер COM-соединений</a> › <a href="obsidian://open?file=Администрирование_кластера_серверов.md">Администрирование кластера серверов</a> › [Соединение с агентом сервера](Соединение_с_агентом_сервера.md)

<!-- signature:start -->
`GetServiceSettingByParams`(`<Кластер>`: [`Кластер серверов`](Кластер_серверов.md), `<Сервер>`: [`Рабочий сервер`](Рабочий_сервер.md), `<ИмяСервиса>`: `Строка`, `<ИмяИнформационнойБазы>`: `Строка`) → [`Настройка сервиса`](Настройка_сервиса.md)
<!-- signature:end -->

Соединение с агентом сервера (IServerAgentConnection)

GetServiceSettingByParams (GetServiceSettingByParams)

Доступен, начиная с версии 8.3.26.

Синтаксис:

GetServiceSettingByParams(<Кластер>, <Сервер>, <ИмяСервиса>, <ИмяИнформационнойБазы>)

Параметры:

<Кластер> (обязательный)

Тип: [Кластер серверов](Кластер_серверов.md).   
Кластер, для которого требуется получить настройку.

<Сервер> (обязательный)

Тип: [Рабочий сервер](Рабочий_сервер.md).   
Сервер, для которого требуется получить настройку.

<ИмяСервиса> (обязательный)

Тип: Строка.   
Имя сервиса кластера.

<ИмяИнформационнойБазы> (обязательный)

Тип: Строка.   
Имя информационной базы или пустая строка, если сервис кластера не разделен по информационным базам.

Возвращаемое значение:

Тип: [Настройка сервиса](Настройка_сервиса.md).   

Описание:

Возвращает настройку сервиса кластера по указанным параметрам.

Доступность:

Интеграция.

Использование в версии:

Доступен, начиная с версии 8.3.26.

---
