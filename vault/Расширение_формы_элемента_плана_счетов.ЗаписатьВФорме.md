# Расширение формы элемента плана счетов.ЗаписатьВФорме

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Прикладные_объекты.md">Прикладные объекты</a> › <a href="obsidian://open?file=Планы_счетов.md">Планы счетов</a> › [Расширение формы элемента плана счетов](Расширение_формы_элемента_плана_счетов.md)

<!-- signature:start -->
`ЗаписатьВФорме`() → `Булево`
<!-- signature:end -->

Расширение формы элемента плана счетов (Chart of accounts item form extension)

ЗаписатьВФорме (WriteInForm)

Доступен, начиная с версии 8.0.

Синтаксис:

ЗаписатьВФорме()

Возвращаемое значение:

Тип: Булево.   
Истина - объект записан; Ложь - в противном случае.

Описание:

Записывает объект, редактируемый в данный момент в форме.

Доступность:

Толстый клиент.

Пример:

|  |
| --- |
| Процедура ВидыСубконтоПередНачаломДобавления(Элемент, Отказ, Копирование)     Если ЭтоНовый() Тогда         Ответ = Вопрос("Счет еще не записан. Записать?", РежимДиалогаВопрос.ОКОтмена);         Если Ответ = КодВозвратаДиалога.ОК Тогда             Отказ = НЕ ЗаписатьВФорме();         Иначе             Отказ = Истина;         КонецЕсли;     КонецЕсли; КонецПроцедуры |

Использование в версии:

Доступен, начиная с версии 8.0.

---
