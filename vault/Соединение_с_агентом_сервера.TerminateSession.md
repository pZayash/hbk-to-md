# Соединение с агентом сервера.TerminateSession

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Средства_интеграции_и_администрирования.md">Средства интеграции и администрирования</a> › <a href="obsidian://open?file=Менеджер_COM-соединений.md">Менеджер COM-соединений</a> › <a href="obsidian://open?file=Администрирование_кластера_серверов.md">Администрирование кластера серверов</a> › [Соединение с агентом сервера](Соединение_с_агентом_сервера.md)

<!-- signature:start -->
`TerminateSession`(`<Кластер>`: [`Кластер серверов`](Кластер_серверов.md), `<Сеанс>`: [`Сеанс`](Сеанс.md), `<СообщениеОбОшибке>`: `Строка`)
<!-- signature:end -->

Соединение с агентом сервера (IServerAgentConnection)

TerminateSession (TerminateSession)

Доступен, начиная с версии 8.2.

Синтаксис:

TerminateSession(<Кластер>, <Сеанс>, <СообщениеОбОшибке>)

Параметры:

<Кластер> (обязательный)

Тип: [Кластер серверов](Кластер_серверов.md).   
Кластер, из которого должен быть удален сеанс. Должен содержать объект, полученный методом [GetClusters](Соединение_с_агентом_сервера.GetClusters.md).

<Сеанс> (обязательный)

Тип: [Сеанс](Сеанс.md).   
Сеанс, который должен быть удален. Должен содержать объект, полученный методом [GetSessions](Соединение_с_агентом_сервера.GetSessions.md) или [GetInfoBaseSessions](Соединение_с_агентом_сервера.GetInfoBaseSessions.md).

<СообщениеОбОшибке> (необязательный)

Тип: Строка.   
Сообщение для пользователя, которое содержит причину прерывания текущего серверного вызова.  
Значение по умолчанию: "Выполнение текущей операции прервано администратором".

Описание:

Удаляет сеанс. Попытка обращения к кластеру серверов от имени удаленного сеанса вызывает исключение.

Доступность:

Интеграция.

Использование в версии:

Доступен, начиная с версии 8.2.

---
