# Соединение с агентом сервера.Authenticate

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Средства интеграции и администрирования](Средства_интеграции_и_администрирования.md) › [Менеджер COM-соединений](Менеджер_COM-соединений.md) › [Администрирование кластера серверов](Администрирование_кластера_серверов.md) › [Соединение с агентом сервера](Соединение_с_агентом_сервера_(IServerAgentConnection).md) › [Методы](Соединение_с_агентом_сервера__Методы.md)

Соединение с агентом сервера (IServerAgentConnection)

Authenticate (Authenticate)

Доступен, начиная с версии 8.1.

Синтаксис:

Authenticate(<Кластер>, <Имя>, <Пароль>)

Параметры:

<Кластер> (обязательный)

Тип: [Кластер серверов](Кластер_серверов_(IClusterInfo).md).   
Кластер серверов. Может быть получен при помощи метода [GetServerWorkingProcesses](Соединение_с_агентом_сервера.GetServerWorkingProcesses_(IServerAgentConnection.GetServerWorkingProcesses).md).

<Имя> (обязательный)

Тип: [Строка](lang__def_String.md).   
Имя администратора кластера серверов.  
Пустая строка означает, что необходимо выполнить аутентификацию администратора по умолчанию (если в кластере не зарегистрирован ни один администратор) или аутентификацию пользователем операционной системы (если администратору кластера разрешена аутентификация пользователем операционной системы).

<Пароль> (обязательный)

Тип: [Строка](lang__def_String.md).   
Пароль пользователя для аутентификации пользователя паролем. Пользователь будет аутентифицирован, если совпадает его имя и пароль, и пользователю разрешена аутентификация паролем.

Описание:

Выполняет аутентификацию администратора кластера серверов.

Доступность:

Интеграция.

Примечание:

Все методы данного объекта, для которых необходима аутентификация администратора кластера серверов, могут быть выполнены только после успешного выполнения этого метода.

Использование в версии:

Доступен, начиная с версии 8.1.

---
