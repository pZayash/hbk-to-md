# ЧтениеXML.ПрочитатьАтрибут

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog565.md">XML</a> › [ЧтениеXML](ЧтениеXML.md)

<!-- signature:start -->
`ПрочитатьАтрибут`() → `Булево`
<!-- signature:end -->

ЧтениеXML (XMLReader)

ПрочитатьАтрибут (ReadAttribute)

Доступен, начиная с версии 8.1.

Синтаксис:

ПрочитатьАтрибут()

Возвращаемое значение:

Тип: Булево.   
Истина - очередной атрибут прочитан; Ложь - атрибутов больше нет.

Описание:

Считывает очередной атрибут элемента XML. После прочтения с помощью метода [ЧтениеXML.Прочитать](ЧтениеXML.Прочитать.md) очередного узла, имеющего тип [НачалоЭлемента](ТипУзлаXML.НачалоЭлемента.md), можно получить доступ к атрибутам данного элемента. Каждый вызов данного метода позволяет прочитать очередной атрибут. После прочтения очередного атрибута свойствам [ЧтениеXML.ТипУзла](ЧтениеXML.ТипУзла.md), [ЧтениеXML.Имя](ЧтениеXML.Имя.md), [ЧтениеXML.ЛокальноеИмя](ЧтениеXML.ЛокальноеИмя.md), [ЧтениеXML.Префикс](ЧтениеXML.Префикс.md), [ЧтениеXML.URIПространстваИмен](ЧтениеXML.URIПространстваИмен.md) и [ЧтениеXML.Значение](ЧтениеXML.Значение.md) присваиваются значения в соответствии с прочитанным атрибутом.

Доступность:

Тонкий клиент, мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер.

Пример:

|  |
| --- |
| Чтение.ОткрытьФайл("c:/docs/data.xml"); Пока Чтение.Прочитать() Цикл     Если Чтение.КоличествоАтрибутов() > 0 Тогда         Пока Чтение.ПрочитатьАтрибут() Цикл             // Обработка атрибута          КонецЦикла     КонецЕсли КонецЦикла |

Использование в версии:

Доступен, начиная с версии 8.1.

---
