# ЭлементБлокировкиДанных.ИспользоватьИзИсточникаДанных

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Общие объекты](Общие_объекты.md) › [Управление блокировкой данных](Управление_блокировкой_данных.md) › [ЭлементБлокировкиДанных](ЭлементБлокировкиДанных_(DataLockItem).md) › [Методы](ЭлементБлокировкиДанных__Методы.md)

ЭлементБлокировкиДанных (DataLockItem)

ИспользоватьИзИсточникаДанных (UseFromDataSource)

Доступен, начиная с версии 8.1.

Синтаксис:

ИспользоватьИзИсточникаДанных(<ПолеПространстваБлокировок>, <ПолеИсточника>)

Параметры:

<ПолеПространстваБлокировок> (обязательный)

Тип: [Строка](lang__def_String.md), [ПланВидовХарактеристикСсылка.](ПланВидовХарактеристикСсылка.Имя_плана_видов_характеристик_(ChartOfCharacteristicTypesRef.Chart_of_characteristic_types_name).md)<[Имя плана видов характеристик](ПланВидовХарактеристикСсылка.Имя_плана_видов_характеристик_(ChartOfCharacteristicTypesRef.Chart_of_characteristic_types_name).md)>.   
Имя поля пространства блокировки. Поддерживаются следующие имена полей:

- Для пространства РегистрСведений.<имя>.НаборЗаписей - Регистратор (только для регистра, подчиненного регистратору);
- Для пространства РегистрСведений.<имя> - Период (если есть), <имя измерения>, <имя разделителя>;
- Для пространства РегистрНакопления.<имя>.НаборЗаписей - Регистратор;
- Для пространства РегистрНакопления.<имя> - Период, <имя измерения>, <имя разделителя>;
- Для пространства РегистрБухгалтерии.<имя>.НаборЗаписей - Регистратор;
- Для пространства РегистрБухгалтерии.<имя> - Период, <имя измерения>, <вид движения> (значение системного перечисления [ВидДвиженияБухгалтерии](ВидДвиженияБухгалтерии_(AccountingRecordType).md)), Счет, Субконто<N>, <вид субконто>, <имя разделителя>;
- Для пространства РегистрРасчета.<имя>.НаборЗаписей - Регистратор;
- Для пространства РегистрРасчета.<имя> - ПериодРегистрации, ПериодДействия, <имя измерения>, <имя разделителя>;
- Для пространства Перерасчет.<имя>.НаборЗаписей - ОбъектПерерасчета;
- Для пространства Последовательность.<имя>.НаборЗаписей - Регистратор;
- Для пространства Последовательность.<имя>.Записи - Регистратор, Период, <Измерение>;
- Для пространства Последовательность.<имя> - <имя измерения>, <имя разделителя>;
- Для пространства Константа.<имя> имена полей не поддерживаются;
- Для пространства ВнешнийИсточникДанных.<имя источника>.Таблица.<имя таблицы> - <имя поля>.
- Тип [ПланВидовХарактеристикСсылка.](ПланВидовХарактеристикСсылка.Имя_плана_видов_характеристик_(ChartOfCharacteristicTypesRef.Chart_of_characteristic_types_name).md)<[Имя плана видов характеристик](ПланВидовХарактеристикСсылка.Имя_плана_видов_характеристик_(ChartOfCharacteristicTypesRef.Chart_of_characteristic_types_name).md)> - блокировка устанавливается на пространство РегистрБухгалтерии.<имя>.

Для пространств, указанных ниже поддерживается поле Ссылка, а также все поля, указанные в свойстве "Поля блокировки данных" данного объекта метаданных:

- Справочник.<имя>;
- Документ.<имя>
- ПланОбмена.<имя>;
- ПланСчетов.<имя>;
- БизнесПроцесс.<имя>;
- Задача.<имя>;
- ПланВидовРасчета.<имя>;
- ПланВидовХарактеристик.<имя>;
- ВнешнийИсточникДанных.<имя источника>.Таблица.<имя таблицы>.

<ПолеИсточника> (обязательный)

Тип: [Строка](lang__def_String.md).   
Имя поля источника данных.

Описание:

Устанавливает соответствие поля пространства блокировки полю источника данных.

Доступность:

Сервер, толстый клиент, внешнее соединение.

Примечание:

Не допускается задавать одно и то же поле пространства блокировок с помощью методов [УстановитьЗначение](ЭлементБлокировкиДанных.УстановитьЗначение_(DataLockItem.SetValue).md) и [ИспользоватьИзИсточникаДанных](ЭлементБлокировкиДанных.ИспользоватьИзИсточникаДанных_(DataLockItem.UseFromDataSource).md).

Использование в версии:

Доступен, начиная с версии 8.1.

---
