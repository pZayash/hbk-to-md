# МенеджерСистемыВзаимодействия.ПодключитьОбработчикПослеОтправкиСообщения

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Общие объекты](Общие_объекты.md) › [Система взаимодействия](Система_взаимодействия-2.md) › [МенеджерСистемыВзаимодействия](МенеджерСистемыВзаимодействия_(CollaborationSystemManager).md) › [Методы](МенеджерСистемыВзаимодействия__Методы.md)

МенеджерСистемыВзаимодействия (CollaborationSystemManager)

ПодключитьОбработчикПослеОтправкиСообщения (AttachAfterMessageSendHandler)

Доступен, начиная с версии 8.3.11.

Синтаксис:

ПодключитьОбработчикПослеОтправкиСообщения(<Обработчик>)

Параметры:

<Обработчик> (обязательный)

Тип: [ОписаниеОповещения](ОписаниеОповещения_(CallbackDescription).md).   
Содержит описание процедуры, которая будет вызвана после интерактивной отправки сообщения в обсуждение со следующими параметрами:

- <Сообщение> - тип [СообщениеСистемыВзаимодействия](СообщениеСистемыВзаимодействия_(CollaborationSystemMessage).md). Сообщение. В обработчик передается копия объекта. В качестве даты сообщения устанавливается текущая дата компьютера. В качестве автора используется текущий пользователь системы взаимодействия;
- <Обсуждение> - тип [ОбсуждениеСистемыВзаимодействия](ОбсуждениеСистемыВзаимодействия_(CollaborationSystemConversation).md). Передается копия значения;
- <ДополнительныеПараметры> - значение, которое было указано при создании объекта [ОписаниеОповещения](ОписаниеОповещения_(CallbackDescription).md).

Описание:

Подключает процедуру-обработчик, вызываемую после интерактивной отправки сообщения в обсуждение.  
Допускается наличие нескольких обработчиков. В этом случае после отправки сообщения будут вызваны все обработчики. Порядок вызова обработчиков не определен.  
Вызов метода с тем же обработчиком замещает обработчик.  
Сравнение обработчиков выполняется по равенству значений свойств:

- ИмяПроцедуры,
- ИмяПроцедурыОбработкиОшибки,
- Модуль,
- МодульОбработкиОшибки.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, толстый клиент.

Использование в версии:

Доступен, начиная с версии 8.3.11.

---
