# Рабочий сервер.SafeCallMemoryLimit

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Средства интеграции и администрирования](Средства_интеграции_и_администрирования.md) › [Менеджер COM-соединений](Менеджер_COM-соединений.md) › [Администрирование кластера серверов](Администрирование_кластера_серверов.md) › [Рабочий сервер](Рабочий_сервер_(IWorkingServerInfo).md) › [Свойства](Рабочий_сервер__Свойства.md)

Рабочий сервер (IWorkingServerInfo)

SafeCallMemoryLimit (SafeCallMemoryLimit)

Доступен, начиная с версии 8.3.1.

Использование:

Чтение и запись.

Описание:

Тип: [Число](lang__def_Number.md).   
Определяет размер безопасного расхода памяти за один вызов.  
Объем памяти в байтах, использование которого в процессе вызова сервера считается безопасным и не приводит к прекращению вызова даже если достигнут максимальный объем памяти рабочих процессов сервера.  
Допустимы значения от -1 до 9223372036854775807.  
Если значение 0, то безопасный расход памяти определяется автоматически, как 5% максимального объема памяти рабочих процессов на данном рабочем сервере.  
Если значение -1, любой вызов считается опасным, если достигнут максимальный объем памяти рабочих процессов.

Доступность:

Интеграция.

См. также:

[Рабочий сервер](Рабочий_сервер_(IWorkingServerInfo).md), свойство [SafeWorkingProcessesMemoryLimit](Рабочий_сервер.SafeWorkingProcessesMemoryLimit_(IWorkingServerInfo.SafeWorkingProcessesMemoryLimit).md)  

Использование в версии:

Доступен, начиная с версии 8.3.1.

---
