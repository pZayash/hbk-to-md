# Глобальный контекст.ЗначениеИзСтрокиВнутр

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Глобальный контекст](Глобальный_контекст.md) › [Методы](Методы.md) › [Процедуры и функции сохранения значений](Процедуры_и_функции_сохранения_значений.md)

Глобальный контекст (Global context)

ЗначениеИзСтрокиВнутр (ValueFromStringInternal)

Доступен, начиная с версии 8.0.

Синтаксис:

ЗначениеИзСтрокиВнутр(<Строка>)

Параметры:

<Строка> (обязательный)

Тип: [Строка](lang__def_String.md).   
Системное представление значения в строковом виде.

Возвращаемое значение:

Тип: Произвольный.   
Значение, полученное из строкового системного представления.

Описание:

Преобразует значение из строкового системного представления во внутреннее.

Доступность:

Сервер, толстый клиент, внешнее соединение.

Примечание:

Строковое представление данных имеет специальный системный формат, использующий идентификацию данных внутри одной информационной базы.  
Используется для сохранения функциональной совместимости с 1С:Предприятием 7.7. Использовать для других целей не рекомендуется.

Пример:

|  |
| --- |
| Файл = Новый ЧтениеXML; Файл.ОткрытьФайл(ИмяФайла); Пока Файл.Прочитать() Цикл     Если (Файл.ТипУзла = ТипУзлаXML.НачалоЭлемента) И          (Файл.Имя = "СоответствиеСчетов") Тогда         // ...      ИначеЕсли (Файл.ТипУзла = ТипУзлаXML.Текст) Тогда         Если (ИмяЭлемента = "СчетХозрасчетный") Тогда             // ...          ИначеЕсли ИмяЭлемента = "ЗначениеТип" Тогда             ТипЗнач = ЗначениеИзСтрокиВнутр(Файл.Значение);         КонецЕсли;     // ...      Иначе         ИмяЭлемента = Файл.Имя;     КонецЕсли; КонецЦикла; |

См. также:

[Глобальный контекст](Глобальный_контекст.md), метод [ЗначениеВСтрокуВнутр](Глобальный_контекст.ЗначениеВСтрокуВнутр_(Global_context.ValueToStringInternal).md)  

Использование в версии:

Доступен, начиная с версии 8.0.

---
