# Расширение формы клиентского приложения для бизнес-процесса.ПослеЗаписи

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Интерфейс](Интерфейс_(управляемый).md) › [Форма клиентского приложения](Форма_клиентского_приложения.md) › [Расширение формы клиентского приложения для бизнес-процесса](Расширение_формы_клиентского_приложения_для_бизнес-процесса_(Client_application_form_extension_for_business_processes).md) › [События](Расширение_формы_клиентского_приложения_для_бизнес-процесса__События.md)

Расширение формы клиентского приложения для бизнес-процесса (Client application form extension for business processes)

ПослеЗаписи (AfterWrite)

Доступен, начиная с версии 8.2.

Синтаксис:

ПослеЗаписи(<ПараметрыЗаписи>)

Параметры:

<ПараметрыЗаписи>

Структура, содержащая параметры записи. Для формы бизнес-процесса существует один предопределенный параметр Старт. При отработке команд "Старт", "Стартовать и закрыть" он устанавливается в [Истина](lang__def_BooleanTrue.md).

Описание:

Возникает на клиенте после записи объекта и после окончания транзакции записи, то есть к моменту вызова этой процедуры запись объекта полностью завершена.

Доступность:

Тонкий клиент, веб-клиент, сервер, толстый клиент.

Примечание:

В обработчике данного события нельзя использовать серверные методы формы с директивой компиляции &НаСервер, а также изменять свойства, которые могут привести к серверному вызову.

Использование в версии:

Доступен, начиная с версии 8.2.

Описание изменено в версии 8.3.15.

---
