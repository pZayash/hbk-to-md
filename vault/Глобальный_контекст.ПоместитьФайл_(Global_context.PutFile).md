# Глобальный контекст.ПоместитьФайл

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Глобальный контекст](Глобальный_контекст.md) › [Методы](Методы.md) › [Процедуры и функции работы с файлами](Процедуры_и_функции_работы_с_файлами.md)

Глобальный контекст (Global context)

ПоместитьФайл (PutFile)

Не рекомендуется использовать, начиная с версии 8.3.13.

Рекомендуется использовать:

- [НачатьПомещениеФайлаНаСервер](Глобальный_контекст.НачатьПомещениеФайлаНаСервер_(Global_context.BeginPutFileToServer).md)

Доступен, начиная с версии 8.2.

Синтаксис:

ПоместитьФайл(<Адрес>, <НачальноеИмяФайла>, <ВыбранноеИмяФайла>, <Интерактивно>, <УникальныйИдентификаторФормы>)

Параметры:

<Адрес> (необязательный)

Тип: [Строка](lang__def_String.md).   
Адрес во временном хранилище, по которому должны быть помещены данные. Если значение параметра не задано или является пустой строкой, то будет создан новый файл. Расположение нового файла возвращается в данном параметре.

<НачальноеИмяФайла> (необязательный)

Тип: [Строка](lang__def_String.md).   
Путь к файлу, данные из которого будут помещены во временное хранилище.  
Если параметр <Интерактивно> = [Истина](lang__def_BooleanTrue.md), то отображается диалог выбора файла и выбранный в диалоге путь возвращается в параметре <ВыбранноеИмяФайла>.

<ВыбранноеИмяФайла> (необязательный)

Тип: [Строка](lang__def_String.md).   
В параметре возвращается путь к файлу, указанный в диалоге выбора файла. Для неинтерактивного режима выбранное имя файла соответствует начальному имени файла.  
В веб-клиенте значение параметра зависит от типа браузера.  
Для FireFox 3 в параметре возвращается только имя файла без пути.  
Для Internet Explorer возвращаемое значение зависит от настроек безопасности.

<Интерактивно> (необязательный)

Тип: [Булево](lang__Булево_(Boolean).md).   
Указывает использование интерактивного режима, при котором отображается диалог выбора файла. Этот файл будет помещен по указанному в параметре <Адрес> адресу во временное хранилище или по новому адресу, если второй параметр метода опущен.   
Если значение параметра [Истина](lang__def_BooleanTrue.md), то метод отобразит стандартное диалоговое окно выбора файла, в котором можно выбрать файл для помещения в хранилище. В этом случае выбранный путь возвращается в параметре <Выбранное имя файла>.  
В веб-клиенте параметр должен быть равен [Истина](lang__def_BooleanTrue.md). В противном случае будет сгенерировано сообщение об ошибке.  
Значение по умолчанию: [Истина](lang__def_BooleanTrue.md).

<УникальныйИдентификаторФормы> (необязательный)

Тип: [УникальныйИдентификатор](УникальныйИдентификатор_(UUID).md).   
Уникальный идентификатор формы, который можно получить с помощью свойства [УникальныйИдентификатор](ФормаКлиентскогоПриложения.УникальныйИдентификатор_(ClientApplicationForm.UUID).md) управляемой формы.   
Файл помещается во временное хранилище и автоматически удаляется после удаления объекта формы.  
Если параметр не указан, помещенное значение будет удалено, после очередного запроса сервера из общего модуля, при контекстном и неконтекстном серверном вызове из формы, при серверном вызове из модуля команды или при получении формы.

Возвращаемое значение:

Тип: [Булево](lang__Булево_(Boolean).md).   
[Ложь](lang__def_BooleanFalse.md) - в параметре <Интерактивно> установлен интерактивный режим ([Истина](lang__def_BooleanTrue.md)) и пользователь отказался от выполнения операции в диалоге выбора файла.

Описание:

Помещает файл из локальной файловой системы во временное хранилище.

Доступность:

Тонкий клиент, мобильный клиент, толстый клиент, мобильное приложение (клиент).  
Вызов метода выполняет обращение к серверу.

Примечание:

Если для конфигурации свойство [РежимИспользованияМодальности](ОбъектМетаданныхКонфигурация.РежимИспользованияМодальности_(ConfigurationMetadataObject.ModalityUseMode).md) установлено в [НеИспользовать](РежимИспользованияМодальности.НеИспользовать_(ModalityUseMode.DontUse).md), следует использовать метод [НачатьПомещениеФайла](Глобальный_контекст.НачатьПомещениеФайла_(Global_context.BeginPutFile).md).

См. также:

[Глобальный контекст](Глобальный_контекст.md), метод [НачатьПомещениеФайла](Глобальный_контекст.НачатьПомещениеФайла_(Global_context.BeginPutFile).md)  
[Глобальный контекст](Глобальный_контекст.md), метод [ПоместитьФайлНаСерверАсинх](Глобальный_контекст.ПоместитьФайлНаСерверАсинх_(Global_context.PutFileToServerAsync).md)  
[Глобальный контекст](Глобальный_контекст.md), метод [ПоместитьФайлыНаСерверАсинх](Глобальный_контекст.ПоместитьФайлыНаСерверАсинх_(Global_context.PutFilesToServerAsync).md)  
[Глобальный контекст](Глобальный_контекст.md), метод [ПоместитьФайлы](Глобальный_контекст.ПоместитьФайлы_(Global_context.PutFiles).md)  
[Глобальный контекст](Глобальный_контекст.md), метод [НачатьПомещениеФайлов](Глобальный_контекст.НачатьПомещениеФайлов_(Global_context.BeginPuttingFiles).md)  

Использование в версии:

Доступен, начиная с версии 8.2.

Не рекомендуется использовать, начиная с версии 8.3.13.

Описание изменено в версии 8.3.18.

---
