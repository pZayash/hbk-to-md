# Соединение с агентом сервера.InterruptCurrentServerCall

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Средства_интеграции_и_администрирования.md">Средства интеграции и администрирования</a> › <a href="obsidian://open?file=Менеджер_COM-соединений.md">Менеджер COM-соединений</a> › <a href="obsidian://open?file=Администрирование_кластера_серверов.md">Администрирование кластера серверов</a> › [Соединение с агентом сервера](Соединение_с_агентом_сервера.md)

<!-- signature:start -->
`InterruptCurrentServerCall`(`<Кластер>`: [`Кластер серверов`](Кластер_серверов.md), `<Сеанс>`: [`Сеанс`](Сеанс.md), `<СообщениеОбОшибке>`: `Строка`)
<!-- signature:end -->

Соединение с агентом сервера (IServerAgentConnection)

InterruptCurrentServerCall (InterruptCurrentServerCall)

Доступен, начиная с версии 8.3.13.

Синтаксис:

InterruptCurrentServerCall(<Кластер>, <Сеанс>, <СообщениеОбОшибке>)

Параметры:

<Кластер> (обязательный)

Тип: [Кластер серверов](Кластер_серверов.md).   
Кластер, из которого будет удален сеанс. Должен содержать объект, полученный методом [GetClusters](Соединение_с_агентом_сервера.GetClusters.md).

<Сеанс> (обязательный)

Тип: [Сеанс](Сеанс.md).   
Сеанс, который будет удален. Должен содержать объект, полученный методом [GetSessions](Соединение_с_агентом_сервера.GetSessions.md) или [GetInfoBaseSessions](Соединение_с_агентом_сервера.GetInfoBaseSessions.md).

<СообщениеОбОшибке> (необязательный)

Тип: Строка.   
Сообщение для пользователя, которое содержит причину прерывания текущего серверного вызова.  
Значение по умолчанию: "Выполнение текущей операции прервано администратором".

Описание:

Прерывает выполнение текущего серверного вызова.

Доступность:

Интеграция.

Использование в версии:

Доступен, начиная с версии 8.3.13.

---
