# СообщениеПользователю

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › [Общие объекты](Общие_объекты.md)

Доступен, начиная с версии 8.2.

Свойства:

[ИдентификаторНазначения (TargetID)](СообщениеПользователю.ИдентификаторНазначения.md)  
[КлючДанных (DataKey)](СообщениеПользователю.КлючДанных.md)  
[Поле (Field)](СообщениеПользователю.Поле.md)  
[ПутьКДанным (DataPath)](СообщениеПользователю.ПутьКДанным.md)  
[Текст (Text)](СообщениеПользователю.Текст.md)  

Методы:

[Сообщить (Message)](СообщениеПользователю.Сообщить.md)  
[УстановитьДанные (SetData)](СообщениеПользователю.УстановитьДанные.md)  

Конструкторы:

[По умолчанию](СообщениеПользователю.По_умолчанию.md)  

Описание:

Предназначен для хранения параметров сообщения, которые необходимо вывести пользователю.  
Если сообщение еще не было показано пользователю (такое может быть при работе на стороне сервера, в фоновом задании, внешнем соединении или Web-сервисах), можно получить накопленные сообщения методом [ПолучитьСообщенияПользователю](Глобальный_контекст.ПолучитьСообщенияПользователю.md).  
  
В форме клиентского приложения, в случае выполнения просмотра сообщения, выполняется следующий механизм обработки:   
Если у сообщения свойство [КлючДанных](СообщениеПользователю.КлючДанных.md) заполнено и не совпадает с ключевым значением основного реквизита текущей формы (ссылкой объекта или ключом записи регистра сведений), то:

- открывается новая форма объекта, соответствующая значению свойства [КлючДанных](СообщениеПользователю.КлючДанных.md);
- если у сообщения свойство [ПутьКДанным](СообщениеПользователю.ПутьКДанным.md) не заполнено, то в него проставляется имя основного реквизита открытой формы объекта;
- вызывается показ сообщения в только что открытой форме;
- выполняется перенос всех соответствующих сообщений из окна первой формы в новую форму (с таким же значением свойств [КлючДанных](СообщениеПользователю.КлючДанных.md), как у выбранного сообщения). При этом перенесенным сообщениям аналогичным образом проставляется свойство [ПутьКДанным](СообщениеПользователю.ПутьКДанным.md), если оно не установлено.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер.

Пример:

|  |
| --- |
| Процедура ДействиеСОбъектом(ОбъектДанных)     ...     Сообщение = Новый СообщениеПользователю();     Сообщение.Текст = НСтр("ru = 'В строке 11 табличной части Номенклатура не хватает "      + "; en = 'In line 11 of tabular section Nomenclature is missing' ")      + НедостающееКоличество + " " + ЕдиницаИзмеренияНоменклатуры;     Сообщение.Поле = "Номенклатура[10].Количество";     Сообщение.УстановитьДанные(ОбъектДанных);     Сообщение.Сообщить();  КонецПроцедуры; |

См. также:

[Глобальный контекст](Глобальный_контекст.md), метод [УстановитьСоответствиеОбъектаИРеквизитаФормы](Глобальный_контекст.УстановитьСоответствиеОбъектаИРеквизитаФормы.md)  
[Глобальный контекст](Глобальный_контекст.md), метод [ПолучитьСоответствиеОбъектаИРеквизитаФормы](Глобальный_контекст.ПолучитьСоответствиеОбъектаИРеквизитаФормы.md)  
[Глобальный контекст](Глобальный_контекст.md), метод [ПолучитьСообщенияПользователю](Глобальный_контекст.ПолучитьСообщенияПользователю.md)  

Использование в версии:

Доступен, начиная с версии 8.2.

---
