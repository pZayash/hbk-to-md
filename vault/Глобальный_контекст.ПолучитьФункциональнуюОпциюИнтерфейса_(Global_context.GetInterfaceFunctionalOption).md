# Глобальный контекст.ПолучитьФункциональнуюОпциюИнтерфейса

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Глобальный контекст](Глобальный_контекст.md) › [Методы](Методы.md) › [Процедуры и функции работы с функциональными опциями](Процедуры_и_функции_работы_с_функциональными_опциями.md)

Глобальный контекст (Global context)

ПолучитьФункциональнуюОпциюИнтерфейса (GetInterfaceFunctionalOption)

Доступен, начиная с версии 8.2.

Синтаксис:

ПолучитьФункциональнуюОпциюИнтерфейса(<Имя>)

Параметры:

<Имя> (обязательный)

Тип: [Строка](lang__def_String.md).   
Имя функциональной опции.

Возвращаемое значение:

Тип: Произвольный.   

Описание:

Получает значение функциональной опции применительно к командному интерфейсу.   
Для получения значения опции используются параметры, установленные с помощью метода [УстановитьПараметрыФункциональныхОпцийИнтерфейса](Глобальный_контекст.УстановитьПараметрыФункциональныхОпцийИнтерфейса_(Global_context.SetInterfaceFunctionalOptionParameters).md).

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, толстый клиент, мобильное приложение (клиент).  
Вызов метода выполняет обращение к серверу.

Примечание:

Результат выполнения не кэшируется, вызов сервера осуществляется при каждом вызове метода.   
Если в качестве параметра функциональной опции интерфейса была передана ссылка на группу, а реквизит, в котором хранится значение функциональной опции, не используется для группы, то при получении значения функциональной опции будет возращено Null.

Использование в версии:

Доступен, начиная с версии 8.2.

---
