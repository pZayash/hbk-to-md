# Глобальный контекст.НачатьПолучениеФайловССервера

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Глобальный_контекст.md">Глобальный контекст</a> › [Процедуры и функции работы с файлами](Процедуры_и_функции_работы_с_файлами.md)

<!-- signature:start -->
`НачатьПолучениеФайловССервера`(`<ПолучаемыеФайлы>`: [`Массив`](Массив.md), `<ПараметрыДиалогаПолученияФайлов>`: [`ПараметрыДиалогаПолученияФайлов`](ПараметрыДиалогаПолученияФайлов.md), `<ПараметрыПолученияАрхиваФайлов>`: [`ПараметрыПолученияАрхиваФайлов`](ПараметрыПолученияАрхиваФайлов.md))

`НачатьПолучениеФайловССервера`(`<ОписаниеОповещенияОЗавершении>`: [`ОписаниеОповещения`](ОписаниеОповещения.md), `<ПолучаемыеФайлы>`: [`Массив`](Массив.md), `<БазовыйКаталог>`: `Строка`, `<ПараметрыПолученияАрхиваФайлов>`: [`ПараметрыПолученияАрхиваФайлов`](ПараметрыПолученияАрхиваФайлов.md))
<!-- signature:end -->

Глобальный контекст (Global context)

НачатьПолучениеФайловССервера (BeginGetFilesFromServer)

Доступен, начиная с версии 8.3.15.

Вариант синтаксиса: С диалогом

Синтаксис:

НачатьПолучениеФайловССервера(<ПолучаемыеФайлы>, <ПараметрыДиалогаПолученияФайлов>, <ПараметрыПолученияАрхиваФайлов>)

Параметры:

<ПолучаемыеФайлы> (обязательный)

Тип: [Массив](Массив.md).   
Массив объектов типа [ОписаниеПередаваемогоФайла](ОписаниеПередаваемогоФайла.md).  
Каждый объект описывает получаемый файл:

- [Хранение](ОписаниеПередаваемогоФайла.Хранение.md) - расположение данных во временном хранилище или в информационной базе. Значение не должно быть равно пустой строке. В противном случае будет сгенерировано сообщение об ошибке.
- [Имя](ОписаниеПередаваемогоФайла.Имя.md) - либо полный путь к файлу, либо относительный путь к файлу. Относительный путь будет объединен со значением параметра <БазовыйКаталог>.

<ПараметрыДиалогаПолученияФайлов> (необязательный)

Тип: [ПараметрыДиалогаПолученияФайлов](ПараметрыДиалогаПолученияФайлов.md).   
Структура, содержащая параметры диалога, который будет показан пользователю.

<ПараметрыПолученияАрхиваФайлов> (необязательный)

Тип: [ПараметрыПолученияАрхиваФайлов](ПараметрыПолученияАрхиваФайлов.md).   
Параметры получения файлов в виде архива. Если параметр не задан, то получаемые файлы не будут упакованы в архив.

Описание варианта метода:

Начинает получение файлов, отображая диалог, и сохраняет их в локальную файловую систему пользователя.   
В веб-клиенте расширение работы с файлами не требуется.

Вариант синтаксиса: Без диалога

Синтаксис:

НачатьПолучениеФайловССервера(<ОписаниеОповещенияОЗавершении>, <ПолучаемыеФайлы>, <БазовыйКаталог>, <ПараметрыПолученияАрхиваФайлов>)

Параметры:

<ОписаниеОповещенияОЗавершении> (необязательный)

Тип: [ОписаниеОповещения](ОписаниеОповещения.md).   
Содержит описание процедуры, которая будет вызвана после завершения со следующими параметрами:

- <ПолученныеФайлы> – массив объектов типа [ОписаниеПереданногоФайла](ОписаниеПереданногоФайла.md).
- <ДополнительныеПараметры> – значение, которое было указано при создании объекта [ОписаниеОповещения](ОписаниеОповещения.md).

<ПолучаемыеФайлы> (обязательный)

Тип: [Массив](Массив.md).   
Массив объектов типа [ОписаниеПередаваемогоФайла](ОписаниеПередаваемогоФайла.md).  
Каждый объект описывает получаемый файл:

- [Хранение](ОписаниеПередаваемогоФайла.Хранение.md) - расположение данных во временном хранилище или в информационной базе. Значение не должно быть равно пустой строке. В противном случае будет сгенерировано сообщение об ошибке.
- [Имя](ОписаниеПередаваемогоФайла.Имя.md) - либо полный путь к файлу, либо относительный путь к файлу. Относительный путь будет объединен со значением параметра <БазовыйКаталог>.

<БазовыйКаталог> (необязательный)

Тип: Строка.   
Устанавливает базовый каталог, относительно которого определяется конечное расположение каждого из файлов.  
Если среди описаний получаемых файлов есть относительные пути или установлен режим получения архива [ПолучатьАрхивВсегда](РежимПолученияАрхиваФайлов.ПолучатьАрхивВсегда.md), то параметр <БазовыйКаталог> должен быть задан обязательно.

<ПараметрыПолученияАрхиваФайлов> (необязательный)

Тип: [ПараметрыПолученияАрхиваФайлов](ПараметрыПолученияАрхиваФайлов.md).   
Параметры получения файлов в виде архива. Если параметр не задан, то получаемые файлы не будут упакованы в архив.

Описание варианта метода:

Начинает получение файлов и сохраняет их в локальную файловую систему пользователя.  
В веб-клиенте необходимо расширение работы с файлами.

Описание:

Начинает получение файлов.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, толстый клиент, мобильное приложение (клиент).

См. также:

[Глобальный контекст](Глобальный_контекст.md), метод [ПолучитьФайл](Глобальный_контекст.ПолучитьФайл.md)  
[Глобальный контекст](Глобальный_контекст.md), метод [ПолучитьФайлы](Глобальный_контекст.ПолучитьФайлы.md)  
[Глобальный контекст](Глобальный_контекст.md), метод [ПолучитьФайлыССервераАсинх](Глобальный_контекст.ПолучитьФайлыССервераАсинх.md)  

Использование в версии:

Доступен, начиная с версии 8.3.15.

Описание изменено в версии 8.3.18.

---
