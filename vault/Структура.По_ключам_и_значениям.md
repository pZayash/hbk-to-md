# Структура.По ключам и значениям

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Универсальные коллекции значений](Универсальные_коллекции_значений.md) › [Структура](Структура_(Structure).md) › [Конструкторы](Структура__Конструкторы.md)

Структура (Structure)

По ключам и значениям

Доступен, начиная с версии 8.0.

Синтаксис:

Новый Структура(<Ключи>, <Значения>)

Параметры:

<Ключи> (необязательный)

Тип: Произвольный.   
Если тип параметра [Строка](lang__def_String.md), то содержит список имен свойств, указанных через запятую.  
В иных случаях значение параметра преобразуется к строке и используется как список имен создаваемых в структуре свойств, разделенных запятыми.   
В случае, если для переданного типа параметра не предусмотрено получение строкового представления, будет вызвано исключение.

<Значения> (необязательный)

Тип: Произвольный.   
Значения элементов структуры.

Описание:

Создает структуру с ключами, значения которых передаются в параметре конструктора. Если в первом параметре заданы ключи элементов структуры, то в следующих параметрах могут быть указаны значения этих элементов в том порядке, в котором они расположены в строке в первом параметре.

Пример:

|  |
| --- |
| Отбор = Новый Структура("Дата, Клиент"); |

Использование в версии:

Доступен, начиная с версии 8.0.

---
