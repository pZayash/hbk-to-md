# Соединение с агентом сервера.GetAssignmentRules

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Средства интеграции и администрирования](Средства_интеграции_и_администрирования.md) › [Менеджер COM-соединений](Менеджер_COM-соединений.md) › [Администрирование кластера серверов](Администрирование_кластера_серверов.md) › [Соединение с агентом сервера](Соединение_с_агентом_сервера_(IServerAgentConnection).md) › [Методы](Соединение_с_агентом_сервера__Методы.md)

Соединение с агентом сервера (IServerAgentConnection)

GetAssignmentRules (GetAssignmentRules)

Доступен, начиная с версии 8.3.1.

Синтаксис:

GetAssignmentRules(<Кластер>, <Сервер>)

Параметры:

<Кластер> (обязательный)

Тип: [Кластер серверов](Кластер_серверов_(IClusterInfo).md).   
Кластер, из которого получается массив требований.

<Сервер> (обязательный)

Тип: [Рабочий сервер](Рабочий_сервер_(IWorkingServerInfo).md).   
Рабочий сервер, из которого получаются требования.

Возвращаемое значение:

Тип: [COMSafeArray](COMSafeArray_(COMSafeArray).md).   
Массив объектов [Требование назначения сервисов](Требование_назначения_сервисов_(IAssignmentRule).md).

Описание:

Получает массив требований назначения сервера, указанного в параметре <Сервер>, и кластера, указанного в параметре <Кластер>. Требования в массиве располагаются в том же порядке, в котором они будут подбираться при назначении новых сервисов.

Доступность:

Интеграция.

Использование в версии:

Доступен, начиная с версии 8.3.1.

---
