# ЭлементDOM.ПолучитьУзелАтрибута

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog1055.md">DOM</a> › [ЭлементDOM](ЭлементDOM.md)

<!-- signature:start -->
`ПолучитьУзелАтрибута`(`<Имя>`: `Строка`)

`ПолучитьУзелАтрибута`(`<URIПространстваИмен>`: `Строка`, `<ЛокальноеИмя>`: `Строка`) → [`АтрибутDOM`](АтрибутDOM.md) | `Неопределено`
<!-- signature:end -->

ЭлементDOM (DOMElement)

ПолучитьУзелАтрибута (GetAttributeNode)

Доступен, начиная с версии 8.1.

Вариант синтаксиса: По имени

Синтаксис:

ПолучитьУзелАтрибута(<Имя>)

Параметры:

<Имя> (обязательный)

Тип: Строка.   
Имя атрибута.

Вариант синтаксиса: По имени и URI пространства имен

Синтаксис:

ПолучитьУзелАтрибута(<URIПространстваИмен>, <ЛокальноеИмя>)

Параметры:

<URIПространстваИмен> (обязательный)

Тип: Строка.   
URI пространства имен атрибута.

<ЛокальноеИмя> (обязательный)

Тип: Строка.   
Локальное имя атрибута.

Возвращаемое значение:

Тип: [АтрибутDOM](АтрибутDOM.md), Неопределено.   
Если у элемента нет атрибута с указанными параметрами, то будет возвращено значение Неопределено.

Описание:

Возвращает узел атрибута по имени.

Доступность:

Сервер, толстый клиент, внешнее соединение, мобильное приложение (сервер), мобильный автономный сервер.

Использование в версии:

Доступен, начиная с версии 8.1.

---
