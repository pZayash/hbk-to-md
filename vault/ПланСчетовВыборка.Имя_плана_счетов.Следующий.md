# ПланСчетовВыборка.<Имя плана счетов>.Следующий

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Прикладные_объекты.md">Прикладные объекты</a> › <a href="obsidian://open?file=Планы_счетов.md">Планы счетов</a> › [ПланСчетовВыборка.<Имя плана счетов>](ПланСчетовВыборка.Имя_плана_счетов.md)

<!-- signature:start -->
`Следующий`() → `Булево`
<!-- signature:end -->

ПланСчетовВыборка.<Имя плана счетов> (ChartOfAccountsSelection.<Chart of accounts name>)

Следующий (Next)

Доступен, начиная с версии 8.0.

Синтаксис:

Следующий()

Возвращаемое значение:

Тип: Булево.   
Истина - следующий элемент выбран; Ложь - достигнут конец выборки.

Описание:

Получает следующий элемент из выборки. Для обхода плана счетов нужно после получения выборки вызвать данный метод для позиционирования на первый элемент и далее вызывать до тех пор, пока не будет возвращено значение Ложь.

Доступность:

Сервер, толстый клиент, внешнее соединение.

Пример:

|  |
| --- |
| Выборка = ПланыСчетов.Основной.Выбрать(); Пока Выборка.Следующий() Цикл     // обработка выборки  КонецЦикла; |

Использование в версии:

Доступен, начиная с версии 8.0.

---
