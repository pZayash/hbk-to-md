# БизнесПроцессОбъект.<Имя бизнес-процесса>.АктивироватьИнтерактивно

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Прикладные_объекты.md">Прикладные объекты</a> › <a href="obsidian://open?file=Бизнес-процессы.md">Бизнес-процессы</a> › [БизнесПроцессОбъект.<Имя бизнес-процесса>](БизнесПроцессОбъект.Имя_бизнес-процесса.md)

<!-- signature:start -->
`АктивироватьИнтерактивно`()
<!-- signature:end -->

БизнесПроцессОбъект.<Имя бизнес-процесса> (BusinessProcessObject.<Business process name>)

АктивироватьИнтерактивно (ActivateInteractively)

Доступен, начиная с версии 8.0.

Синтаксис:

АктивироватьИнтерактивно()

Описание:

Выполняет интерактивную активацию бизнес-процесса. Это приводит к возникновению события [ОбработкаИнтерактивнойАктивации](БизнесПроцессОбъект.Имя_бизнес-процесса.ОбработкаИнтерактивнойАктивации.md). Если обработчик этого события не изменит стандартную обработку то, бизнес-процесс автоматически отбирает все невыполненные задачи для текущего пользователя и вызывает для каждой из них метод [АктивироватьИнтерактивно](ЗадачаОбъект.Имя_задачи.АктивироватьИнтерактивно.md). Текущий пользователь выбирается из параметра сеанса, который назначен в свойстве [Исполнитель](Расширение_табличного_поля_списка_задач.Исполнитель.md) у задачи, связанной с данным бизнес-процессом. Использование этого метода позволяет, например, при создании бизнес-процесса сразу приступать к выполнению заданий, выданных текущему исполнителю, если таковые имеются.

Доступность:

Сервер, толстый клиент, внешнее соединение.

Пример:

|  |
| --- |
| // Создание, старт и активация бизнес-процесса  БизнесПроцесс = БизнесПроцессы.ПодготовкаПрессРелиза.СоздатьБизнесПроцесс(); БизнесПроцесс.Дата = ТекущаяДата(); БизнесПроцесс.Записать(); БизнесПроцесс.Старт(); БизнесПроцесс.АктивироватьИнтерактивно(); |

Использование в версии:

Доступен, начиная с версии 8.0.

---
