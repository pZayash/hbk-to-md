# МенеджерРасширенийКонфигурации.Получить

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Общие объекты](Общие_объекты.md) › [Расширения конфигурации](Расширения_конфигурации.md) › [МенеджерРасширенийКонфигурации](МенеджерРасширенийКонфигурации_(ConfigurationExtensionsManager).md) › [Методы](МенеджерРасширенийКонфигурации__Методы.md)

МенеджерРасширенийКонфигурации (ConfigurationExtensionsManager)

Получить (Get)

Доступен, начиная с версии 8.3.6.

Синтаксис:

Получить(<Отбор>, <ИсточникРасширенийКонфигурации>)

Параметры:

<Отбор> (необязательный)

Тип: [Структура](Структура_(Structure).md).   
Если параметр не задан, возвращаются все возможные расширения.  
Структура для указания отбора может содержать следующие поля:

- <УникальныйИдентификатор> (UUID) - идентификатор расширения.
- <Имя> (Name) - имя расширения.

  
Значение по умолчанию: [Неопределено](lang__def_Undefined.md).

<ИсточникРасширенийКонфигурации> (необязательный)

Тип: [ИсточникРасширенийКонфигурации](ИсточникРасширенийКонфигурации_(ConfigurationExtensionsSource).md).   
Устанавливает источник получаемого расширения.  
Значение по умолчанию: БазаДанных.

Возвращаемое значение:

Тип: [Массив](Массив_(Array).md).   
Массив из объектов вида [РасширениеКонфигурации](РасширениеКонфигурации_(ConfigurationExtension).md), удовлетворяющих заданному критерию отбора.

Описание:

Получает массив расширений конфигурации

Доступность:

Сервер, толстый клиент, внешнее соединение.

Использование в версии:

Доступен, начиная с версии 8.3.6.

---
