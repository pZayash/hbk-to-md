# МенеджерРаботыСРечью.НачатьСинтез

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=Работа_с_речью.md">Работа с речью</a> › [МенеджерРаботыСРечью](МенеджерРаботыСРечью.md)

<!-- signature:start -->
`НачатьСинтез`(`<Текст>`: `Строка`, `<Ключ>`: `Строка`, `<Голос>`: `Строка`, `<ПараметрыСинтеза>`: [`Соответствие`](Соответствие.md), `<ОбработчикОкончанияСинтеза>`: [`ОписаниеОповещения`](ОписаниеОповещения.md))
<!-- signature:end -->

МенеджерРаботыСРечью (SpeechProcessingManager)

НачатьСинтез (StartTextToSpeech)

Доступен, начиная с версии 8.3.25.

Синтаксис:

НачатьСинтез(<Текст>, <Ключ>, <Голос>, <ПараметрыСинтеза>, <ОбработчикОкончанияСинтеза>)

Параметры:

<Текст> (обязательный)

Тип: Строка.   
Текст, по которому генерируется речь.

<Ключ> (необязательный)

Тип: Строка.   
Идентификатор генерации речи.  
Если уже существует активное задание с указанным идентификатором, генерируется исключение.

<Голос> (необязательный)

Тип: Строка.   
Голос синтеза речи.  
Если не указан – голос определяется сервисом.

<ПараметрыСинтеза> (необязательный)

Тип: [Соответствие](Соответствие.md).   
Дополнительные параметры синтеза. Возможный список параметров и их значений зависит от выбранного голоса; будет получен с помощью метода [ПолучитьДоступныеГолосаСинтеза](МенеджерРаботыСРечью.ПолучитьДоступныеГолосаСинтеза.md).  
  
Заполняется на основании [ОписаниеПараметраГолосаСинтезаРечи](ОписаниеПараметраГолосаСинтезаРечи.md).

<ОбработчикОкончанияСинтеза> (необязательный)

Тип: [ОписаниеОповещения](ОписаниеОповещения.md).   
Содержит описание процедуры, которая будет вызвана после окончания генерации речи со следующими параметрами:

- <РезультатГенерации> – тип [ДвоичныеДанные](ДвоичныеДанные.md). Файл WAV с результатом.
- <ДополнительныеПараметры> – значение, которое было указано при создании объекта [ОписаниеОповещения](ОписаниеОповещения.md).

Описание:

Запускает синтез речи. Полученные данные в потоковом режиме отправляются на аудиоустройство по умолчанию. При параллельном начале синтеза речи, речь синтезируется по очереди в порядке начала синтеза, т.е. одно воспроизведение синтезированной речи не накладывается на другое воспроизведение синтезированной речи.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, сервер, толстый клиент, мобильное приложение (клиент).

Использование в версии:

Доступен, начиная с версии 8.3.25.

---
