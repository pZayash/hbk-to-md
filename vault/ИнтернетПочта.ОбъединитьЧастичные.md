# ИнтернетПочта.ОбъединитьЧастичные

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog2442.md">Почта</a> › <a href="obsidian://open?file=ИнтернетПочта.md">ИнтернетПочта</a> › [ИнтернетПочта](ИнтернетПочта-2.md)

<!-- signature:start -->
`ОбъединитьЧастичные`(`<МассивСообщений>`: [`Массив`](Массив.md)) → [`Массив`](Массив.md)
<!-- signature:end -->

ИнтернетПочта (InternetMail)

ОбъединитьЧастичные (CombinePartial)

Доступен, начиная с версии 8.1.

Синтаксис:

ОбъединитьЧастичные(<МассивСообщений>)

Параметры:

<МассивСообщений> (обязательный)

Тип: [Массив](Массив.md).   
Массив, содержащий частичные сообщения для объединения.

Возвращаемое значение:

Тип: [Массив](Массив.md).   
Массив объектов типа [ИнтернетПочтовоеСообщение](ИнтернетПочтовоеСообщение.md).

Описание:

Объединяет частичные сообщения. Если в переданном массиве находятся не все части, то сообщение не объединяется. В передаваемом массиве могут находится части нескольких сообщений. После объединения они возвратятся в массиве. У каждого объединенного сообщения свойство [Идентификатор](ИнтернетПочтовоеСообщение.Идентификатор.md) будет содержать несколько значений, соответствующих взятым из частичных сообщений при объединении.

Доступность:

Тонкий клиент, мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер.

Пример:

|  |
| --- |
| Профиль = Новый ИнтернетПочтовыйПрофиль; ЗаполнитьПрофиль(Профиль);  Почта = Новый ИнтернетПочта; Почта.Подключиться(Профиль);  Масс = Почта.Выбрать(Ложь);  МассЧ = Новый Массив; Для Ин = 0 По Масс.Количество() - 1 Цикл          Если Масс[Ин].Частичное Тогда                  МассЧ.Добавить(Масс[Ин]);              КонецЕсли;          КонецЦикла;  Сообщения = Почта.ОбъединитьЧастичные(МассЧ); Сообщить(" \*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* "); Для Инд = 0 По Сообщения.Количество() - 1 Цикл          Для Каждого Идентификатор Из Сообщения[Инд].Идентификатор Цикл         Сообщить("Серверный идентификатор " + Строка(Инд) + ": " + Идентификатор);     КонецЦикла;      КонецЦикла;  Почта.Отключиться(); |

См. также:

[ИнтернетПочтовоеСообщение](ИнтернетПочтовоеСообщение.md), свойство [Частичное](ИнтернетПочтовоеСообщение.Частичное.md)  

Использование в версии:

Доступен, начиная с версии 8.1.

---
