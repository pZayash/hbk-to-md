# РезультатЗапроса.Выгрузить

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Работа_с_запросами.md">Работа с запросами</a> › <a href="obsidian://open?file=Выполнение_и_работа_с_запросами_во_встроенном_языке.md">Выполнение и работа с запросами во встроенном языке</a> › [РезультатЗапроса](РезультатЗапроса.md)

<!-- signature:start -->
`Выгрузить`(`<ТипОбхода>`: [`ОбходРезультатаЗапроса`](ОбходРезультатаЗапроса.md)) → [`ТаблицаЗначений`](ТаблицаЗначений.md) | [`ДеревоЗначений`](ДеревоЗначений.md)
<!-- signature:end -->

РезультатЗапроса (QueryResult)

Выгрузить (Unload)

Доступен, начиная с версии 8.0.

Синтаксис:

Выгрузить(<ТипОбхода>)

Параметры:

<ТипОбхода> (необязательный)

Тип: [ОбходРезультатаЗапроса](ОбходРезультатаЗапроса.md).   
Задает тип обхода записей в получаемой выборке.  
Значение по умолчанию: [Прямой](ОбходРезультатаЗапроса.Прямой.md).

Возвращаемое значение:

Тип: [ТаблицаЗначений](ТаблицаЗначений.md), [ДеревоЗначений](ДеревоЗначений.md).   
Если тип обхода задан [Прямой](ОбходРезультатаЗапроса.Прямой.md), результат выгружается в таблицу значений, в противном случае в дерево значений.

Описание:

Создает таблицу значений (или дерево значений) и копирует в нее все записи набора.

Доступность:

Сервер, толстый клиент, внешнее соединение, мобильное приложение (сервер), мобильный автономный сервер.

Пример:

|  |
| --- |
| ТаблицаРезультатов = РезультатЗапроса.Выгрузить(); |

Использование в версии:

Доступен, начиная с версии 8.0.

---
