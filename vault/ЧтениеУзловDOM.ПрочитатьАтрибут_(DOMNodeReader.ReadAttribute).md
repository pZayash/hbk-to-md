# ЧтениеУзловDOM.ПрочитатьАтрибут

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Общие объекты](Общие_объекты.md) › [DOM](DOM-2.md) › [ЧтениеУзловDOM](ЧтениеУзловDOM_(DOMNodeReader).md) › [Методы](ЧтениеУзловDOM__Методы.md)

ЧтениеУзловDOM (DOMNodeReader)

ПрочитатьАтрибут (ReadAttribute)

Доступен, начиная с версии 8.1.

Синтаксис:

ПрочитатьАтрибут()

Возвращаемое значение:

Тип: [Булево](lang__Булево_(Boolean).md).   
[Истина](lang__def_BooleanTrue.md) - очередной атрибут прочитан; [Ложь](lang__def_BooleanFalse.md) - атрибутов больше нет.

Описание:

Считывает очередной атрибут элемента XML. После прочтения с помощью метода [ЧтениеУзловDOM.Прочитать](ЧтениеУзловDOM.Прочитать_(DOMNodeReader.Read).md) очередного узла, имеющего тип [НачалоЭлемента](ТипУзлаXML.НачалоЭлемента_(XMLNodeType.StartElement).md), можно получить доступ к атрибутам данного элемента. Каждый вызов данного метода позволяет прочитать очередной атрибут. После прочтения очередного атрибута свойствам [ЧтениеУзловDOM.ТипУзла](ЧтениеУзловDOM.ТипУзла_(DOMNodeReader.NodeType).md), [ЧтениеУзловDOM.Имя](ЧтениеУзловDOM.Имя_(DOMNodeReader.Name).md), [ЧтениеУзловDOM.ЛокальноеИмя](ЧтениеУзловDOM.ЛокальноеИмя_(DOMNodeReader.LocalName).md), [ЧтениеУзловDOM.Префикс](ЧтениеУзловDOM.Префикс_(DOMNodeReader.Prefix).md), [ЧтениеУзловDOM.URIПространстваИмен](ЧтениеУзловDOM.URIПространстваИмен_(DOMNodeReader.NamespaceURI).md) и [ЧтениеУзловDOM.Значение](ЧтениеУзловDOM.Значение_(DOMNodeReader.Value).md) присваиваются значения в соответствии с прочитанным атрибутом.

Доступность:

Тонкий клиент, мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер.

Пример:

|  |
| --- |
| Чтение.ОткрытьФайл("c:/docs/data.xml"); Пока Чтение.Прочитать() Цикл     Если Чтение.КоличествоАтрибутов() > 0 Тогда         Пока Чтение.ПрочитатьАтрибут() Цикл             // Обработка атрибута          КонецЦикла     КонецЕсли КонецЦикла |

Использование в версии:

Доступен, начиная с версии 8.1.

---
