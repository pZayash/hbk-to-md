# Глобальный контекст.УстановитьСоставСтандартногоИнтерфейсаOData

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Глобальный контекст](Глобальный_контекст.md) › [Методы](Методы.md) › [Процедуры и функции работы со стандартным интерфейсом OData](Процедуры_и_функции_работы_со_стандартным_интерфейсом_OData.md)

Глобальный контекст (Global context)

УстановитьСоставСтандартногоИнтерфейсаOData (SetStandardODataInterfaceContent)

Доступен, начиная с версии 8.3.5.

Синтаксис:

УстановитьСоставСтандартногоИнтерфейсаOData(<ОбъектыМетаданных>)

Параметры:

<ОбъектыМетаданных> (обязательный)

Тип: [Массив](Массив_(Array).md).   
Массив объектов метаданных и/или имен объектов метаданных.

Описание:

Устанавливает состав объектов метаданных, доступных через стандартный сервис OData API.  
Объекты, доступные для включения в стандартный OData API:

- Справочник.<ИмяСправочника>
- Документ.<ИмяДокумента>
- ПланОбмена.<ИмяПланаОбмена>
- ПланСчетов.<ИмяПланаСчетов>
- ПланВидовРасчета.<ИмяПланаВидовРасчета>
- ПланВидовХарактеристик.<ИмяПланаВидовХарактеристик>
- РегистрБухгалтерии.<ИмяРегистраБухгалтерии>
- РегистрСведений.<ИмяРегистраСведений>
- РегистрРасчета.<ИмяРегистраРасчета>
- РегистрНакопления.<ИмяРегистраНакопления>
- ЖурналДокументов.<ИмяЖурналаДокументов>
- Перечисление.<ИмяПеречисления>
- Задача.<ИмяЗадачи>
- БизнесПроцесс.<ИмяБизнесПроцесса>
- Константа.<ИмяКонстанты>

Доступность:

Сервер, толстый клиент, внешнее соединение, интеграция.

Пример:

|  |
| --- |
| Массив = Новый Массив();      Для Каждого Справочник Из Метаданные.Справочники Цикл     Массив.Добавить(Справочник); КонецЦикла;  УстановитьСоставСтандартногоИнтерфейсаOData(Массив); |

См. также:

[Глобальный контекст](Глобальный_контекст.md), метод [ПолучитьСоставСтандартногоИнтерфейсаOData](Глобальный_контекст.ПолучитьСоставСтандартногоИнтерфейсаOData_(Global_context.GetStandardODataInterfaceContent).md)  

Использование в версии:

Доступен, начиная с версии 8.3.5.

---
