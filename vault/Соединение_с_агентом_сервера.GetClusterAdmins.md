# Соединение с агентом сервера.GetClusterAdmins

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Средства_интеграции_и_администрирования.md">Средства интеграции и администрирования</a> › <a href="obsidian://open?file=Менеджер_COM-соединений.md">Менеджер COM-соединений</a> › <a href="obsidian://open?file=Администрирование_кластера_серверов.md">Администрирование кластера серверов</a> › [Соединение с агентом сервера](Соединение_с_агентом_сервера.md)

<!-- signature:start -->
`GetClusterAdmins`(`<Кластер>`: [`Кластер серверов`](Кластер_серверов.md)) → [`COMSafeArray`](COMSafeArray.md)
<!-- signature:end -->

Соединение с агентом сервера (IServerAgentConnection)

GetClusterAdmins (GetClusterAdmins)

Доступен, начиная с версии 8.1.

Синтаксис:

GetClusterAdmins(<Кластер>)

Параметры:

<Кластер> (обязательный)

Тип: [Кластер серверов](Кластер_серверов.md).   
Описание кластера с интерфейсом [Администратор](Администратор.md), администраторов которых необходимо получить.

Возвращаемое значение:

Тип: [COMSafeArray](COMSafeArray.md).   
Массив описаний администраторов. Каждый элемент массива является объектом с интерфейсом IRegUserInfo.

Описание:

Получает массив администраторов кластера. Про каждого администратора доступна вся информация, кроме его пароля. Для успешного выполнения метода необходима аутентификация одного из администраторов кластера. Если не зарегистрирован ни один администратор кластера, то перед выполнением этого метода следует выполнить метод [Authenticate](Соединение_с_агентом_сервера.Authenticate.md)(Кластер, "", ""), где Кластер - кластер, массив администраторов которого требуется получить.

Доступность:

Интеграция.

Использование в версии:

Доступен, начиная с версии 8.1.

---
