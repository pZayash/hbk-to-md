# ПреобразованиеXSL.УдалитьПараметр

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog565.md">XML</a> › [ПреобразованиеXSL](ПреобразованиеXSL.md)

<!-- signature:start -->
`УдалитьПараметр`(`<ПолноеИмя>`: `Строка`)

`УдалитьПараметр`(`<ЛокальноеИмя>`: `Строка`, `<URIПространстваИмен>`: `Строка`)
<!-- signature:end -->

ПреобразованиеXSL (XSLTransform)

УдалитьПараметр (RemoveParameter)

Доступен, начиная с версии 8.0.

Вариант синтаксиса: По полному имени

Синтаксис:

УдалитьПараметр(<ПолноеИмя>)

Параметры:

<ПолноеИмя> (обязательный)

Тип: Строка.   
Полное имя параметра.

Вариант синтаксиса: По имени и URI пространства имен

Синтаксис:

УдалитьПараметр(<ЛокальноеИмя>, <URIПространстваИмен>)

Параметры:

<ЛокальноеИмя> (обязательный)

Тип: Строка.   
Локальное имя параметра.

<URIПространстваИмен> (обязательный)

Тип: Строка.   
URI пространства имен параметра.

Описание:

Удаляет значение параметра.

Доступность:

Тонкий клиент, сервер, толстый клиент, внешнее соединение.

Примечание:

Отменяет ранее заданное значение параметра. Параметр принимает значение, определенное в описании XSL-преобразования.

Пример:

|  |
| --- |
| Преобразование.УдалитьПараметр("low\_sales", 30000); |

Использование в версии:

Доступен, начиная с версии 8.0.

---
