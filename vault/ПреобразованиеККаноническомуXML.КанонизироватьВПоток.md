# ПреобразованиеККаноническомуXML.КанонизироватьВПоток

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog565.md">XML</a> › [ПреобразованиеККаноническомуXML](ПреобразованиеККаноническомуXML.md)

<!-- signature:start -->
`КанонизироватьВПоток`(`<ДокументDOM>`: [`ДокументDOM`](ДокументDOM.md), `<Поток>`: [`Поток`](Поток.md) | [`ПотокВПамяти`](ПотокВПамяти.md) | [`ФайловыйПоток`](ФайловыйПоток.md), `<ТипКаноническогоXML>`: [`ТипКаноническогоXML`](ТипКаноническогоXML.md), `<ФильтрXPath>`: [`РезультатXPath`](РезультатXPath.md) | `Строка`, `<ПрефиксыДляВключения>`: `Строка`)
<!-- signature:end -->

ПреобразованиеККаноническомуXML (ConversionToCanonicalXML)

КанонизироватьВПоток (CanonicalizeToStream)

Доступен, начиная с версии 8.3.10.

Синтаксис:

КанонизироватьВПоток(<ДокументDOM>, <Поток>, <ТипКаноническогоXML>, <ФильтрXPath>, <ПрефиксыДляВключения>)

Параметры:

<ДокументDOM> (обязательный)

Тип: [ДокументDOM](ДокументDOM.md).   
Документ DOM, который требуется записать в каноническом виде.

<Поток> (обязательный)

Тип: [Поток](Поток.md), [ПотокВПамяти](ПотокВПамяти.md), [ФайловыйПоток](ФайловыйПоток.md).   
Поток, в который будет записан текст XML.

<ТипКаноническогоXML> (необязательный)

Тип: [ТипКаноническогоXML](ТипКаноническогоXML.md).   
Тип алгоритма приведения к каноническому виду.  
Значение по умолчанию: [КаноническийXML1\_1](ТипКаноническогоXML.КаноническийXML1_1.md).

<ФильтрXPath> (необязательный)

Тип: [РезультатXPath](РезультатXPath.md), Строка.   
Если передана Строка, то перед преобразованием к каноническому виду будет выполнен запрос XPath из этой строки, и каноническая форма XML-документа будет содержать только результирующее подмножество документа.  
Если передан объект типа [РезультатXPath](РезультатXPath.md), то к каноническому виду будет преобразовано подмножество узлов из переданного параметра.  
Значение по умолчанию: Неопределено.

<ПрефиксыДляВключения> (необязательный)

Тип: Строка.   
Содержит список префиксов пространств имен для включения в формате: "Префикс1 Префикс2", и так далее.  
Префиксы для включения используются в алгоритме исключающего канонического XML (см. стандарт W3C Exclusive XML Canonicalization - https://www.w3.org/TR/xml-exc-c14n/).   
В остальных случаях параметр игнорируется.  
Значение по умолчанию: Неопределено.

Описание:

Записывает XML-документ в поток в каноническом виде.

Доступность:

Тонкий клиент, сервер, толстый клиент, внешнее соединение.

Использование в версии:

Доступен, начиная с версии 8.3.10.

---
