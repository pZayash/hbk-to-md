# Картинка.Преобразовать

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=Картинка.md">Картинка</a> › [Картинка](Картинка-2.md)

<!-- signature:start -->
`Преобразовать`(`<ФорматКартинки>`: [`ФорматКартинки`](ФорматКартинки.md), `<ВсеВарианты>`: `Булево`, `<ПодобратьВариант>`: [`Соответствие`](Соответствие.md) | `Неопределено`) → [`Картинка`](Картинка.md) | `Неопределено`
<!-- signature:end -->

Картинка (Picture)

Преобразовать (Convert)

Не рекомендуется использовать, начиная с версии 8.3.14.

Доступен, начиная с версии 8.0.

Синтаксис:

Преобразовать(<ФорматКартинки>, <ВсеВарианты>, <ПодобратьВариант>)

Параметры:

<ФорматКартинки> (необязательный)

Тип: [ФорматКартинки](ФорматКартинки.md).   
Определяет формат картинки.

<ВсеВарианты> (необязательный)

Тип: Булево.   
Данный параметр используется, если картинка представлена архивом, содержащим варианты этой картинки. Если значение Ложь - будет преобразована одна картинка. Если значение Истина - все варианты картинки.  
Значение по умолчанию: Ложь.

<ПодобратьВариант> (необязательный)

Тип: [Соответствие](Соответствие.md), Неопределено.   
Данный параметр используется только, если картинка представлена архивом, содержащим варианты этой картинки, и <ВсеВарианты> установлен в значение Ложь.  
Пары "Ключ-Значение" соответствуют xml-тегам в манифесте вариантов картинки. Если параметры выбора варианта не заданы, будет выбран вариант по умолчанию. Если параметры указывают на несуществующий в архиве вариант, то будет возвращено Неопределено.  
Значение по умолчанию: Неопределено.

Возвращаемое значение:

Тип: [Картинка](Картинка.md), Неопределено.   

Описание:

Создает копию картинки и преобразовывает ее в заданный формат.

Доступность:

Тонкий клиент, мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер.

Примечание:

Картинки растровых форматов и векторного формата SVG преобразовываются в форматы BMP и PNG.  
Картинки других векторных форматов не преобразовываются.  
Для многокадровых картинок (Icon и анимированный GIF) преобразуется первый кадр.  
В мобильной версии платформы доступно преобразование только в форматы PNG и JPEG.

Использование в версии:

Доступен, начиная с версии 8.0.

Не рекомендуется использовать, начиная с версии 8.3.14.

Описание изменено в версии 8.3.22.

---
