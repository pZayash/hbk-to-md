# ИсточникДанныхСводнойТаблицыКомпоновкиДанных.ПолучитьНастройки

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Общие объекты](Общие_объекты.md) › [Система компоновки данных](Система_компоновки_данных-2.md) › [Работа с механизмом](Работа_с_механизмом.md) › [ИсточникДанныхСводнойТаблицыКомпоновкиДанных](ИсточникДанныхСводнойТаблицыКомпоновкиДанных_(DataCompositionPivotTableDataSource).md) › [Методы](ИсточникДанныхСводнойТаблицыКомпоновкиДанных__Методы.md)

ИсточникДанныхСводнойТаблицыКомпоновкиДанных (DataCompositionPivotTableDataSource)

ПолучитьНастройки (GetSettings)

Доступен, начиная с версии 8.1.

Синтаксис:

ПолучитьНастройки()

Возвращаемое значение:

Тип: [НастройкиКомпоновкиДанных](НастройкиКомпоновкиДанных_(DataCompositionSettings).md).   

Описание:

Получает текущее состояние сводной таблицы в виде настроек системы компоновки данных.  
Настройки содержат таблицу, группировки которой соответствуют выбранным измерениям сводной таблицы, выбранные поля группировок соответствуют выбранным реквизитам.  
Выбранные ресурсы сохраняются как выбранные поля в настройках.  
Также настройки могут содержать отбор и упорядочивание, которые сохранены после последней установки настроек в сводную таблицу.

Доступность:

Сервер, толстый клиент, внешнее соединение.

Примечание:

Метод может быть вызван после того, как в источник была установлена схема компоновки данных (метод [УстановитьСхему](ИсточникДанныхСводнойТаблицыКомпоновкиДанных.УстановитьСхему_(DataCompositionPivotTableDataSource.SetSchema).md)) и источник был установлен в сводную таблицу.

Использование в версии:

Доступен, начиная с версии 8.1.

---
