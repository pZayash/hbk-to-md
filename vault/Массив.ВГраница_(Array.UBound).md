# Массив.ВГраница

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Универсальные коллекции значений](Универсальные_коллекции_значений.md) › [Массив](Массив_(Array).md) › [Методы](Массив__Методы.md)

Массив (Array)

ВГраница (UBound)

Доступен, начиная с версии 8.0.

Синтаксис:

ВГраница()

Возвращаемое значение:

Тип: [Число](lang__def_Number.md).   
Наибольший индекс в массиве. Если количество элементов массива равно 0, возвращает -1.

Описание:

Получает наибольший индекс элемента массива.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер.

Примечание:

Наибольший индекс соответствует количеству элементов массива минус 1.

Пример:

|  |
| --- |
| Для Индекс = 0 по Массив.ВГраница() Цикл     Массив[Индекс] = Массив[Индекс] + 1; КонецЦикла; |

Использование в версии:

Доступен, начиная с версии 8.0.

---
