# ОбработкаМенеджер.<Имя обработки>.ПолучитьФорму

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Прикладные_объекты.md">Прикладные объекты</a> › <a href="obsidian://open?file=Обработки.md">Обработки</a> › [ОбработкаМенеджер.<Имя обработки>](ОбработкаМенеджер.Имя_обработки.md)

<!-- signature:start -->
`ПолучитьФорму`(`<Форма>`: `Строка` | [`ОбъектМетаданных: Форма`](ОбъектМетаданных_Форма.md), `<Владелец>`: [`Форма`](Форма.md) | `; Элемент управления`, `<КлючУникальности>`: `Произвольный`) → [`Форма`](Форма.md) | `Неопределено`
<!-- signature:end -->

ОбработкаМенеджер.<Имя обработки> (DataProcessorManager.<Data processor name>)

ПолучитьФорму (GetForm)

Доступен, начиная с версии 8.0.

Синтаксис:

ПолучитьФорму(<Форма>, <Владелец>, <КлючУникальности>)

Параметры:

<Форма> (необязательный)

Тип: Строка, [ОбъектМетаданных: Форма](ОбъектМетаданных_Форма.md) .   
Имя формы, как оно задано в конфигураторе, или объект описания метаданного требуемой формы. Если параметр не указан, то используется основная форма, заданная в конфигураторе.

<Владелец> (необязательный)

Тип: [Форма](Форма.md); Элемент управления.   
Объект, который будет являться владельцем открываемой формы.

<КлючУникальности> (необязательный)

Тип: Произвольный.   
В данном параметре может быть задан некоторый ключ, значение которого будет использоваться для поиска уже открытых форм.   
При поиске учитываются значения свойств открытых форм соответствующим значениям параметров метода. Если найдена открытая форма (по совпадению всех параметров), то она будет возвращена вместо новой формы.   
Если из некой формы (один и тот же владелец) требуется открывать новые экземпляры другой формы, то при каждом вызове метода значение данного параметра должно быть уникально (в рамках соответствующих значений свойств открытых форм).  
Значение параметра присваивается свойству открываемой формы [КлючУникальности](Форма.КлючУникальности.md).  
Следует учитывать, что при получении формы из "объекта" система получает форму для конкретного экземпляра объекта с учетом совокупности переданных параметров, в то время как для "ссылки" всегда существует только один объект в базе данных и форма получается одна (для одинаковой совокупности параметров).

Возвращаемое значение:

Тип: [Форма](Форма.md), Неопределено.   
Неопределено - если в обработчике [ПриСозданииНаСервере](ФормаКлиентскогоПриложения.ПриСозданииНаСервере.md) установлен признак отказа от создания формы.

Описание:

Получает форму обработки.

Доступность:

Толстый клиент.

Пример:

|  |
| --- |
| ИдФормы = "ОсновнаяФорма"; Форма = Обработки.ОбработкаДокументов.ПолучитьФорму(ИдФормы); |

Использование в версии:

Доступен, начиная с версии 8.0.

---
