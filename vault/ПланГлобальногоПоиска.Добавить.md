# ПланГлобальногоПоиска.Добавить

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=Глобальный_поиск.md">Глобальный поиск</a> › [ПланГлобальногоПоиска](ПланГлобальногоПоиска.md)

<!-- signature:start -->
`Добавить`(`<Значение>`: [`ЭлементПланаГлобальногоПоиска`](ЭлементПланаГлобальногоПоиска.md))

`Добавить`(`<ВидПоиска>`: [`СтандартныйВидГлобальногоПоиска`](СтандартныйВидГлобальногоПоиска.md), `<Порядок>`: `Число`)

`Добавить`(`<ИмяПроцедуры>`: `Строка`, `<Модуль>`: `Строка`, `<НаСервере>`: `Булево`, `<Фоновый>`: `Булево`, `<Порядок>`: `Число`, `<ДополнительныеПараметры>`: `Произвольный`)
<!-- signature:end -->

ПланГлобальногоПоиска (GlobalSearchPlan)

Добавить (Add)

Доступен, начиная с версии 8.3.15.

Вариант синтаксиса: По значению

Синтаксис:

Добавить(<Значение>)

Параметры:

<Значение> (обязательный)

Тип: [ЭлементПланаГлобальногоПоиска](ЭлементПланаГлобальногоПоиска.md).   
Вставляемое значение.

Вариант синтаксиса: По виду поиска

Синтаксис:

Добавить(<ВидПоиска>, <Порядок>)

Параметры:

<ВидПоиска> (обязательный)

Тип: [СтандартныйВидГлобальногоПоиска](СтандартныйВидГлобальногоПоиска.md).   
Вид поиска.

<Порядок> (необязательный)

Тип: Число.   
Порядок выдачи результатов поиска.  
Значение по умолчанию: 0.

Вариант синтаксиса: По обработчику

Синтаксис:

Добавить(<ИмяПроцедуры>, <Модуль>, <НаСервере>, <Фоновый>, <Порядок>, <ДополнительныеПараметры>)

Параметры:

<ИмяПроцедуры> (обязательный)

Тип: Строка.   
Имя экспортируемой процедуры общего модуля.

<Модуль> (обязательный)

Тип: Строка.   
Имя общего модуля.

<НаСервере> (обязательный)

Тип: Булево.   
Признак выполнения поиска на сервере.

<Фоновый> (необязательный)

Тип: Булево.   
Признак выполнения поиска в фоновом задании.  
Значение по умолчанию: Ложь.

<Порядок> (необязательный)

Тип: Число.   
Порядок выдачи результатов поиска.  
Значение по умолчанию: 0.

<ДополнительныеПараметры> (необязательный)

Тип: Произвольный.   
Дополнительные параметры, передаваемые в вызываемую процедуру.  
Значение по умолчанию: Неопределено.

Описание:

Добавляет элемент в конец коллекции плана глобального поиска.

Доступность:

Тонкий клиент, веб-клиент, мобильный клиент, толстый клиент, внешнее соединение, мобильное приложение (клиент).

Использование в версии:

Доступен, начиная с версии 8.3.15.

---
