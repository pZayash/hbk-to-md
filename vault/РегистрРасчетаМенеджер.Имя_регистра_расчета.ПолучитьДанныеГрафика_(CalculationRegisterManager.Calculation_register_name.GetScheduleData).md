# РегистрРасчетаМенеджер.<Имя регистра расчета>.ПолучитьДанныеГрафика

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Прикладные объекты](Прикладные_объекты.md) › [Регистры расчета](Регистры_расчета.md) › [РегистрРасчетаМенеджер.<Имя регистра расчета>](РегистрРасчетаМенеджер.Имя_регистра_расчета_(CalculationRegisterManager.Calculation_register_name).md) › [Методы](РегистрРасчетаМенеджер.Имя_регистра_расчета__Методы.md)

РегистрРасчетаМенеджер.<Имя регистра расчета> (CalculationRegisterManager.<Calculation register name>)

ПолучитьДанныеГрафика (GetScheduleData)

Доступен, начиная с версии 8.0.

Синтаксис:

ПолучитьДанныеГрафика(<Отбор>, <ВидПериода>)

Параметры:

<Отбор> (обязательный)

Тип: [Структура](Структура_(Structure).md), [Отбор](Отбор_(Filter).md).   
Если параметр имеет тип [Структура](Структура_(Structure).md), то представляет собой фильтр для записей регистра, по которым нужно получить данные графика. Имена элементов структуры должны быть или словом "Регистратор" и/или совпадать с именами измерений регистра расчета. Значения элементов структуры представляют собственно значения отбора. Должно присутствовать как минимум условие на регистратор.  
Если параметр имеет тип [Отбор](Отбор_(Filter).md), то представляет отбор, налагаемый на записи регистра, по которым нужно получить данные графика. В отборе должен быть использован как минимум элемент отбора по регистратору. Все элементы отбора должны устанавливать условие с вариантом сравнения "Равно".

<ВидПериода> (обязательный)

Тип: [ВидПериодаРегистраРасчета](ВидПериодаРегистраРасчета_(CalculationRegisterPeriodType).md).   
Вид периода регистра расчета.  
Если [ПериодДействия](ВидПериодаРегистраРасчета.ПериодДействия_(CalculationRegisterPeriodType.ActionPeriod).md), возвращаются данные календаря, собранные по фактическому периоду действия.  
Если [БазовыйПериод](ВидПериодаРегистраРасчета.БазовыйПериод_(CalculationRegisterPeriodType.BasePeriod).md) - данные по базовому периоду.  
Если [ПериодРегистрации](ВидПериодаРегистраРасчета.ПериодРегистрации_(CalculationRegisterPeriodType.RegistrationPeriod).md) - по данные календаря по периоду регистрации. Если, например, регистр расчета имеет месячную периодичность, то суммируются данные календаря за весь месяц, в котором зарегистрирована запись регистра расчета.  
Если выбран [ФактическийПериодДействия](ВидПериодаРегистраРасчета.ФактическийПериодДействия_(CalculationRegisterPeriodType.ActualActionPeriod).md), то приводит к вычислению данных графика по фактическому периоду действия записей (записи) регистра расчета.

Возвращаемое значение:

Тип: [ТаблицаЗначений](ТаблицаЗначений_(ValueTable).md).   
Таблица значений содержит две колонки - номер записи и полученные данные календаря.

Описание:

Получает календаря для движений удовлетворяющих заданному отбору. Число строк в возвращаемой таблице значений совпадает с числом записей движений по заданному отбору.

Доступность:

Сервер, толстый клиент, внешнее соединение.

Пример:

|  |
| --- |
| МенеджерРегистра = РегистрыРасчета["ОсновныеНачисленияСотрудниковОрганизаций"]; ТаблицаНормыВремени = МенеджерРегистра.ПолучитьДанныеГрафика(Отбор,          ВидПериодаРегистраРасчета.ПериодДействия); |

Использование в версии:

Доступен, начиная с версии 8.0.

---
