# ЗаписьZipФайла.Добавить

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Общие объекты](Общие_объекты.md) › [ZIP](ZIP-2.md) › [ЗаписьZipФайла](ЗаписьZipФайла_(ZipFileWriter).md) › [Методы](ЗаписьZipФайла__Методы.md)

ЗаписьZipФайла (ZipFileWriter)

Добавить (Add)

Не рекомендуется использовать, начиная с версии 8.3.26.

Рекомендуется использовать:

- [Добавить](ЗаписьФайлаАрхива.Добавить_(ArchiveFileWriter.Add).md)

Доступен, начиная с версии 8.0.

Синтаксис:

Добавить(<ИмяФайла>, <МетодСохраненияПутей>, <РекурсивнаяОбработкаПодпапок>)

Параметры:

<ИмяФайла> (обязательный)

Тип: [Строка](lang__def_String.md).   
Имя файла, помещаемого в архив, или маска.  
Недопустимо использование в именах файлов и папок букв национальных алфавитов, которые при преобразовании из символов UNICODE в узкие символы могут быть преобразованы с потерей информации. Рекомендуется использовать в именах файлов и папок символы латинского алфавита.  
Запрещены символы: > < | ? \* / \ : ".

<МетодСохраненияПутей> (необязательный)

Тип: [РежимСохраненияПутейZIP](РежимСохраненияПутейZIP_(ZIPStorePathMode).md).   
Метод сохранения путей к файлу в архиве.  
Значение по умолчанию: [НеСохранятьПути](РежимСохраненияПутейZIP.НеСохранятьПути_(ZIPStorePathMode.DontStorePath).md).

<РекурсивнаяОбработкаПодпапок> (необязательный)

Тип: [РежимОбработкиПодкаталоговZIP](РежимОбработкиПодкаталоговZIP_(ZIPSubDirProcessingMode).md).   
В случае добавления файлов по маске обрабатывать дополнительно подкаталоги.  
Значение по умолчанию: [НеОбрабатывать](РежимОбработкиПодкаталоговZIP.НеОбрабатывать_(ZIPSubDirProcessingMode.DontProcess).md).

Описание:

Добавляет новый файл в архив.

Доступность:

Тонкий клиент, сервер, толстый клиент, внешнее соединение.

Примечание:

Если при открытии в параметре <ИмяФайла> метода [Открыть](ЗаписьZipФайла.Открыть_(ZipFileWriter.Open).md) был указан существующий файл ZIP архива, то выполнение данного метода не приводит к добавлению указанного файла к существующему архиву. Прежний архив при выполнении метода [Записать](ЗаписьZipФайла.Записать_(ZipFileWriter.Write).md) будет полностью заменен на новый, состав которого формируется данным методом.  
Добавление в архив файла текущего архива вызывает исключительную ситуацию при выполнении метода [Записать](ЗаписьZipФайла.Записать_(ZipFileWriter.Write).md).  
Параметр <МетодСохраненияПутей> имеет смысл указывать, если в архив добавляется один файл или группа файлов по указанной в параметре <ИмяФайла> маске.

Использование в версии:

Доступен, начиная с версии 8.0.

Не рекомендуется использовать, начиная с версии 8.3.26.

---
