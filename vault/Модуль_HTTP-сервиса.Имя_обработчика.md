# Модуль HTTP-сервиса.<Имя обработчика>

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=HTTP-сервисы.md">HTTP-сервисы</a> › [Модуль HTTP-сервиса](Модуль_HTTP-сервиса.md)

Модуль HTTP-сервиса (HTTP-service module)

<Имя обработчика> (<Handler name>)

Доступен, начиная с версии 8.3.5.

Синтаксис:

<Имя обработчика>(<Запрос>)

Параметры:

<Запрос>

Тип: [HTTPСервисЗапрос](HTTPСервисЗапрос.md).   
Содержит клиентский HTTP-запрос.

Возвращаемое значение:

Тип: [HTTPСервисОтвет](HTTPСервисОтвет.md).   

Описание:

Обрабатывает клиентские HTTP-запросы.

Доступность:

Сервер, толстый клиент.

Использование в версии:

Доступен, начиная с версии 8.3.5.

---
