# КоллекцияПакетовXDTO.Получить

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog1015.md">XDTO</a> › [КоллекцияПакетовXDTO](КоллекцияПакетовXDTO.md)

<!-- signature:start -->
`Получить`(`<Индекс>`: `Число`)

`Получить`(`<URIПространстваИмен>`: `Строка`) → [`ПакетXDTO`](ПакетXDTO.md)
<!-- signature:end -->

КоллекцияПакетовXDTO (XDTOPackageCollection)

Получить (Get)

Доступен, начиная с версии 8.1.

Вариант синтаксиса: По индексу

Синтаксис:

Получить(<Индекс>)

Параметры:

<Индекс> (обязательный)

Тип: Число.   
Индекс элемента коллекции.

Вариант синтаксиса: По URI пространства имен

Синтаксис:

Получить(<URIПространстваИмен>)

Параметры:

<URIПространстваИмен> (обязательный)

Тип: Строка.   
URI пространства имен пакета.

Возвращаемое значение:

Тип: [ПакетXDTO](ПакетXDTO.md).   

Описание:

Получает пакет XDTO по индексу или имени.

Доступность:

Тонкий клиент, мобильный клиент, сервер, толстый клиент, внешнее соединение, мобильное приложение (клиент), мобильное приложение (сервер), мобильный автономный сервер.

Использование в версии:

Доступен, начиная с версии 8.1.

---
