# РегистрРасчетаНаборЗаписей.<Имя регистра расчета>.ЗагрузитьКолонку

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Прикладные_объекты.md">Прикладные объекты</a> › <a href="obsidian://open?file=Регистры_расчета.md">Регистры расчета</a> › [РегистрРасчетаНаборЗаписей.<Имя регистра расчета>](РегистрРасчетаНаборЗаписей.Имя_регистра_расчета.md)

<!-- signature:start -->
`ЗагрузитьКолонку`(`<Массив>`: [`Массив`](Массив.md), `<ИмяПоля>`: `Строка`)
<!-- signature:end -->

РегистрРасчетаНаборЗаписей.<Имя регистра расчета> (CalculationRegisterRecordSet.<Calculation register name>)

ЗагрузитьКолонку (LoadColumn)

Доступен, начиная с версии 8.0.

Синтаксис:

ЗагрузитьКолонку(<Массив>, <ИмяПоля>)

Параметры:

<Массив> (обязательный)

Тип: [Массив](Массив.md).   
Массив, содержащий значения для заполнения колонки набора.

<ИмяПоля> (обязательный)

Тип: Строка.   
Идентификатор поля записи, в которую будут загружены значения из массива.

Описание:

Загружает значения из массива в заданное поле набора записей.

Доступность:

Сервер, толстый клиент, внешнее соединение.

Примечание:

Значения из массива загружаются в поле в порядке индексов.

Использование в версии:

Доступен, начиная с версии 8.0.

---
