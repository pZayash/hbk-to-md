# ИсточникДанныхСводнойТаблицыКомпоновкиДанных.УстановитьСхему

**↑** [Главная](_index.md) › [Объекты](_index__Объекты.md) › [Общие объекты](Общие_объекты.md) › [Система компоновки данных](Система_компоновки_данных-2.md) › [Работа с механизмом](Работа_с_механизмом.md) › [ИсточникДанныхСводнойТаблицыКомпоновкиДанных](ИсточникДанныхСводнойТаблицыКомпоновкиДанных_(DataCompositionPivotTableDataSource).md) › [Методы](ИсточникДанныхСводнойТаблицыКомпоновкиДанных__Методы.md)

ИсточникДанныхСводнойТаблицыКомпоновкиДанных (DataCompositionPivotTableDataSource)

УстановитьСхему (SetSchema)

Доступен, начиная с версии 8.1.

Синтаксис:

УстановитьСхему(<Схема>)

Параметры:

<Схема> (обязательный)

Тип: [СхемаКомпоновкиДанных](СхемаКомпоновкиДанных_(DataCompositionSchema).md).   
Устанавливаемая схема компоновки данных.

Описание:

Устанавливает схему компоновки данных в источник сводной таблицы.   
Схема компоновки данных определяет перечень ресурсов, измерений и реквизитов, которыми можно будет манипулировать в сводной таблице.

Доступность:

Сервер, толстый клиент, внешнее соединение.

Примечание:

Метод должен быть вызван до установки источника данных в сводную таблицу.

Использование в версии:

Доступен, начиная с версии 8.1.

---
