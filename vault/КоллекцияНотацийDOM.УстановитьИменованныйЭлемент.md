# КоллекцияНотацийDOM.УстановитьИменованныйЭлемент

**↑** <a href="obsidian://open?file=_index.md">Главная</a> › <a href="obsidian://open?file=_index__Объекты.md">Объекты</a> › <a href="obsidian://open?file=Общие_объекты.md">Общие объекты</a> › <a href="obsidian://open?file=objects__catalog63__catalog1055.md">DOM</a> › [КоллекцияНотацийDOM](КоллекцияНотацийDOM.md)

<!-- signature:start -->
`УстановитьИменованныйЭлемент`(`<Нотация>`: [`НотацияDOM`](НотацияDOM.md)) → [`НотацияDOM`](НотацияDOM.md)
<!-- signature:end -->

КоллекцияНотацийDOM (DOMNotationMap)

УстановитьИменованныйЭлемент (SetNamedItem)

Доступен, начиная с версии 8.1.

Синтаксис:

УстановитьИменованныйЭлемент(<Нотация>)

Параметры:

<Нотация> (обязательный)

Тип: [НотацияDOM](НотацияDOM.md).   
Узел Нотации DOM.

Возвращаемое значение:

Тип: [НотацияDOM](НотацияDOM.md).   

Описание:

Устанавливает нотацию DOM.  
В качестве ключа в коллекции будет использовано имя узла.  
Если в коллекции присутствует узел нотации с таким же именем, то он будет замещен и возвращен в качестве возвращаемого значения.

Доступность:

Сервер, толстый клиент, внешнее соединение, мобильное приложение (сервер), мобильный автономный сервер.

Использование в версии:

Доступен, начиная с версии 8.1.

---
